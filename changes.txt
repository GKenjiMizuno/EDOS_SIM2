EDOS_SIM2 - Registro de Alterações (Change Log)
================================================

Branch: develop_calibrating_treshold
Objetivo geral: remover o teto artificial de capacidade da instância alvo (GIL)
e o auto-throttling do injetor de ataque, para permitir calibrar com precisão
os limiares de CPU que disparam o autoscaling (base para o ataque Yo-Yo).

--------------------------------------------------------------------------------
1) config.py
--------------------------------------------------------------------------------
Adicionado:
  INSTANCE_MAX_CONCURRENT_REQUESTS = 2
  HTTP_ATTACK_MAX_CONCURRENT_SENDS = 64

Por quê:
  - INSTANCE_MAX_CONCURRENT_REQUESTS define explicitamente quantos processos
    concorrentes uma instância pode usar para atender requisições (substitui
    o teto acidental que vinha do GIL do Python quando o servidor usava threads).
  - HTTP_ATTACK_MAX_CONCURRENT_SENDS limita quantas requisições de ataque podem
    estar "em voo" ao mesmo tempo, evitando crescimento ilimitado de threads/tarefas
    caso o alvo não consiga responder no ritmo configurado.

--------------------------------------------------------------------------------
2) app/simple_server.py
--------------------------------------------------------------------------------
Alterado:
  - Import: "ThreadingHTTPServer" trocado por "HTTPServer" + "socketserver".
  - Nova classe ForkingHTTPServer(socketserver.ForkingMixIn, HTTPServer), com
    max_children lido da variável de ambiente INSTANCE_MAX_CONCURRENT_REQUESTS.
  - O bloco __main__ agora instancia ForkingHTTPServer em vez de ThreadingHTTPServer.

Por quê:
  - O loop de trabalho de CPU (math.sqrt/math.sin) não libera o GIL, então com
    ThreadingHTTPServer múltiplas requisições concorrentes eram, na prática,
    processadas em série por um único núcleo, não importa quantas threads/CPUs
    existissem. Isso criava um teto de capacidade "acidental" (~17 RPS observado),
    que dependia do hardware/interpretador, não de uma escolha de projeto.
  - Com ForkingMixIn, cada requisição roda em um processo do SO separado,
    permitindo paralelismo real entre núcleos, com um teto explícito e
    reprodutível (max_children).

--------------------------------------------------------------------------------
3) docker_manager.py
--------------------------------------------------------------------------------
Alterado:
  - start_instance(): a chamada client.containers.run(...) agora passa
    environment={"INSTANCE_MAX_CONCURRENT_REQUESTS": str(config.INSTANCE_MAX_CONCURRENT_REQUESTS)}

Por quê:
  - Antes nenhuma variável de ambiente era passada para os containers (estava
    comentado no código). Sem isso, o simple_server.py não teria como saber
    qual o valor de max_children configurado em config.py.

--------------------------------------------------------------------------------
4) traffic_injectorV0.py — envio open-loop
--------------------------------------------------------------------------------
Alterado:
  - Import adicional: "from concurrent.futures import ThreadPoolExecutor".
  - Novo pool global _send_pool = ThreadPoolExecutor(max_workers=config.HTTP_ATTACK_MAX_CONCURRENT_SENDS)
  - Nova função _send_one(session, target_url, counters, counters_lock): executa de fato
    a chamada bloqueante session.get(...), mede o RTT e atualiza os contadores
    ok/err. Roda dentro de uma thread do _send_pool.
  - http_request_worker(): antes fazia o request de forma bloqueante dentro do
    próprio laço de ritmo (closed-loop): enviava, esperava a resposta, só então
    decidia dormir ou não. Agora o laço só decide QUANDO agendar o próximo envio
    (via _send_pool.submit(...)) e nunca espera a resposta antes de continuar
    (open-loop). O tempo de resposta do alvo não afeta mais o ritmo de disparo.

Por quê:
  - Antes, cada worker só tinha 1 requisição "em voo" por vez. Se o alvo demorasse
    mais que o intervalo alvo (1/RPS) para responder, o worker automaticamente
    reduzia sua taxa real de envio — o RPS "planejado" (planned_total_rps) e o
    "real" (real_rps) do CSV divergiam sem que isso representasse perda de pacotes
    (nenhum erro/timeout era registrado), apenas o injetor se autolimitando.
  - Isso tornava impossível calibrar com precisão "quanto tráfego preciso mandar
    para estourar o limiar de CPU", porque o tráfego realmente entregue dependia
    de fatores incidentais (contenção, nº de instâncias), não só da configuração.
  - Com o envio desacoplado da espera pela resposta, o RPS configurado passa a
    refletir a demanda real ofertada ao alvo, tornando os experimentos de
    calibração de limiar (CPU_THRESHOLD_SCALE_UP/DOWN) reprodutíveis.

--------------------------------------------------------------------------------
5) traffic_injectorV0.py — correção de subcontagem no fim do pulso
--------------------------------------------------------------------------------
Problema encontrado:
  Ao parar o ataque, o laço de ritmo saía e lia os contadores (ok/err)
  imediatamente, mas requisições que ainda estavam em voo no _send_pool
  (enviadas mas sem resposta ainda) só terminariam DEPOIS dessa leitura,
  e nunca eram contabilizadas no log de resumo. Ou seja, total_requests/errors
  no attack_summary_log.csv podiam ficar levemente subestimados no fim de cada pulso.

Correção:
  - http_request_worker() agora guarda as Futures de cada envio numa lista
    (pending_futures), descartando periodicamente as que já terminaram (para não
    acumular memória em ataques longos).
  - Ao sair do laço (attack_active == False), o worker espera (com timeout de
    segurança) todas as Futures pendentes terminarem antes de ler os contadores
    finais e chamar attack_summary_logger.log_worker_stop(...).

Por quê:
  - Garante que total_requests/errors no CSV reflitam TODAS as requisições que
    o worker efetivamente enviou durante o pulso, sem subcontagem na borda do
    fim do ataque — importante porque esses números alimentam o dataset usado
    para treinar o modelo de ML.

--------------------------------------------------------------------------------
6) app/simple_server.py — remoção do fork-per-request
--------------------------------------------------------------------------------
Problema encontrado (via teste manual):
  Testando tráfego normal (benigno) com NORMAL_WORK_UNITS=10 (praticamente
  gratuito) a 100 RPS x 4 clientes, o CPU médio da instância ficou em ~200%
  (2 núcleos cheios) SEM nenhum ataque. Com RPS=10 x 4 clientes, ~30%.
  Isso não podia vir do trabalho em si (quase zero); veio do ForkingHTTPServer
  (mudança A3 anterior), que cria um PROCESSO NOVO do zero para cada requisição
  HTTP recebida. Criar processo tem custo real de CPU (kernel, tabelas de
  página, agendamento, waitpid) mesmo que o trabalho dentro dele seja irrisório.
  Em alta taxa de requisições (400/s), esse custo por requisição, multiplicado
  centenas de vezes por segundo, virou o principal consumidor de CPU — fazendo
  com que o CPU% passasse a refletir a TAXA de requisições, não o WORK_UNITS
  configurado. Isso comprometeria a separação entre tráfego benigno e ataque
  no dataset (tráfego benigno "pesado em volume" passaria a parecer ataque).

Correção:
  - app/simple_server.py voltou a usar ThreadingHTTPServer (sem fork por
    requisição) para aceitar/parsear conexões HTTP (barato).
  - O laço de CPU (math.sqrt/math.sin) foi movido para uma função de nível de
    módulo, _burn_cpu(work_units), executada dentro de um
    concurrent.futures.ProcessPoolExecutor PERSISTENTE (_pool), criado uma
    única vez no startup do servidor (bloco __main__), com
    max_workers=INSTANCE_MAX_CONCURRENT_REQUESTS.
  - do_GET agora faz _pool.submit(_burn_cpu, work_units).result() em vez do
    laço direto ou de depender do ForkingHTTPServer.
  - config.py: comentário de INSTANCE_MAX_CONCURRENT_REQUESTS atualizado para
    refletir que agora é o tamanho do pool persistente, não mais "max_children"
    de um servidor forking.

Por quê:
  - O pool de processos é criado UMA VEZ; os processos são reaproveitados para
    todas as requisições subsequentes, então o custo de criar processos deixa
    de ser pago a cada requisição.
  - Cada processo do pool ainda roda em paralelo real (seu próprio GIL), então
    o objetivo original da mudança A3 (paralelismo real entre núcleos) é
    preservado — só o mecanismo de obter esse paralelismo mudou.
  - Efeito colateral esperado: os números de capacidade medidos anteriormente
    (~17 RPS de teto, tempos de 1x/2x/4x) ficam desatualizados, pois incluíam
    o custo de fork-per-request. Precisarão ser remedidos antes da Fase 1 de
    calibração.

--------------------------------------------------------------------------------
7) stats_collector.py — average_cpu_percent passa a ser uma média real da janela
--------------------------------------------------------------------------------
Problema encontrado (via análise de simulation_metrics.csv de um teste real):
  Quatro pulsos de ataque IDÊNTICOS (confirmado por attack_summary_log.csv:
  mesmo real_rps, mesmo total_requests, mesma duração) produziam leituras de
  CPU bem diferentes entre si (28/36/30/41%). Causa: get_averages() apenas
  retornava a última amostra em cache (_cache[cid]), sobrescrita a cada
  ~1s pela thread de polling — não uma média sobre a janela de 5s do
  MONITOR_INTERVAL_SECONDS. Qual amostra de ~1s calhava de estar em cache no
  momento exato da leitura dependia de jitter de agendamento entre duas
  threads independentes, não da intensidade real do ataque.

Correção:
  - Novo _history: Dict[str, deque] guarda (timestamp, cpu_percent,
    mem_app_mb, name) por container, podado para as últimas
    history_seconds (padrão 30s).
  - _run() agora grava cada amostra também no histórico, além do cache.
  - get_averages(window_seconds=None): sem argumento, mantém o
    comportamento antigo (última amostra, útil p/ inspeção pontual/self-test).
    Com window_seconds, calcula a média de TODAS as amostras dos últimos
    window_seconds segundos, com fallback para o cache mais recente se não
    houver amostras na janela (ex.: container recém-adicionado).
  - update_containers() agora também poda o histórico de containers que saíram.

Por quê:
  - Faz average_cpu_percent refletir genuinamente a janela de monitoramento
    inteira (múltiplas amostras), não uma leitura pontual sujeita a sorte de
    timing — elimina a variação espúria entre pulsos idênticos.
  - Verificado com teste unitário isolado (histórico sintético) confirmando
    que a média da janela bate com o cálculo manual, e com uma execução real
    da simulação (ver item 8).

--------------------------------------------------------------------------------
8) main_orchestrator.py — label do CSV de métricas alinhado com average_cpu_percent
--------------------------------------------------------------------------------
Problema encontrado (mesma análise do item 7):
  A cada pulso, a linha com label='attack' aparecia com CPU baixo (pré-ataque),
  e o pico de CPU real aparecia na linha SEGUINTE, já rotulada 'normal'.
  Causa: label só virava 'attack' dentro do bloco que DECIDE iniciar o
  injetor nesta iteração — decisão tomada DEPOIS da leitura de avg_cpu, que
  por sua vez refletia o estado ANTES dessa decisão ter efeito.

Correção:
  - stats_collector.get_averages() agora é chamado com
    window_seconds=config.MONITOR_INTERVAL_SECONDS (ver item 7).
  - label passa a ser calculado logo após a leitura de métricas, ANTES da
    decisão de start/stop desta iteração: label = 'attack' if
    attack_has_started else 'normal' — ou seja, reflete o estado herdado da
    decisão da iteração ANTERIOR, que é o que avg_cpu acabou de medir.
  - Removidas as duas atribuições antigas de label ('normal' redundante logo
    após o bloco de ataque; 'attack' dentro do bloco de start/restart do
    injetor), que sobrescreviam o valor correto calculado acima.
  - sniffer.set_label(...) (usado por tcpdump_sniffer.py para rotular
    traffic_capture.csv em tempo real) NÃO foi alterado — já estava correto
    (confirmado inspecionando o CSV de captura: rótulo "attack" aparece
    exatamente na janela real do pulso, não deslocado).

Por quê:
  - label e average_cpu_percent agora sempre descrevem a MESMA janela de
    tempo, em vez de janelas deslocadas por um tick.
  - Verificado com uma execução real de 50s (2 pulsos): CPU de pico (49.61%
    e 37.4%) apareceu corretamente nas linhas rotuladas 'attack' (t=25 e
    t=45), não mais nas linhas seguintes rotuladas 'normal'.

--------------------------------------------------------------------------------
9) Problemas encontrados — status atualizado
--------------------------------------------------------------------------------
  - [CORRIGIDO no item 18] normal_traffic.py: save_rtt_log() usava o nome de
    arquivo "rtt_log.csv" fixo no código, ignorando qualquer configuração —
    inconsistente com os outros logs (que usam config.METRICS_LOG_FILE etc).
  - [AINDA NÃO CORRIGIDO] O problema de permissão em si (arquivos ficarem
    donos de root após uma execução com sudo, bloqueando execuções futuras
    sem sudo) continua existindo para simulation_metrics.csv,
    traffic_capture.csv, attack_summary_log.csv e agora também rtt_log.csv
    (mesmo lendo o nome do config, o arquivo em si ainda fica root-owned se
    criado via sudo). Não é um bug de código — é uma questão de como as
    execuções são feitas (sudo vs. não-sudo). Mencionado aqui para referência
    futura, caso vire um problema recorrente.

--------------------------------------------------------------------------------
18) normal_traffic.py — save_rtt_log() lê o nome do arquivo de config.py
--------------------------------------------------------------------------------
Alterado:
  - config.py: novo RTT_LOG_FILE = "rtt_log.csv".
  - normal_traffic.py: save_rtt_log(filename=None) agora usa
    config.RTT_LOG_FILE como padrão quando nenhum argumento é passado, em vez
    de ter "rtt_log.csv" fixo na assinatura da função — mesmo padrão já usado
    por METRICS_LOG_FILE, TCPDUMP_OUTPUT_CSV, ATTACK_SUMMARY_LOG_FILE.

Por quê:
  - Consistência com o resto do projeto (todo log configurável via
    config.py) e para permitir redirecionar o caminho de saída em testes
    (como os testes de verificação já feitos nesta sessão) sem precisar
    tocar no arquivo real do repositório.

Verificado:
  - config.RTT_LOG_FILE sobrescrito para um caminho em /tmp antes de chamar
    save_rtt_log() sem argumentos: arquivo escrito corretamente no caminho
    configurado, com o conteúdo esperado.

--------------------------------------------------------------------------------
10) main_orchestrator.py — novos overrides de CLI para varreduras automatizadas
--------------------------------------------------------------------------------
Adicionado:
  --work-units (config.ATTACK_WORK_UNITS)
  --attack-duration (config.ATTACK_DURATION_SECONDS)
  --normal-rps (config.HTTP_NORMAL_RPS_PER_CLIENT)
  --normal-work-units (config.NORMAL_WORK_UNITS)

Por quê:
  - Antes, apenas --rps/--attackers/--duration eram configuráveis via CLI;
    WORK_UNITS e os parâmetros de tráfego normal só podiam ser alterados
    editando config.py manualmente entre execuções do run_experiments.py.
  - Permite automatizar varreduras de 3+ variáveis (RPS x ATTACKERS x
    WORK_UNITS) e rodar cenários apenas com tráfego normal
    (--attack-duration 0) sem editar config.py, viabilizando os scripts de
    varredura descritos a seguir (itens 11 e 12).

--------------------------------------------------------------------------------
11) run_experiments.py — varredura automatizada também em WORK_UNITS
--------------------------------------------------------------------------------
Alterado:
  - Adicionado WORK_UNITS_VALUES = [5000, 20000, 50000, 100000].
  - O loop agora é itertools.product(RPS_VALUES, ATTACKERS_VALUES,
    WORK_UNITS_VALUES) (antes: só RPS_VALUES x ATTACKERS_VALUES).
  - Cada execução passa --work-units <valor> para main_orchestrator.py.
  - Removida a leitura estática "work_units = config.ATTACK_WORK_UNITS" (e o
    "import config", agora sem uso) — work_units passa a ser a variável do
    loop, com o mesmo valor usado tanto no comando quanto no nome dos
    arquivos de saída.

Por quê:
  - Antes, WORK_UNITS só mudava entre lotes de execução se alguém editasse
    config.py manualmente entre chamadas do script — não fazia parte da
    varredura automatizada. Isso impedia encontrar o ponto de cruzamento do
    limiar de CPU sem intervenção manual repetida.
  - Valores escolhidos com base em dados já coletados nesta sessão: WU=5000
    a RPS=40 total (10 rps x 4 atacantes) chegou a ~35-49% de CPU, abaixo do
    CPU_THRESHOLD_SCALE_UP (60%) — a varredura estende para valores maiores
    (20000/50000/100000) para encontrar o ponto onde o scale-up realmente
    dispara.

--------------------------------------------------------------------------------
12) run_experiments_normal.py (novo) — varredura de baseline, só tráfego normal
--------------------------------------------------------------------------------
Adicionado:
  - Novo script, irmão de run_experiments.py, varrendo
    itertools.product(NORMAL_RPS_VALUES=[10,25,50,100],
    NORMAL_WORK_UNITS_VALUES=[10,100,1000,5000]) — 16 combinações.
  - Cada execução chama main_orchestrator.py com --normal-rps,
    --normal-work-units e --attack-duration 0 (força o modo sem ataque já
    existente no orquestrador), --duration 180.
  - Salva em experiment_results/metrics_normal_rps{X}_WU{Y}.csv.
  - attack_summary_log.csv (sempre criado, mesmo vazio, por
    attack_summary_logger.init_attack_summary_log()) é descartado a cada
    execução em vez de salvo, já que não há dados de ataque neste modo.

Por quê:
  - Fornece o baseline de comparação para a varredura de ataque (item 11):
    caracteriza CPU/RTT sob tráfego normal na MESMA faixa de intensidade,
    para permitir avaliar depois se uma configuração de ataque que cruza o
    limiar de scale-up é estatisticamente distinguível do que o tráfego
    normal produziria no mesmo ponto (questão de "stealth").
  - Testado com uma execução curta (20s, --normal-rps 25 --normal-work-units
    100 --attack-duration 0): confirmado que os argumentos de CLI chegam
    corretamente ao config, o modo sem ataque é ativado, e o CPU responde de
    forma sensata (~40-44%) à intensidade configurada.

--------------------------------------------------------------------------------
13) Fase 1 do roadmap W-EDoS: aposentar a lógica de pulso repetido (Yo-Yo)
--------------------------------------------------------------------------------
Contexto:
  Decisão do usuário de seguir apenas a estrutura workload-based (W-EDoS) do
  artigo de Sotelo Monge et al., abandonando a síntese com o ataque Yo-Yo
  (Bremler-Barr et al.) discutida anteriormente. O ataque deixa de pulsar
  repetidamente e passa a ser uma janela ÚNICA: a simulação começa só com
  tráfego normal, o ataque liga uma vez, roda por uma duração configurável,
  desliga, e o tráfego normal continua sozinho até o fim.

Alterado:
  - config.py:
      * PULSE_DURATION agora representa a duração TOTAL e única do ataque
        (não mais a duração de um pulso individual dentro de um trem de
        pulsos repetidos).
      * ATTACK_DURATION_SECONDS mantém seu papel de liga/desliga (0 = sem
        ataque agendado, usado por --attack-duration 0), mas seu valor
        numérico deixou de ter qualquer efeito sobre a duração do ataque —
        isso agora é controlado inteiramente por PULSE_DURATION. Comentários
        atualizados para deixar isso explícito.
      * Removido o bloco inteiro de configurações EDOS_PULSE_*/EDOS_IDLE_*/
        EDOS_SATURATION_PHASE_DURATION_SECONDS — nunca chegaram a ser
        conectadas à lógica do orquestrador e eram específicas do desenho
        Yo-Yo agora abandonado.
  - main_orchestrator.py:
      * Removidas as variáveis mortas tracked_attack_state e
        last_pulse_end_time (declaradas, nunca usadas — resquício do desenho
        Yo-Yo).
      * Removido o clamp que limitava PULSE_DURATION a no máximo
        SCALE_COOLDOWN_SECONDS antes do loop principal — não faz mais
        sentido, já que PULSE_DURATION agora pode ter qualquer duração (é a
        duração inteira do ataque, não mais um pulso que precisa caber dentro
        de um ciclo de cooldown).
      * attack_start_time/attack_end agora são calculados UMA VEZ antes do
        loop e nunca recalculados — removido o bloco que reagendava o
        próximo pulso (attack_start_time += SCALE_COOLDOWN_SECONDS) a cada
        vez que a janela terminava.
      * Removida a checagem de "limite externo" baseada na magnitude de
        ATTACK_DURATION_SECONDS (if config.ATTACK_DURATION_SECONDS <
        elapsed_time_seconds), redundante agora que a janela única já define
        completamente quando o ataque está ativo.
      * Removida a lógica de is_max_instance que empurrava a janela de
        ataque para frente quando o cluster já estava no tamanho máximo
        (mecanismo específico para evitar pulsar inutilmente durante
        oscilação Yo-Yo). As duas ramificações (com/sem is_max_instance)
        foram unificadas em uma única checagem de start/restart do injetor.

Por quê:
  - Implementa exatamente o modelo W-EDoS sustentado (não pulsado) do artigo
    de referência: uma única janela de ataque, sem ciclos repetidos.
  - Simplifica o código removendo mecanismos (reagendamento, clamp de
    cooldown, desvio por instância máxima) que só faziam sentido para pulsos
    repetidos.

Verificação:
  - Execução real de 60s (--duration 60, config padrão: ATTACK_START_TIME_
    SECONDS=20, PULSE_DURATION=5): confirmado que a janela [20s, 25s] nunca
    muda ao longo de toda a execução, o ataque liga e desliga exatamente uma
    vez (CPU sobe para 40.55% na linha rotulada 'attack' em t=25, alinhado
    corretamente com o fix de label do item 8), e "Should attack be active
    now?" permanece False do t=30 até o t=55 — sem o segundo pulso que a
    lógica antiga teria disparado por volta do t=40 (20+SCALE_COOLDOWN_
    SECONDS=20). Tráfego normal (~11-17% CPU) roda sem interrupção durante
    toda a simulação, incluindo antes, durante e depois da janela de ataque.

--------------------------------------------------------------------------------
14) main_orchestrator.py — CLI overrides para a janela de ataque
--------------------------------------------------------------------------------
Adicionado:
  --attack-start (config.ATTACK_START_TIME_SECONDS)
  --pulse-duration (config.PULSE_DURATION)

Por quê:
  - Permite que o sweep runner combinado (Fase 2, cenários estilo Tabela 5)
    controle a janela de ataque por execução via CLI, no mesmo padrão dos
    overrides existentes, em vez de depender de valores fixos em config.py.
  - Atenção ao nome: --attack-duration (já existente) continua controlando
    apenas o liga/desliga (ATTACK_DURATION_SECONDS); --pulse-duration é quem
    de fato controla a duração da janela única de ataque (PULSE_DURATION,
    redefinido na Fase 1). São dois flags parecidos no nome com papéis bem
    diferentes — documentado aqui para não confundir no futuro.

Verificado:
  - Execução real de 40s com --attack-start 10 --pulse-duration 20: janela
    corretamente deslocada para [10s, 30s], rótulos 'attack'/'normal' no CSV
    de métricas alinhados corretamente antes/durante/depois da janela.

--------------------------------------------------------------------------------
15) run_experiments_combined.py (novo) — varredura combinada (Tabela 5)
--------------------------------------------------------------------------------
Adicionado:
  - Novo script, irmão de run_experiments.py/run_experiments_normal.py,
    varrendo NORMAL_SCENARIOS (S1-S4: 50/60/70/80 req/s agregado, igual à
    Tabela 4) x ATTACK_INTENSITIES_PCT ([1, 5, 10]) — 12 combinações.
  - Para cada combinação, calcula o RPS agregado de ataque como
    normal_aggregate_rps * pct / 100 (a duração se cancela na conversão de
    "% do total de requisições normais" para RPS), distribui entre
    NORMAL_NUM_CLIENTS=4 / ATTACK_NUM_ATTACKERS=4, e chama
    main_orchestrator.py via --normal-rps/--rps/--attackers/--work-units/
    --normal-work-units/--duration.
  - ATTACK_WORK_UNITS fixado em 100000 (maior valor já calibrado nesta sessão
    que cruza o limiar de scale-up em RPS moderado — ver item 11).
  - Deliberadamente NÃO sobrescreve --attack-start/--pulse-duration: herda
    os valores atuais de config.py (janela [20s, 160s] dentro de uma
    simulação de 180s), mantendo o formato de warm-up/cool-down com tráfego
    só-normal já estabelecido na Fase 1, em vez do desenho original da
    Tabela 5 (ataque cobrindo a janela inteira).
  - Salva em experiment_results/metrics_{cenário}_atk{pct}pct.csv e
    attack_summary_log_{cenário}_atk{pct}pct.csv.

Por quê:
  - Reproduz a Tabela 5 de Sotelo Monge et al.: para cada cenário de tráfego
    normal, sobrepõe ataque como uma fração do volume total de requisições
    normais — o experimento central da estrutura W-EDoS escolhida.

Verificado:
  - Execução real de um cenário (S1 @ 5%, duração reduzida para 40s para
    teste): tráfego normal roda desde t=0, sobreposição de ataque ativa
    corretamente a partir da janela configurada, CPU sobe de ~15% (baseline)
    para ~21-30% e RTT sobe de ~6-8ms para ~42-51ms durante o ataque — sinal
    claramente detectável mesmo numa intensidade baixa (5%) que não chega a
    disparar SCALE_UP, o que é o caso interessante para a análise estatística
    da Fase 3.5.

--------------------------------------------------------------------------------
16) Chegadas de Poisson para tráfego normal e de ataque
--------------------------------------------------------------------------------
Contexto:
  Sotelo Monge et al. geram tráfego normal seguindo um processo de Poisson
  (intervalos entre requisições exponencialmente distribuídos, taxa λ) em vez
  de intervalos fixos — modela clientes independentes de forma mais realista
  e dá à análise de entropia (Fase 3.5) uma variabilidade genuína para
  caracterizar, em vez de principalmente ruído de fila/rede.

Alterado:
  - config.py: novo HTTP_NORMAL_MAX_CONCURRENT_SENDS = 64 (mesmo padrão de
    HTTP_ATTACK_MAX_CONCURRENT_SENDS).
  - traffic_injectorV0.py (http_request_worker): o cálculo de sleep_duration
    deixou de ser "intervalo fixo menos tempo de despacho" e passou a ser um
    sorteio independente de random.expovariate(rps_per_worker) a cada
    iteração — processo de Poisson de taxa rps_per_worker.
  - normal_traffic.py: normal_http_request_worker foi reestruturado para
    seguir exatamente o mesmo padrão open-loop de traffic_injectorV0.py (B2):
    novo _send_pool (ThreadPoolExecutor) e _send_one_normal() executam a
    chamada bloqueante de fato, desacoplados do laço de ritmo; o laço agora
    só decide QUANDO agendar o próximo envio, com o intervalo sorteado de
    random.expovariate(rps_per_worker); ao parar, drena as requisições em
    voo (pending_futures) antes de logar o resumo final, evitando
    subcontagem (mesmo fix do item 4).

Por quê:
  - Chegadas de Poisson exigem que os disparos sejam independentes uns dos
    outros — um laço fechado (que espera a resposta antes de decidir o
    próximo intervalo) distorceria os sorteios sob carga, o mesmo problema
    de auto-limitação já corrigido no injetor de ataque (item 2). Por isso
    normal_traffic.py precisou do mesmo tratamento open-loop, não só da
    troca da fórmula de intervalo.
  - Mantém tráfego normal e de ataque estruturalmente simétricos (mesmo
    padrão open-loop + Poisson dos dois lados), evitando que uma
    regularidade "robótica" de um dos dois vire um sinal de detecção
    artificial não relacionado ao consumo de CPU real.

Verificado:
  - Teste real contra um container de teste (10 rps, 1 worker, 8s cada):
    coeficiente de variação dos intervalos entre chegadas (std/média, que
    deve ser ~1.0 para um processo de Poisson e ~0 para intervalo fixo).
    normal_traffic.py: CV=0.944 (n=76). traffic_injectorV0.py: CV=1.101
    (n=88). Ambos consistentes com chegadas de Poisson.

--------------------------------------------------------------------------------
17) Bug real encontrado em execução do usuário: RuntimeError no shutdown
--------------------------------------------------------------------------------
Problema encontrado (via execução real do usuário com configuração padrão):
  Ao final de uma simulação bem-sucedida, threads de tráfego normal
  (InjectorWorker-N, normal_traffic.py) lançavam
  "RuntimeError: cannot schedule new futures after shutdown" ao tentar
  _send_pool.submit(...). Causa raiz: normal_traffic.stop_http_traffic()
  nunca era chamado no caminho de encerramento bem-sucedido de main() em
  main_orchestrator.py — só existia nos handlers de KeyboardInterrupt/erro.
  Como o tráfego normal roda a simulação inteira por design (nunca parado
  dentro do loop), os workers (daemon threads) continuavam vivos até o
  interpretador Python encerrar. O hook de shutdown automático do
  concurrent.futures (registrado via atexit na primeira vez que o módulo é
  importado, cobre TODAS as ThreadPoolExecutor do processo) finaliza
  _send_pool antes desses workers notarem — daí o erro ao tentar agendar
  mais uma requisição. Isso só passou a se manifestar depois da mudança de
  open-loop/Poisson (item 16): antes não existia _send_pool para o hook de
  shutdown derrubar.

Correção:
  - main_orchestrator.py: adicionada chamada a normal_traffic.stop_http_traffic()
    no caminho de encerramento bem-sucedido de main() (mesmo padrão já usado
    para traffic_injectorV0.stop_http_flood()).
  - normal_traffic.py e traffic_injectorV0.py: a chamada a _send_pool.submit()
    dentro do laço de cada worker agora está protegida por
    try/except RuntimeError, tratando "pool já finalizada" como sinal para
    encerrar o laço graciosamente (break) em vez de propagar uma exceção não
    tratada — cobre a corrida residual que ainda existiria mesmo com o fix
    acima (o join em stop_http_traffic()/stop_http_flood() é curto e não
    bloqueante, então um worker no meio de um sleep sorteado mais longo pode
    ainda não ter notado o sinal de parada).

Verificado:
  - Reproduzido o erro original com uma execução real de 30s (--duration 30
    via script), confirmando o traceback antes da correção.
  - Após a correção, mesma execução: código de saída 0, "Stopping normal
    traffic..." aparece antes da limpeza dos containers, todos os workers
    reportam suas contagens finais e param graciosamente, zero exceções/
    tracebacks no log completo.

Nota:
  - Durante o diagnóstico, ao tentar reproduzir o erro localmente sem sudo,
    um rtt_log.csv de dono root (da execução real do usuário) bloqueou a
    escrita e foi removido para desbloquear o teste — sem querer, isso
    apagou o resultado real da execução de diagnóstico do usuário (que já
    tinha sido apenas um teste, sem dados necessários preservados,
    confirmado com o usuário). Verificações seguintes passaram a usar
    caminhos de saída em /tmp, como já era o padrão estabelecido antes.

--------------------------------------------------------------------------------
19) app/simple_server.py — keep-alive (HTTP/1.1) para evitar esgotamento de conexões
--------------------------------------------------------------------------------
Problema encontrado (via análise de experiment_results/metrics_normal_*.csv):
  8 de 16 execuções de run_experiments_normal.py mostravam um colapso súbito
  e permanente para avg_rtt_ms=0.0 (zero requisições bem-sucedidas por
  janela) que nunca se recuperava — incluindo rps50 (um valor real da
  Tabela 4 de Sotelo Monge et al., não só valores exploratórios extras).
  Quanto maior o RPS agregado, mais cedo e mais severo o colapso.

  Causa raiz: SimpleAppHandler nunca usava HTTP keep-alive (sem
  protocol_version="HTTP/1.1", cai no padrão HTTP/1.0, que sempre fecha a
  conexão após cada resposta, independente do que o cliente pedir). A ~100
  req/s sem nenhum reaproveitamento de conexão, isso significa ~100 conexões
  TCP novas por segundo, cada uma ficando em TIME_WAIT por 60s
  (tcp_fin_timeout) depois de fechada.

  Reproduzido diretamente contra um container de teste (rps=100, WU=5000,
  30s): TIME_WAIT subiu de 2 para 2694 em 30s, e o CPU do container caiu de
  ~56% para 3.3% no mesmo intervalo de amostragem — reprodução direta do
  colapso visto nos dados reais. nf_conntrack_max (65536) e a faixa de
  portas efêmeras (28232) foram descartados como causa direta (não chegaram
  perto do limite), mas o mecanismo — esgotamento por alto churn de conexões
  — ficou claro e diretamente correlacionado.

Correção:
  - SimpleAppHandler ganhou protocol_version = "HTTP/1.1", habilitando o
    keep-alive nativo do BaseHTTPRequestHandler da stdlib.
  - Necessário também: a resposta passou a incluir o header Content-Length
    (calculado a partir do corpo já montado em bytes, antes de enviar os
    headers). Sem isso, keep-alive quebraria a comunicação — o cliente não
    teria como saber onde uma resposta termina numa conexão que continua
    aberta para a próxima requisição (antes, o fechamento da conexão é que
    sinalizava o fim da resposta; com keep-alive isso deixa de existir).

Verificado:
  - Reproduzida a mesma cena (rps=100, WU=5000, 30s) contra um container
    reconstruído com a correção: TIME_WAIT ficou estável em ~4 durante toda
    a execução (em vez de subir a 2694), conexões estabelecidas
    estabilizaram em ~42 (reaproveitadas, consistente com ~10 conexões por
    sessão x 4 workers), CPU ficou estável entre 15-48% o tempo todo, e os
    4 workers reportaram 0 erros ao final.

Nota:
  - Como cada worker usa uma única requests.Session() compartilhada entre
    todos os envios da _send_pool, o keep-alive também limita implicitamente
    a ~10 conexões concorrentes por worker (pool_maxsize padrão do
    urllib3/requests) — um efeito colateral razoável (limita uso de
    recursos), mas vale ter em mente.
  - As execuções de run_experiments_normal.py já coletadas (que mostraram o
    colapso) precisam ser refeitas com esta correção antes de serem usadas
    como dataset final.

--------------------------------------------------------------------------------
20) experiment_results/ — limpeza de resultados obsoletos
--------------------------------------------------------------------------------
Removido (git rm, 42 arquivos):
  - metrics_rps{1,5,10}_att4_WU{5000,20000,50000}.csv e os
    attack_summary_log_{1,5,10}_4_WU{5000,20000,50000}.csv correspondentes
    (18 arquivos): pontos de uma faixa de calibração de WORK_UNITS já
    abandonada — run_experiments.py hoje varre apenas
    WORK_UNITS_VALUES = [100000, 200000, 500000].
  - metrics_S1..S4_atk{1,5,10}pct.csv e os attack_summary_log_S{1-4}_atk*pct
    correspondentes (24 arquivos): saída de run_experiments_combined.py de
    quando o script ainda usava um único ATTACK_WORK_UNITS=100000 fixo. A
    investigação de WU (ver seção 21) mostrou que esse valor não força
    scale-up e não representa um ataque W-EDoS bem-sucedido, então esses
    resultados foram descartados em vez de mantidos como baseline.

Mantido:
  - metrics_normal_rps*_WU*.csv (16 arquivos, baseline de tráfego normal,
    eixo independente da decisão de WU do ataque).
  - metrics_rps{1,5,10}_att4_WU{100000,200000,500000}.csv e respectivos
    attack_summary_log (18 arquivos, batem com o run_experiments.py atual).

Por quê:
  - O diretório estava confuso, misturando resultados de configurações já
    abandonadas com os atuais, dificultando saber quais dados realmente
    valem para o dataset final da tese.

--------------------------------------------------------------------------------
21) run_experiments.py — salvar rtt_log.csv por execução da varredura
--------------------------------------------------------------------------------
Problema encontrado:
  normal_traffic.save_rtt_log() sobrescreve rtt_log.csv (modo "w") ao final
  de cada execução do main_orchestrator.py, mas run_experiments.py nunca
  movia esse arquivo para experiment_results/ como já fazia com
  simulation_metrics.csv e attack_summary_log.csv. Resultado: só o rtt_log
  da última iteração da varredura sobrevivia, o de todas as anteriores era
  perdido silenciosamente.

Alterado:
  - Após cada iteração (mesmo bloco que já move os outros dois logs),
    rtt_log.csv passou a ser movido para
    experiment_results/rtt_log_rps{rps}_att{attackers}_WU{work_units}.csv,
    seguindo a mesma convenção de nome já usada para o metrics_*.csv
    correspondente.

Por quê:
  - rtt_log.csv contém as amostras de RTT (tráfego normal + ataque) que o
    EntCusumZV3.py usa para detectar bursts — sem preservar um arquivo por
    combinação de RPS/attackers/WORK_UNITS, não dava para rodar a detecção
    de bursts em cada cenário da varredura, só no último.

--------------------------------------------------------------------------------
22) EntCusumZV3.py + run_burst_analysis.py (novo) — detecção de bursts em lote
--------------------------------------------------------------------------------
Alterado (EntCusumZV3.py):
  - analisar_bursts_tunavel() ganhou os parâmetros input_file (antes fixo em
    'rtt_log.csv'), output_prefix e show_plot. Os nomes de saída
    (rtt_bursts_*.xlsx / rtt_todos_bursts_*.png) agora são derivados do nome
    do arquivo de entrada e escritos no mesmo diretório dele.
  - Quando chamado sem argumentos (uso manual, python EntCusumZV3.py), o
    comportamento é idêntico ao de antes: lê rtt_log.csv e grava
    rtt_bursts_tunaveis.xlsx / rtt_todos_bursts.png no diretório atual —
    isso é tratado como caso especial, não só "sem sufixo", para não mudar
    o uso interativo já conhecido.
  - plt.show() agora é condicional a show_plot (True por padrão, False no
    uso em lote) e a figura é fechada com plt.close(fig) depois de salva,
    para não acumular janelas/figuras abertas ao processar vários arquivos
    em sequência.

Criado (run_burst_analysis.py):
  - Script novo que varre experiment_results/rtt_log_*.csv (produzidos pela
    mudança da seção 21) e chama analisar_bursts_tunavel() uma vez para
    cada um, com show_plot=False.

Por quê:
  - O pedido era rodar a detecção de bursts em cada rtt_log.csv salvo por
    execução da varredura, não só no último rtt_log.csv solto na raiz do
    projeto. Parametrizar a função em vez de duplicar a lógica de detecção
    evita ter duas implementações do mesmo cálculo (uma para uso manual,
    outra para lote) que poderiam divergir com o tempo.

Verificado:
  - python3 -m py_compile em run_experiments.py, EntCusumZV3.py e
    run_burst_analysis.py, sem erros de sintaxe. Não executado de ponta a
    ponta ainda (precisa dos containers Docker e de sudo para
    main_orchestrator.py); a varredura real fica para quando o usuário
    rodar run_experiments.py em seguida.

--------------------------------------------------------------------------------
23) run_experiments_combined.py — reenquadrar a varredura em WU=300000/500000
--------------------------------------------------------------------------------
Contexto:
  A varredura de calibração de WORK_UNITS (run_experiments.py, ver seção 20)
  mostrou que só WU=300000 (scale-up limítrofe) e WU=500000 (scale-up claro,
  repetido) de fato forçam SCALE_UP mantendo o RPS baixo/stealth — ou seja,
  são os únicos que constituem um ataque W-EDoS bem-sucedido pela definição
  do artigo (custo forçado sem volume que pareça DDoS). WU=100000, usado
  fixo aqui antes, não força scale-up: é estatisticamente detectável via RTT
  em alguns casos, mas não atinge o objetivo econômico do ataque.

Alterado:
  - ATTACK_WORK_UNITS (valor único, 100000) virou
    ATTACK_WORK_UNITS_LIST = [300000, 500000].
  - Novo laço interno sobre ATTACK_WORK_UNITS_LIST (cenário x intensidade x
    WU), então o total de execuções passa de 12 para 24 (4 cenários x 3
    intensidades x 2 valores de WU), ~72+ min de execução real além do
    overhead do Docker.
  - Nomes de saída passaram a incluir o WU, para não sobrescrever entre os
    dois valores: metrics_{cenário}_atk{pct}pct_wu{work_units}.csv e
    attack_summary_log_{cenário}_atk{pct}pct_wu{work_units}.csv (antes não
    tinham WU no nome, porque só existia um valor fixo).
  - Linha de progresso no console passou a mostrar o WU também.

Por quê:
  - Os resultados anteriores desta varredura (S1-S4 x 1/5/10%, todos com
    WU=100000 fixo) foram removidos na limpeza da seção 20 justamente por
    representarem um ataque que não força scale-up — ou seja, não é o "bom"
    exemplo de W-EDoS bem-sucedido que este experimento deveria caracterizar.
    Esta mudança troca o valor fixo pelos dois valores que de fato produzem
    esse resultado.

Verificado:
  - python3 -m py_compile run_experiments_combined.py, sem erros de sintaxe.
    Não executado de ponta a ponta ainda (precisa de Docker + sudo); fica
    para quando o usuário rodar a varredura de verdade.

--------------------------------------------------------------------------------
24) run_experiments_combined.py — salvar rtt_log.csv por execução também aqui
--------------------------------------------------------------------------------
Alterado:
  - Mesmo tratamento da seção 21 (run_experiments.py), aplicado aqui: após
    cada iteração (cenário x intensidade x WU), rtt_log.csv passa a ser
    movido para
    experiment_results/rtt_log_{cenário}_atk{pct}pct_wu{work_units}.csv,
    seguindo a mesma convenção de nome já usada para o metrics_*.csv
    correspondente.

Por quê:
  - Fechava a lacuna apontada na nota da seção 23: sem isso, run_burst_
    analysis.py só enxergava os rtt_log_*.csv da varredura de calibração
    (run_experiments.py), não os dos cenários combinados S1-S4 x WU=
    300000/500000 — que são justamente os cenários mais próximos do
    experimento final (Tabela 5) a caracterizar estatisticamente.

Verificado:
  - python3 -m py_compile run_experiments_combined.py, sem erros de sintaxe.
    Não executado de ponta a ponta ainda; fica para a regeneração completa
    da varredura que o usuário vai rodar em seguida.

--------------------------------------------------------------------------------
25) experiment_results/ — limpeza completa para regeneração do zero
--------------------------------------------------------------------------------
Removido (sudo rm -rf experiment_results/* pelo usuário, 64 arquivos):
  - Todo o conteúdo restante do diretório, incluindo os conjuntos até então
    mantidos na limpeza da seção 20 (baseline de tráfego normal e a
    varredura de calibração WU=100000/200000/500000).

Por quê:
  - Com o fix de keep-alive (seção 19), o reenquadramento de WU (seção 23)
    e o salvamento de rtt_log.csv por execução (seções 21 e 24) todos já
    aplicados, os resultados antigos ficaram desatualizados em relação ao
    código atual de qualquer forma — o usuário optou por regenerar tudo do
    zero em vez de manter dados parcialmente obsoletos misturados com os
    novos.
  - O diretório estava root:root (de execuções antigas com sudo), o que já
    tinha bloqueado tentativas anteriores de limpeza automática; o comando
    também corrigiu o dono para bibie:bibie, evitando o mesmo bloqueio nas
    próximas varreduras.

Verificado:
  - experiment_results/ confirmado vazio (0 arquivos) e com dono bibie:
    bibie após o comando. As 64 remoções aparecem staged no git (git status
    --short mostra "D" para cada arquivo), recuperáveis via histórico do
    git se necessário.

Próximo passo (ainda não executado): rodar, em ordem, run_experiments_
normal.py, run_experiments.py, run_experiments_combined.py e run_burst_
analysis.py para regenerar experiment_results/ do zero com o código atual.

--------------------------------------------------------------------------------
26) EntCusumZV3.py — piso de burst adaptativo por execução (em vez de fixo)
--------------------------------------------------------------------------------
Problema encontrado:
  min_rtt_burst=50 (piso fixo em ms, igual para todo cenário) fazia o
  detector ignorar ataques de baixa intensidade mas reais: rps1/WU500000
  (0 bursts detectados) força scale-up sustentado e RTT bem acima do seu
  próprio baseline (15ms -> 30-44ms), mas nunca cruza 50ms em termos
  absolutos. O RTT "normal" varia com RPS/WU (15-64ms observado entre
  cenários da varredura de baseline), então nenhum valor fixo separa bem
  ataque de tráfego normal em todos os cenários ao mesmo tempo.

  Tentativa descartada antes de implementar: usar média + k*desvio-padrão
  do baseline (as amostras antes do ataque começar). Testado com dados
  reais: o desvio-padrão por amostra bruta é alto (~19ms) mesmo em
  repouso (jitter de rede/agendamento), e só há ~20s de dado limpo antes
  do ataque (ATTACK_START_TIME_SECONDS=20) — uma única janela, amostra
  pequena demais pra estimar desvio-padrão de janela com confiança.
  média+3*desvio chegou a ~72-76ms, PIOR que o piso fixo de 50ms antigo.

Alterado:
  - Parâmetro min_rtt_burst (fixo, 50) substituído por baseline_ratio
    (tunável, padrão 1.8). O piso de burst passa a ser
    baseline_mean * baseline_ratio, onde baseline_mean é a média do RTT
    de CADA execução, calculada só com as amostras de antes do ataque
    começar (df['tempo_rel'] < config.ATTACK_START_TIME_SECONDS). Usa
    razão sobre a média (não média+desvio) porque só precisa de uma
    média estável, que já é confiável com ~750-900 amostras brutas em
    20s, ao contrário do desvio-padrão de janela.
  - Fallback: se houver menos de 10 amostras antes do ataque (config
    atípica), usa o arquivo inteiro como baseline e imprime aviso.
  - CUSUM (kappa, h, e a referência usada em cada incremento) passou a
    usar essa mesma baseline pré-ataque em vez de global_mean/global_std
    calculados sobre o arquivo INTEIRO. Corrige de quebra uma
    inconsistência já documentada no CLAUDE.md: antes, o período de
    ataque contaminava a própria referência de "normal" que o CUSUM usava
    pra se comparar.
  - Baseline/piso calculados são impressos no início de cada análise
    (média, desvio, nº de amostras, piso resultante), e o piso também
    aparece como linha horizontal tracejada no gráfico e no título.

Por quê:
  - Faz o detector reagir ao que É normal PARA AQUELA execução específica,
    em vez de assumir que RTT "normal" é sempre parecido entre cenários
    com RPS/WU bem diferentes — o que os próprios dados de baseline já
    tinham desmentido.

Verificado:
  - Reexecutado run_burst_analysis.py sobre os 9 rtt_log_*.csv reais da
    varredura de calibração (não só um protótipo isolado). Resultado
    bursts detectados, piso fixo (50ms) -> piso novo (1.8x baseline):
      rps1/WU100000:  0 -> 0   (CPU nunca subiu de verdade — sem falso positivo)
      rps1/WU200000:  0 -> 0   (idem)
      rps1/WU500000:  0 -> 14  (caso "limpo" que antes passava batido)
      rps5/WU100000:  0 -> 0   (idem, sem falso positivo)
      rps5/WU200000:  2 -> 14  (estava sub-detectado)
      rps5/WU500000: 16 -> 16  (já óbvio antes, sem mudança)
      rps10/WU100000: 0 -> 13  (outro caso que passava batido)
      rps10/WU200000:15 -> 15  (já óbvio antes, sem mudança)
      rps10/WU500000: 6 -> 6   (já óbvio antes, sem mudança)
    Os três cenários onde a CPU nunca chegou a subir de verdade continuam
    em 0 bursts com o piso novo — a mudança pega os casos reais que
    faltavam sem introduzir falso positivo nos casos claramente sem
    ataque efetivo, pelo menos nesta bateria de 9 arquivos.
  - python3 -m py_compile EntCusumZV3.py, sem erros.

--------------------------------------------------------------------------------
27) run_experiments_combined.py + EntCusumZV3.py — sweep real e achado de diluição
--------------------------------------------------------------------------------
Contexto:
  Rodado o sweep combinado real (S1-S4 x 1/5/10% x WU=300000/500000, 24
  execuções) e run_burst_analysis.py sobre os 24 rtt_log_* resultantes.
  Tendência geral do sweep ficou como esperado: detecção mais forte conforme
  cenário (S1->S4), intensidade (1%->10%) e WU aumentam. Todas as execuções
  em 1% de intensidade não escalaram e não geraram burst; nenhum erro de
  requisição apareceu em nenhum attack_summary_log deste sweep (volumes de
  ataque bem menores que os do sweep de calibração da seção 20/23).

Achado (não é bug, não requer mudança de código):
  S1_atk5pct_wu500000 é a única exceção real à tendência: 2 SCALE_UP de
  verdade ocorreram (CPU pico 81%, RTT médio de ATAQUE 92-208ms nas linhas
  rotuladas 'attack' do metrics_*.csv), mas run_burst_analysis.py reportou
  0 bursts nesse arquivo, mesmo com Z-score e CUSUM disparando quase toda
  janela ('Sim' na maioria das linhas). Investigado: a média de RTT POR
  JANELA nunca passou de ~34ms (o piso adaptativo da seção 26 é 34.1ms
  aqui) — não por o ataque ser fraco, mas porque rtt_log.csv mistura TODAS
  as requisições completadas (normais + ataque) sem rótulo. Em 5% de
  intensidade sobre S1 (50 req/s normal), o ataque agrega só ~2.5 req/s —
  proporção ~20:1 normal:ataque em cada janela de 20s. Uma janela ~95%
  normal (~18ms) + ~5% ataque (~150ms) calcula pra
  0.95*18 + 0.05*150 ≈ 24.6ms, perto do que foi observado — a média da
  janela dilui o sinal do ataque para dentro do ruído da maioria normal,
  independente de onde o piso (RTT ou desvio) esteja ajustado.

Por quê isso importa (sem ser um problema a corrigir):
  - É exatamente o comportamento que a definição de W-EDoS "furtivo"
    (Sotelo Monge et al.) prevê: um ataque que mantém baixo volume
    relativo ao tráfego normal é, por construção, difícil de enxergar com
    estatística agregada simples — não é uma falha do detector, é a
    própria dificuldade que motiva usar ML (o objetivo final do projeto,
    ver project_edos_purpose) em vez de uma regra de limiar fixo. Vale
    citar como limitação da linha de base estatística simples (entropia/
    Z/CUSUM sobre média de RTT) na discussão/limitações da monografia,
    não como algo a "consertar" — não dá pra separar ataque de normal
    dentro de rtt_log.csv sem usar informação que um detector real
    também não teria (rótulo por requisição).

Verificado:
  - Conferido manualmente: reexecutado analisar_bursts_tunavel() sobre
    experiment_results/rtt_log_S1_atk5pct_wu500000.csv isoladamente,
    tabela janela-a-janela confirma média de RTT sempre <= 34.0ms mesmo
    com Z e CUSUM alarmando na maioria das janelas.
  - Comparado numericamente com metrics_S1_atk5pct_wu500000.csv (RTT só
    de ataque, calculado pelo orquestrador): 92-208ms durante o ataque,
    bem acima da média diluída vista em rtt_log.csv — confirma que o
    ataque em si não é fraco, é a mistura normal+ataque no arquivo que
    mascara o sinal.

--------------------------------------------------------------------------------
28) experiment_results/ — organizado em subpastas por varredura
--------------------------------------------------------------------------------
Contexto:
  experiment_results/ tinha os arquivos das três varreduras (baseline
  normal, calibração de WU, sweep combinado S1-S4) todos soltos juntos no
  mesmo diretório, misturados — difícil de navegar e de separar o que é
  relevante pra apresentar.

Alterado:
  - Criadas três subpastas e movidos os arquivos existentes pra elas:
      experiment_results/normal_baseline/  (16 arquivos, de run_experiments_normal.py)
      experiment_results/wu_calibration/   (45 arquivos, de run_experiments.py + run_burst_analysis.py)
      experiment_results/combined_sweep/   (120 arquivos, de run_experiments_combined.py + run_burst_analysis.py)
  - run_experiments_normal.py, run_experiments.py, run_experiments_
    combined.py: RESULTS_DIR de cada um agora aponta direto pra sua
    subpasta correspondente, então as próximas execuções já salvam
    organizado, sem precisar mover nada manualmente depois.
  - run_burst_analysis.py: glob de rtt_log_*.csv passou a ser recursivo
    (glob.glob(..., "**", "rtt_log_*.csv"), recursive=True) pra continuar
    encontrando os arquivos dentro das subpastas. Saída (.xlsx/.png) de
    cada análise continua indo pro mesmo diretório do rtt_log_*.csv de
    entrada (lógica já existente, não mudou) — como as subpastas agora
    separam por varredura, isso já mantém cada subpasta autocontida
    (dado bruto + análise de burst juntos).

Por quê:
  - Pedido do usuário: organizar os dados coletados por varredura antes de
    selecionar o que é relevante pra uma apresentação ao orientador.

Verificado:
  - Nenhum arquivo perdido: contagem em cada subpasta bate com o total
    esperado (16 / 45 / 120), e "find experiment_results -maxdepth 1
    -type f" não retornou nada solto na raiz.
  - python3 -m py_compile nos quatro scripts alterados, sem erros.
  - Reexecutado run_burst_analysis.py de ponta a ponta após a
    reorganização: encontrou os 33 rtt_log_*.csv dentro das subpastas via
    glob recursivo, salvou as saídas no lugar certo, e os 33 números de
    bursts detectados bateram exatamente com os valores de antes da
    reorganização (nenhuma mudança de comportamento, só de local).

--------------------------------------------------------------------------------
29) generate_presentation_graphs.py (novo) — gráficos para a apresentação
--------------------------------------------------------------------------------
Adicionado:
  Script novo que lê os dados já salvos em experiment_results/combined_sweep/
  (varredura combinada) e nas saídas já calculadas por EntCusumZV3.py, e
  gera 3 PNGs em graficos_apresentacao/:
    01_tabela5_heatmap.png — heatmap 4 (cenário) x 3 (intensidade), um
      painel por custo de ataque (WU 300000/500000), célula = nº de
      janelas classificadas como burst.
    02_deteccao_S2_atk10pct_wu500000.png — painel com 6 gráficos
      empilhados (mesmo eixo do tempo) para uma execução real: RTT médio
      por janela, CPU média, instâncias ativas, entropia de Shannon,
      Z-score e CUSUM, com a janela de ataque sombreada e as janelas
      sinalizadas como burst marcadas em vermelho nos 4 gráficos
      estatísticos.
    03_resumo_bursts_por_cenario.png — gráfico de barras resumindo bursts
      detectados por cenário/intensidade/WU.
  Não roda nenhuma simulação nem reimplementa a lógica de detecção — só
  lê os CSVs/XLSX já gerados.

Por quê:
  - Pedido do usuário: gerar gráficos (equivalente à Tabela 5, entropia de
    Shannon, Z-score, CUSUM) para apresentar ao orientador que o
    detector está funcionando.
  - CPU (%) e nº de instâncias ativas foram deixados em dois gráficos
    separados (não sobrepostos com dois eixos Y) porque são grandezas de
    escalas diferentes — um gráfico com dois eixos Y é enganoso (não dá
    pra comparar as curvas de verdade) e foi evitado de propósito.

Verificado:
  - python3 -m py_compile generate_presentation_graphs.py, sem erros.
  - Rodado de ponta a ponta (python3 generate_presentation_graphs.py),
    gerou os 3 PNGs sem erro.
  - Inspecionados visualmente os 3 PNGs gerados: heatmap com números e
    escala de cor legíveis, sem sobreposição de texto/título; gráfico de
    barras sem sobreposição; painel de 6 gráficos com a janela de ataque
    (20-160s) e as janelas de burst corretamente alinhadas visualmente
    entre RTT/CPU/instâncias/entropia/Z-score/CUSUM.

--------------------------------------------------------------------------------
30) run_experiments_combined.py — recalibrar NORMAL_SCENARIOS (S1-S4) para este simulador
--------------------------------------------------------------------------------
Contexto:
  NORMAL_SCENARIOS usava os valores de RPS agregado da Tabela 4 de Sotelo
  Monge et al. (50/60/70/80) direto, sem ajuste. O testbed do artigo é
  maior/mais complexo que este simulador, então esses números não
  representam a mesma coisa aqui — o que importa da Tabela 4 é a ideia de
  uma progressão de carga (leve até perto do limiar de scale-up), não os
  valores absolutos em si. Conferindo contra o baseline já medido deste
  simulador (run_experiments_normal.py, WU=10), os quatro valores antigos
  (50-80) caem todos dentro da MESMA faixa "bem leve" (entre os pontos já
  medidos de 40 e 100 agregado) — não representam uma progressão real de
  proximidade ao limiar neste sistema.

Medição nova:
  Rodado main_orchestrator.py --normal-rps 75 --normal-work-units 10
  --attack-duration 0 --duration 180 (equivalente a um RPS agregado de 300,
  já que --normal-rps é por cliente e há 4 clientes normais fixos) — um
  ponto entre os 200 (estável) e 400 (instável) agregado já medidos
  anteriormente, faltando no baseline original.
  Resultado: pico de CPU 53.6%, média 41.1%, RTT médio 38.6ms, 36/36
  janelas NO_ACTION, nunca saiu de 1 instância — ainda estável, mas bem
  mais perto do limiar de 60% que os pontos anteriores. Salvo em
  experiment_results/normal_baseline/metrics_normal_rps75_WU10.csv.
  Isso restringe a fronteira de instabilidade (neste simulador, WU=10) para
  entre 300 e 400 agregado, mais apertada do que a estimativa anterior
  (entre 200 e 400).

Alterado:
  - NORMAL_SCENARIOS: {"S1":50,"S2":60,"S3":70,"S4":80} ->
    {"S1":40,"S2":100,"S3":200,"S4":300} — os quatro pontos agora vêm de
    medições reais deste simulador (40/100/200 já estavam no baseline
    original, 300 é a medição nova acima), cobrindo uma progressão real de
    ~8% a ~53,6% de CPU, todos confirmadamente sem escalar sozinhos.
  - Comentário do arquivo reescrito pra explicar a recalibração (por que os
    valores da Tabela 4 foram trocados, e com base em que dado).

Por quê:
  - Preserva o ESPÍRITO da Tabela 4 (cenários de carga leve até perto do
    limiar, nenhum escalando sozinho) usando pontos calibrados para a
    curva de capacidade real deste simulador, em vez de números emprestados
    de um testbed diferente que aqui caem todos no mesmo patamar "sem
    efeito".

Nota:
  - Os 24 resultados já coletados em experiment_results/combined_sweep/
    (seções 23/24/27) foram gerados com os valores ANTIGOS de
    NORMAL_SCENARIOS (50-80) e ficam desatualizados a partir desta mudança
    — precisam ser regerados (rodar run_experiments_combined.py de novo)
    antes de serem usados como resultado final. Os gráficos de apresentação
    (seção 29) também precisarão ser regenerados depois disso.

Verificado:
  - python3 -m py_compile run_experiments_combined.py, sem erros.
  - Não executado de ponta a ponta ainda (precisa de Docker + sudo); fica
    para quando o usuário rodar a varredura combinada novamente.

--------------------------------------------------------------------------------
31) Auditoria completa do simulador (pedido do usuário antes de partir pro
    dataset de ML) — 3 bugs reais encontrados e corrigidos
--------------------------------------------------------------------------------
Contexto:
  Releitura completa do código (main_orchestrator.py, config.py,
  stats_collector.py, docker_manager.py, tcpdump_sniffer.py,
  attack_summary_logger.py, cost_calculator.py, app/simple_server.py,
  traffic_injectorV0.py, normal_traffic.py) e verificação estrutural dos 50
  runs já coletados (normal_baseline/wu_calibration/combined_sweep), a
  pedido do usuário, para garantir que o simulador está correto ANTES de
  começar a gerar o dataset real de ML. A maior parte do código já estava
  correta (lógica de escalonamento, geração de tráfego, medição de CPU —
  nenhum problema novo encontrado ali). Três bugs reais encontrados:

Bug 1 — main_orchestrator.py: RTT "vazando" entre fases:
  Problema: durante a janela de ataque, o loop principal só lê
  get_average_rtt_attack_ms() (RTT só do ataque); get_average_rtt_ms()
  (RTT do tráfego normal) só é chamado nas iterações rotuladas 'normal'.
  Só que o tráfego normal nunca para durante o ataque (roda a simulação
  inteira por design) — então normal_traffic.rtt_measurements fica
  acumulando SEM SER DRENADO durante toda a janela de ataque (até ~140s).
  Na primeira iteração depois que o ataque termina, get_average_rtt_ms()
  finalmente é chamado de novo e despeja a média de TODAS essas amostras
  acumuladas numa linha só — que deveria representar um snapshot de 5s como
  qualquer outra linha, mas na prática é uma média sobre um período de até
  ~140s.
  Confirmado nos dados reais: em metrics_S2_atk10pct_wu500000.csv, a
  primeira linha 'normal' depois do ataque mostra avg_rtt_ms=73.1, contra
  ~26-27ms nas linhas logo antes e logo depois. Levantamento em todas as 33
  execuções com ataque (wu_calibration + combined_sweep): distorção de
  0.9x a ~30x (ou indefinida, nos casos de falha total de requisição já
  documentados na seção 27), afetando exatamente 1 linha por execução (33
  de ~1750 linhas no total, ~2% — mas justamente a linha da transição de
  fase, um ponto de alto valor informativo pra um futuro modelo de ML).
  Correção: get_average_rtt_ms() agora é chamado TODA iteração,
  incondicionalmente (pra sempre drenar a lista), e o valor só É USADO
  quando a fase não é de ataque — em vez de só ser chamado condicionalmente.

Bug 2 — traffic_capture.csv (captura de pacotes do tcpdump) nunca era
  salvo por execução:
  Problema: nenhum dos três scripts de varredura (run_experiments.py,
  run_experiments_normal.py, run_experiments_combined.py) movia/renomeava
  traffic_capture.csv como já faziam com simulation_metrics.csv,
  attack_summary_log.csv e rtt_log.csv — mesmo problema que rtt_log.csv
  tinha antes da correção da seção 21/24, nunca corrigido pra este arquivo.
  Resultado: dos 50 runs já coletados, só o ÚLTIMO tem traffic_capture.csv
  preservado — todos os outros 49 tiveram a captura de pacotes (o dado
  bruto rotulado por pacote, candidato a fonte de features mais rica que
  rtt_log.csv pro modelo de ML) sobrescrita e perdida.
  Correção: os três scripts agora movem traffic_capture.csv pro subdiretório
  de resultado correspondente, seguindo a mesma convenção de nome já usada
  pros outros logs (traffic_capture_rps{rps}_att{attackers}_WU{wu}.csv,
  traffic_capture_normal_rps{rps}_WU{wu}.csv,
  traffic_capture_{cenário}_atk{pct}pct_wu{wu}.csv).

Bug 3 — tcpdump_sniffer.py: parâmetro "interface" nunca era usado de
  verdade, e config.TCPDUMP_INTERFACE estava morto:
  Problema: TcpdumpSniffer.start() sempre rodava com "-i any" fixo no
  código, ignorando por completo o parâmetro `interface` recebido no
  construtor (e portanto ignorando config.TCPDUMP_INTERFACE =
  "br-4b3ba7f6ed06", um nome de bridge Docker específico de uma máquina,
  que muda se a rede for recriada). Na prática isso não chegou a
  contaminar dados: o traffic_capture.csv existente (do último run) só
  contém tráfego 127.0.0.1 nas portas esperadas (8080-8083 + portas
  efêmeras de cliente) — mas a captura era estruturalmente mais ampla que
  o necessário (toda interface do host, sem filtro de porta), o que é um
  risco real numa máquina de desenvolvimento geral (não uma VM isolada).
  Investigado ANTES de corrigir: tentar simplesmente "consertar" isso
  passando a interface da bridge Docker seria uma REGRESSÃO — o tráfego do
  simulador (cliente no host conectando em "http://localhost:<porta>")
  trafega pela interface de loopback (lo), não pela bridge Docker (que só
  vê o lado do container). Uma interface específica ficaria cega pra esse
  tráfego.
  Correção: mantido "-i any" (agora de propósito, documentado), mas o
  escopo da captura passou a ser restringido por um filtro BPF de portas
  (tcp and portrange STARTING_HOST_PORT..STARTING_HOST_PORT+MAX_INSTANCES-1)
  em vez de por interface — não depende de nomes de bridge gerados
  dinamicamente pelo Docker, e elimina o risco de capturar tráfego alheio
  do host. TCPDUMP_INTERFACE removido de config.py (estava morto, e
  "consertá-lo" teria sido a regressão acima). TcpdumpSniffer ganhou o
  parâmetro port_range; main_orchestrator.py passa interface="any" e
  port_range=(config.STARTING_HOST_PORT, config.STARTING_HOST_PORT +
  config.MAX_INSTANCES - 1) explicitamente.

Verificação estrutural dos 50 runs já coletados (sem mudança de código):
  - Nenhum arquivo faltando, nenhuma contagem de linha fora do esperado
    (34-38 por execução), nenhum NaN além do campo
    active_containers_names vazio (esperado) na primeira linha (t=0.01,
    antes da primeira leitura de stats) de cada execução.

Achado novo, ainda não investigado (fora do escopo desta correção):
  - attack_summary_log_S3_atk1pct_wu300000.csv mostra 491 erros de
    requisição mesmo com taxa de ataque planejada de só 2 req/s agregado —
    não bate com o padrão "volume alto sobrecarrega o servidor" visto em
    outros casos. Pode ser workers do injetor de ataque presos numa URL de
    alvo que ficou inválida após um SCALE_DOWN, mas não foi confirmado.
    Fica como item pra investigar depois.

Por quê:
  - Pedido explícito do usuário: garantir que o simulador está correto e
    os dados coletados são confiáveis ANTES de partir pra geração do
    dataset de ML — os bugs 1 e 2 em particular afetariam diretamente a
    qualidade de um dataset gerado a partir dos dados atuais (RTT
    distorcido numa linha por execução; e a fonte de dado mais rica pro ML
    praticamente ausente de 49 dos 50 runs já coletados).

Verificado:
  - python3 -m py_compile em main_orchestrator.py, config.py,
    tcpdump_sniffer.py, run_experiments.py, run_experiments_normal.py,
    run_experiments_combined.py — sem erros.
  - grep confirma zero referências restantes a config.TCPDUMP_INTERFACE
    fora do comentário que documenta a remoção.
  - Verificação de ponta a ponta CONFIRMADA (ver seção 33): rodado
    main_orchestrator.py de verdade com --attack-start 10 --pulse-duration
    15 --duration 40, os três fixes checados contra a saída real.

--------------------------------------------------------------------------------
32) apresentacao_professor.txt e CLAUDE.md — atualizados após a auditoria (seção 31)
--------------------------------------------------------------------------------
Contexto:
  Os dois documentos tinham ficado desatualizados em relação ao código e
  aos dados atuais — apresentacao_professor.txt foi escrito antes da
  recalibração de NORMAL_SCENARIOS (seção 30), e CLAUDE.md não era tocado
  desde bem antes desta sessão (ainda descrevia o ataque como pulsado
  estilo Yo-Yo, min_rtt_burst fixo em 50ms, CUSUM sobre o arquivo inteiro,
  EDOS_PULSE_*/EDOS_IDLE_*/TCPDUMP_INTERFACE como se ainda existissem em
  config.py, e rtt_log.csv como não sendo salvo por execução).

Alterado (apresentacao_professor.txt):
  - Item 3: adicionado item (f) resumindo a auditoria e os 3 bugs
    corrigidos na seção 31.
  - Item 4.3: corrigida a alegação "nenhuma das 24 execuções teve falha de
    requisição" (falsa depois da recalibração — S3/S4 em intensidade/custo
    altos têm falhas reais, até ~3800 erros em S4@10%/500mil) e adicionada
    uma nota sobre a recalibração de S1-S4 (40/100/200/300, não mais
    50/60/70/80 da Tabela 4).
  - Item 5, Achado 2: exemplo trocado de S1@5%/500mil (0 detecções antes
    da recalibração) para S2@1%/500mil (0 detecções, ainda válido nos
    dados atuais) — o exemplo antigo não reflete mais os dados depois da
    seção 30.
  - Item 5: adicionado Achado 3 (novo, desta auditoria) — o detector perde
    cobertura justamente no cenário mais grave (S4@10%/500mil) por causa
    das falhas reais de requisição lá, não porque o ataque enfraqueceu.
  - Item 6: mencionado que traffic_capture.csv agora é salvo por execução.
  - "28 itens" -> "31 itens" (contagem de changes.txt).

Alterado (CLAUDE.md):
  - Seção "Running the simulator": comandos atualizados (os três scripts
    de varredura e seus subdiretórios de saída, run_burst_analysis.py e
    generate_presentation_graphs.py adicionados).
  - Descrição do tráfego de ataque: de "pulsado, agendado por
    SCALE_COOLDOWN_SECONDS" pra "janela única, sem repetição" (o desenho
    Yo-Yo foi retirado há muito tempo, seção 13, mas o CLAUDE.md nunca
    tinha sido atualizado pra refletir isso).
  - Descrição do simple_server.py: de "ThreadingHTTPServer single-
    threaded-per-request" pra mencionar o ProcessPoolExecutor persistente
    e o keep-alive HTTP/1.1.
  - Descrição dos três scripts de varredura: subdiretórios de saída
    (normal_baseline/wu_calibration/combined_sweep), e corrigido "rtt_log.csv
    não é salvo por execução" pra refletir que rtt_log.csv E
    traffic_capture.csv agora SÃO salvos pelos três scripts.
  - Descrição do EntCusumZV3.py: piso fixo (50ms) e CUSUM sobre o arquivo
    inteiro substituídos pela descrição do piso adaptativo (seção 26);
    adicionada menção a run_burst_analysis.py; achados de diluição e perda
    de cobertura (seção 27) documentados como caveats conhecidos.
  - "Known inconsistencies": removida a entrada sobre EDOS_PULSE_*/
    EDOS_IDLE_*/EDOS_SATURATION_* (não existem mais em config.py, foram
    removidos de verdade na seção 13, não só "não usados" como a entrada
    antiga dizia). Adicionadas entradas sobre TCPDUMP_INTERFACE removido,
    attack_summary_log.csv só ter linhas de ataque, e o achado ainda não
    explicado do S3_atk1pct_wu300000 (ver seção 31).

Por quê:
  - Documentação desatualizada que afirma algo FALSO sobre o comportamento
    atual do código (ex: "ataque é pulsado", "nenhuma falha de requisição
    em 24 execuções") é pior que não ter documentação — engana quem lê
    depois, seja o orientador seja uma sessão futura do Claude Code neste
    projeto.

Verificado:
  - Releitura completa dos dois arquivos depois da edição, conferindo que
    nenhuma referência a EDOS_PULSE_*/TCPDUMP_INTERFACE/"pulsado" continua
    presente fora do contexto histórico (changes.txt, docs/*.md).
  - Números citados nos dois documentos (contagem de itens do changes.txt,
    valores de S1-S4, contagem de erros por cenário) conferidos contra os
    dados reais em experiment_results/combined_sweep/ antes de escrever,
    não copiados de memória.

--------------------------------------------------------------------------------
33) Verificação de ponta a ponta dos 3 fixes da seção 31 (confirmada)
--------------------------------------------------------------------------------
Execução real:
  sudo python3 main_orchestrator.py --rps 5 --attackers 4 --work-units
  300000 --attack-start 10 --pulse-duration 15 --duration 40
  (run curto: ataque de t=10 a t=25, 15s de tráfego só-normal depois, o
  suficiente pra observar a transição fim-de-ataque sem esperar os 180s
  padrão.)

Resultado — Fix 1 (RTT vazando entre fases):
  Linha de transição (t=30, primeira rotulada 'normal' depois do ataque)
  mostrou avg_rtt_ms=42.65 — elevado frente ao baseline (~18ms) mas um
  valor plausível de janela de 5s, não a distorção de até ~30x (ou
  indefinida) vista nos dados antigos antes da correção.

Resultado — Fix 2 (traffic_capture.csv por execução):
  traffic_capture.csv presente com 9312 linhas de dado real (562KB) —
  antes da correção esse arquivo simplesmente não seria salvo por nenhum
  dos scripts de varredura.

Resultado — Fix 3 (filtro de porta do tcpdump):
  0 dos 9312 pacotes capturados caem fora da faixa de porta alvo
  (8080-8083) tanto no src_port quanto no dst_port — confirmado via
  `awk -F, '!(($3>=8080 && $3<=8083) || ($5>=8080 && $5<=8083))'`. As
  portas altas observadas na coluna dst_port (ex.: 58818) são o lado
  efêmero do cliente nos pacotes de RESPOSTA do servidor (src_port=8080),
  não vazamento — captura bidirecional funcionando como esperado com
  "portrange 8080-8083".

Verificado:
  - Os 4 arquivos de saída (simulation_metrics.csv, rtt_log.csv,
    traffic_capture.csv, attack_summary_log.csv) apareceram na raiz do
    projeto como esperado (chamada direta a main_orchestrator.py, fora de
    um script de varredura) e foram removidos depois de conferidos (só
    lixo de verificação, não fazem parte do dataset real).
