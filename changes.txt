EDOS_SIM2 - Registro de Alterações (Change Log)
================================================

Branch: develop_calibrating_treshold
Objetivo geral: remover o teto artificial de capacidade da instância alvo (GIL)
e o auto-throttling do injetor de ataque, para permitir calibrar com precisão
os limiares de CPU que disparam o autoscaling (base para o ataque Yo-Yo).

--------------------------------------------------------------------------------
1) config.py
--------------------------------------------------------------------------------
Adicionado:
  INSTANCE_MAX_CONCURRENT_REQUESTS = 2
  HTTP_ATTACK_MAX_CONCURRENT_SENDS = 64

Por quê:
  - INSTANCE_MAX_CONCURRENT_REQUESTS define explicitamente quantos processos
    concorrentes uma instância pode usar para atender requisições (substitui
    o teto acidental que vinha do GIL do Python quando o servidor usava threads).
  - HTTP_ATTACK_MAX_CONCURRENT_SENDS limita quantas requisições de ataque podem
    estar "em voo" ao mesmo tempo, evitando crescimento ilimitado de threads/tarefas
    caso o alvo não consiga responder no ritmo configurado.

--------------------------------------------------------------------------------
2) app/simple_server.py
--------------------------------------------------------------------------------
Alterado:
  - Import: "ThreadingHTTPServer" trocado por "HTTPServer" + "socketserver".
  - Nova classe ForkingHTTPServer(socketserver.ForkingMixIn, HTTPServer), com
    max_children lido da variável de ambiente INSTANCE_MAX_CONCURRENT_REQUESTS.
  - O bloco __main__ agora instancia ForkingHTTPServer em vez de ThreadingHTTPServer.

Por quê:
  - O loop de trabalho de CPU (math.sqrt/math.sin) não libera o GIL, então com
    ThreadingHTTPServer múltiplas requisições concorrentes eram, na prática,
    processadas em série por um único núcleo, não importa quantas threads/CPUs
    existissem. Isso criava um teto de capacidade "acidental" (~17 RPS observado),
    que dependia do hardware/interpretador, não de uma escolha de projeto.
  - Com ForkingMixIn, cada requisição roda em um processo do SO separado,
    permitindo paralelismo real entre núcleos, com um teto explícito e
    reprodutível (max_children).

--------------------------------------------------------------------------------
3) docker_manager.py
--------------------------------------------------------------------------------
Alterado:
  - start_instance(): a chamada client.containers.run(...) agora passa
    environment={"INSTANCE_MAX_CONCURRENT_REQUESTS": str(config.INSTANCE_MAX_CONCURRENT_REQUESTS)}

Por quê:
  - Antes nenhuma variável de ambiente era passada para os containers (estava
    comentado no código). Sem isso, o simple_server.py não teria como saber
    qual o valor de max_children configurado em config.py.

--------------------------------------------------------------------------------
4) traffic_injectorV0.py — envio open-loop
--------------------------------------------------------------------------------
Alterado:
  - Import adicional: "from concurrent.futures import ThreadPoolExecutor".
  - Novo pool global _send_pool = ThreadPoolExecutor(max_workers=config.HTTP_ATTACK_MAX_CONCURRENT_SENDS)
  - Nova função _send_one(session, target_url, counters, counters_lock): executa de fato
    a chamada bloqueante session.get(...), mede o RTT e atualiza os contadores
    ok/err. Roda dentro de uma thread do _send_pool.
  - http_request_worker(): antes fazia o request de forma bloqueante dentro do
    próprio laço de ritmo (closed-loop): enviava, esperava a resposta, só então
    decidia dormir ou não. Agora o laço só decide QUANDO agendar o próximo envio
    (via _send_pool.submit(...)) e nunca espera a resposta antes de continuar
    (open-loop). O tempo de resposta do alvo não afeta mais o ritmo de disparo.

Por quê:
  - Antes, cada worker só tinha 1 requisição "em voo" por vez. Se o alvo demorasse
    mais que o intervalo alvo (1/RPS) para responder, o worker automaticamente
    reduzia sua taxa real de envio — o RPS "planejado" (planned_total_rps) e o
    "real" (real_rps) do CSV divergiam sem que isso representasse perda de pacotes
    (nenhum erro/timeout era registrado), apenas o injetor se autolimitando.
  - Isso tornava impossível calibrar com precisão "quanto tráfego preciso mandar
    para estourar o limiar de CPU", porque o tráfego realmente entregue dependia
    de fatores incidentais (contenção, nº de instâncias), não só da configuração.
  - Com o envio desacoplado da espera pela resposta, o RPS configurado passa a
    refletir a demanda real ofertada ao alvo, tornando os experimentos de
    calibração de limiar (CPU_THRESHOLD_SCALE_UP/DOWN) reprodutíveis.

--------------------------------------------------------------------------------
5) traffic_injectorV0.py — correção de subcontagem no fim do pulso
--------------------------------------------------------------------------------
Problema encontrado:
  Ao parar o ataque, o laço de ritmo saía e lia os contadores (ok/err)
  imediatamente, mas requisições que ainda estavam em voo no _send_pool
  (enviadas mas sem resposta ainda) só terminariam DEPOIS dessa leitura,
  e nunca eram contabilizadas no log de resumo. Ou seja, total_requests/errors
  no attack_summary_log.csv podiam ficar levemente subestimados no fim de cada pulso.

Correção:
  - http_request_worker() agora guarda as Futures de cada envio numa lista
    (pending_futures), descartando periodicamente as que já terminaram (para não
    acumular memória em ataques longos).
  - Ao sair do laço (attack_active == False), o worker espera (com timeout de
    segurança) todas as Futures pendentes terminarem antes de ler os contadores
    finais e chamar attack_summary_logger.log_worker_stop(...).

Por quê:
  - Garante que total_requests/errors no CSV reflitam TODAS as requisições que
    o worker efetivamente enviou durante o pulso, sem subcontagem na borda do
    fim do ataque — importante porque esses números alimentam o dataset usado
    para treinar o modelo de ML.

--------------------------------------------------------------------------------
6) app/simple_server.py — remoção do fork-per-request
--------------------------------------------------------------------------------
Problema encontrado (via teste manual):
  Testando tráfego normal (benigno) com NORMAL_WORK_UNITS=10 (praticamente
  gratuito) a 100 RPS x 4 clientes, o CPU médio da instância ficou em ~200%
  (2 núcleos cheios) SEM nenhum ataque. Com RPS=10 x 4 clientes, ~30%.
  Isso não podia vir do trabalho em si (quase zero); veio do ForkingHTTPServer
  (mudança A3 anterior), que cria um PROCESSO NOVO do zero para cada requisição
  HTTP recebida. Criar processo tem custo real de CPU (kernel, tabelas de
  página, agendamento, waitpid) mesmo que o trabalho dentro dele seja irrisório.
  Em alta taxa de requisições (400/s), esse custo por requisição, multiplicado
  centenas de vezes por segundo, virou o principal consumidor de CPU — fazendo
  com que o CPU% passasse a refletir a TAXA de requisições, não o WORK_UNITS
  configurado. Isso comprometeria a separação entre tráfego benigno e ataque
  no dataset (tráfego benigno "pesado em volume" passaria a parecer ataque).

Correção:
  - app/simple_server.py voltou a usar ThreadingHTTPServer (sem fork por
    requisição) para aceitar/parsear conexões HTTP (barato).
  - O laço de CPU (math.sqrt/math.sin) foi movido para uma função de nível de
    módulo, _burn_cpu(work_units), executada dentro de um
    concurrent.futures.ProcessPoolExecutor PERSISTENTE (_pool), criado uma
    única vez no startup do servidor (bloco __main__), com
    max_workers=INSTANCE_MAX_CONCURRENT_REQUESTS.
  - do_GET agora faz _pool.submit(_burn_cpu, work_units).result() em vez do
    laço direto ou de depender do ForkingHTTPServer.
  - config.py: comentário de INSTANCE_MAX_CONCURRENT_REQUESTS atualizado para
    refletir que agora é o tamanho do pool persistente, não mais "max_children"
    de um servidor forking.

Por quê:
  - O pool de processos é criado UMA VEZ; os processos são reaproveitados para
    todas as requisições subsequentes, então o custo de criar processos deixa
    de ser pago a cada requisição.
  - Cada processo do pool ainda roda em paralelo real (seu próprio GIL), então
    o objetivo original da mudança A3 (paralelismo real entre núcleos) é
    preservado — só o mecanismo de obter esse paralelismo mudou.
  - Efeito colateral esperado: os números de capacidade medidos anteriormente
    (~17 RPS de teto, tempos de 1x/2x/4x) ficam desatualizados, pois incluíam
    o custo de fork-per-request. Precisarão ser remedidos antes da Fase 1 de
    calibração.

--------------------------------------------------------------------------------
7) stats_collector.py — average_cpu_percent passa a ser uma média real da janela
--------------------------------------------------------------------------------
Problema encontrado (via análise de simulation_metrics.csv de um teste real):
  Quatro pulsos de ataque IDÊNTICOS (confirmado por attack_summary_log.csv:
  mesmo real_rps, mesmo total_requests, mesma duração) produziam leituras de
  CPU bem diferentes entre si (28/36/30/41%). Causa: get_averages() apenas
  retornava a última amostra em cache (_cache[cid]), sobrescrita a cada
  ~1s pela thread de polling — não uma média sobre a janela de 5s do
  MONITOR_INTERVAL_SECONDS. Qual amostra de ~1s calhava de estar em cache no
  momento exato da leitura dependia de jitter de agendamento entre duas
  threads independentes, não da intensidade real do ataque.

Correção:
  - Novo _history: Dict[str, deque] guarda (timestamp, cpu_percent,
    mem_app_mb, name) por container, podado para as últimas
    history_seconds (padrão 30s).
  - _run() agora grava cada amostra também no histórico, além do cache.
  - get_averages(window_seconds=None): sem argumento, mantém o
    comportamento antigo (última amostra, útil p/ inspeção pontual/self-test).
    Com window_seconds, calcula a média de TODAS as amostras dos últimos
    window_seconds segundos, com fallback para o cache mais recente se não
    houver amostras na janela (ex.: container recém-adicionado).
  - update_containers() agora também poda o histórico de containers que saíram.

Por quê:
  - Faz average_cpu_percent refletir genuinamente a janela de monitoramento
    inteira (múltiplas amostras), não uma leitura pontual sujeita a sorte de
    timing — elimina a variação espúria entre pulsos idênticos.
  - Verificado com teste unitário isolado (histórico sintético) confirmando
    que a média da janela bate com o cálculo manual, e com uma execução real
    da simulação (ver item 8).

--------------------------------------------------------------------------------
8) main_orchestrator.py — label do CSV de métricas alinhado com average_cpu_percent
--------------------------------------------------------------------------------
Problema encontrado (mesma análise do item 7):
  A cada pulso, a linha com label='attack' aparecia com CPU baixo (pré-ataque),
  e o pico de CPU real aparecia na linha SEGUINTE, já rotulada 'normal'.
  Causa: label só virava 'attack' dentro do bloco que DECIDE iniciar o
  injetor nesta iteração — decisão tomada DEPOIS da leitura de avg_cpu, que
  por sua vez refletia o estado ANTES dessa decisão ter efeito.

Correção:
  - stats_collector.get_averages() agora é chamado com
    window_seconds=config.MONITOR_INTERVAL_SECONDS (ver item 7).
  - label passa a ser calculado logo após a leitura de métricas, ANTES da
    decisão de start/stop desta iteração: label = 'attack' if
    attack_has_started else 'normal' — ou seja, reflete o estado herdado da
    decisão da iteração ANTERIOR, que é o que avg_cpu acabou de medir.
  - Removidas as duas atribuições antigas de label ('normal' redundante logo
    após o bloco de ataque; 'attack' dentro do bloco de start/restart do
    injetor), que sobrescreviam o valor correto calculado acima.
  - sniffer.set_label(...) (usado por tcpdump_sniffer.py para rotular
    traffic_capture.csv em tempo real) NÃO foi alterado — já estava correto
    (confirmado inspecionando o CSV de captura: rótulo "attack" aparece
    exatamente na janela real do pulso, não deslocado).

Por quê:
  - label e average_cpu_percent agora sempre descrevem a MESMA janela de
    tempo, em vez de janelas deslocadas por um tick.
  - Verificado com uma execução real de 50s (2 pulsos): CPU de pico (49.61%
    e 37.4%) apareceu corretamente nas linhas rotuladas 'attack' (t=25 e
    t=45), não mais nas linhas seguintes rotuladas 'normal'.

--------------------------------------------------------------------------------
9) Problemas encontrados — status atualizado
--------------------------------------------------------------------------------
  - [CORRIGIDO no item 18] normal_traffic.py: save_rtt_log() usava o nome de
    arquivo "rtt_log.csv" fixo no código, ignorando qualquer configuração —
    inconsistente com os outros logs (que usam config.METRICS_LOG_FILE etc).
  - [AINDA NÃO CORRIGIDO] O problema de permissão em si (arquivos ficarem
    donos de root após uma execução com sudo, bloqueando execuções futuras
    sem sudo) continua existindo para simulation_metrics.csv,
    traffic_capture.csv, attack_summary_log.csv e agora também rtt_log.csv
    (mesmo lendo o nome do config, o arquivo em si ainda fica root-owned se
    criado via sudo). Não é um bug de código — é uma questão de como as
    execuções são feitas (sudo vs. não-sudo). Mencionado aqui para referência
    futura, caso vire um problema recorrente.

--------------------------------------------------------------------------------
18) normal_traffic.py — save_rtt_log() lê o nome do arquivo de config.py
--------------------------------------------------------------------------------
Alterado:
  - config.py: novo RTT_LOG_FILE = "rtt_log.csv".
  - normal_traffic.py: save_rtt_log(filename=None) agora usa
    config.RTT_LOG_FILE como padrão quando nenhum argumento é passado, em vez
    de ter "rtt_log.csv" fixo na assinatura da função — mesmo padrão já usado
    por METRICS_LOG_FILE, TCPDUMP_OUTPUT_CSV, ATTACK_SUMMARY_LOG_FILE.

Por quê:
  - Consistência com o resto do projeto (todo log configurável via
    config.py) e para permitir redirecionar o caminho de saída em testes
    (como os testes de verificação já feitos nesta sessão) sem precisar
    tocar no arquivo real do repositório.

Verificado:
  - config.RTT_LOG_FILE sobrescrito para um caminho em /tmp antes de chamar
    save_rtt_log() sem argumentos: arquivo escrito corretamente no caminho
    configurado, com o conteúdo esperado.

--------------------------------------------------------------------------------
10) main_orchestrator.py — novos overrides de CLI para varreduras automatizadas
--------------------------------------------------------------------------------
Adicionado:
  --work-units (config.ATTACK_WORK_UNITS)
  --attack-duration (config.ATTACK_DURATION_SECONDS)
  --normal-rps (config.HTTP_NORMAL_RPS_PER_CLIENT)
  --normal-work-units (config.NORMAL_WORK_UNITS)

Por quê:
  - Antes, apenas --rps/--attackers/--duration eram configuráveis via CLI;
    WORK_UNITS e os parâmetros de tráfego normal só podiam ser alterados
    editando config.py manualmente entre execuções do run_experiments.py.
  - Permite automatizar varreduras de 3+ variáveis (RPS x ATTACKERS x
    WORK_UNITS) e rodar cenários apenas com tráfego normal
    (--attack-duration 0) sem editar config.py, viabilizando os scripts de
    varredura descritos a seguir (itens 11 e 12).

--------------------------------------------------------------------------------
11) run_experiments.py — varredura automatizada também em WORK_UNITS
--------------------------------------------------------------------------------
Alterado:
  - Adicionado WORK_UNITS_VALUES = [5000, 20000, 50000, 100000].
  - O loop agora é itertools.product(RPS_VALUES, ATTACKERS_VALUES,
    WORK_UNITS_VALUES) (antes: só RPS_VALUES x ATTACKERS_VALUES).
  - Cada execução passa --work-units <valor> para main_orchestrator.py.
  - Removida a leitura estática "work_units = config.ATTACK_WORK_UNITS" (e o
    "import config", agora sem uso) — work_units passa a ser a variável do
    loop, com o mesmo valor usado tanto no comando quanto no nome dos
    arquivos de saída.

Por quê:
  - Antes, WORK_UNITS só mudava entre lotes de execução se alguém editasse
    config.py manualmente entre chamadas do script — não fazia parte da
    varredura automatizada. Isso impedia encontrar o ponto de cruzamento do
    limiar de CPU sem intervenção manual repetida.
  - Valores escolhidos com base em dados já coletados nesta sessão: WU=5000
    a RPS=40 total (10 rps x 4 atacantes) chegou a ~35-49% de CPU, abaixo do
    CPU_THRESHOLD_SCALE_UP (60%) — a varredura estende para valores maiores
    (20000/50000/100000) para encontrar o ponto onde o scale-up realmente
    dispara.

--------------------------------------------------------------------------------
12) run_experiments_normal.py (novo) — varredura de baseline, só tráfego normal
--------------------------------------------------------------------------------
Adicionado:
  - Novo script, irmão de run_experiments.py, varrendo
    itertools.product(NORMAL_RPS_VALUES=[10,25,50,100],
    NORMAL_WORK_UNITS_VALUES=[10,100,1000,5000]) — 16 combinações.
  - Cada execução chama main_orchestrator.py com --normal-rps,
    --normal-work-units e --attack-duration 0 (força o modo sem ataque já
    existente no orquestrador), --duration 180.
  - Salva em experiment_results/metrics_normal_rps{X}_WU{Y}.csv.
  - attack_summary_log.csv (sempre criado, mesmo vazio, por
    attack_summary_logger.init_attack_summary_log()) é descartado a cada
    execução em vez de salvo, já que não há dados de ataque neste modo.

Por quê:
  - Fornece o baseline de comparação para a varredura de ataque (item 11):
    caracteriza CPU/RTT sob tráfego normal na MESMA faixa de intensidade,
    para permitir avaliar depois se uma configuração de ataque que cruza o
    limiar de scale-up é estatisticamente distinguível do que o tráfego
    normal produziria no mesmo ponto (questão de "stealth").
  - Testado com uma execução curta (20s, --normal-rps 25 --normal-work-units
    100 --attack-duration 0): confirmado que os argumentos de CLI chegam
    corretamente ao config, o modo sem ataque é ativado, e o CPU responde de
    forma sensata (~40-44%) à intensidade configurada.

--------------------------------------------------------------------------------
13) Fase 1 do roadmap W-EDoS: aposentar a lógica de pulso repetido (Yo-Yo)
--------------------------------------------------------------------------------
Contexto:
  Decisão do usuário de seguir apenas a estrutura workload-based (W-EDoS) do
  artigo de Sotelo Monge et al., abandonando a síntese com o ataque Yo-Yo
  (Bremler-Barr et al.) discutida anteriormente. O ataque deixa de pulsar
  repetidamente e passa a ser uma janela ÚNICA: a simulação começa só com
  tráfego normal, o ataque liga uma vez, roda por uma duração configurável,
  desliga, e o tráfego normal continua sozinho até o fim.

Alterado:
  - config.py:
      * PULSE_DURATION agora representa a duração TOTAL e única do ataque
        (não mais a duração de um pulso individual dentro de um trem de
        pulsos repetidos).
      * ATTACK_DURATION_SECONDS mantém seu papel de liga/desliga (0 = sem
        ataque agendado, usado por --attack-duration 0), mas seu valor
        numérico deixou de ter qualquer efeito sobre a duração do ataque —
        isso agora é controlado inteiramente por PULSE_DURATION. Comentários
        atualizados para deixar isso explícito.
      * Removido o bloco inteiro de configurações EDOS_PULSE_*/EDOS_IDLE_*/
        EDOS_SATURATION_PHASE_DURATION_SECONDS — nunca chegaram a ser
        conectadas à lógica do orquestrador e eram específicas do desenho
        Yo-Yo agora abandonado.
  - main_orchestrator.py:
      * Removidas as variáveis mortas tracked_attack_state e
        last_pulse_end_time (declaradas, nunca usadas — resquício do desenho
        Yo-Yo).
      * Removido o clamp que limitava PULSE_DURATION a no máximo
        SCALE_COOLDOWN_SECONDS antes do loop principal — não faz mais
        sentido, já que PULSE_DURATION agora pode ter qualquer duração (é a
        duração inteira do ataque, não mais um pulso que precisa caber dentro
        de um ciclo de cooldown).
      * attack_start_time/attack_end agora são calculados UMA VEZ antes do
        loop e nunca recalculados — removido o bloco que reagendava o
        próximo pulso (attack_start_time += SCALE_COOLDOWN_SECONDS) a cada
        vez que a janela terminava.
      * Removida a checagem de "limite externo" baseada na magnitude de
        ATTACK_DURATION_SECONDS (if config.ATTACK_DURATION_SECONDS <
        elapsed_time_seconds), redundante agora que a janela única já define
        completamente quando o ataque está ativo.
      * Removida a lógica de is_max_instance que empurrava a janela de
        ataque para frente quando o cluster já estava no tamanho máximo
        (mecanismo específico para evitar pulsar inutilmente durante
        oscilação Yo-Yo). As duas ramificações (com/sem is_max_instance)
        foram unificadas em uma única checagem de start/restart do injetor.

Por quê:
  - Implementa exatamente o modelo W-EDoS sustentado (não pulsado) do artigo
    de referência: uma única janela de ataque, sem ciclos repetidos.
  - Simplifica o código removendo mecanismos (reagendamento, clamp de
    cooldown, desvio por instância máxima) que só faziam sentido para pulsos
    repetidos.

Verificação:
  - Execução real de 60s (--duration 60, config padrão: ATTACK_START_TIME_
    SECONDS=20, PULSE_DURATION=5): confirmado que a janela [20s, 25s] nunca
    muda ao longo de toda a execução, o ataque liga e desliga exatamente uma
    vez (CPU sobe para 40.55% na linha rotulada 'attack' em t=25, alinhado
    corretamente com o fix de label do item 8), e "Should attack be active
    now?" permanece False do t=30 até o t=55 — sem o segundo pulso que a
    lógica antiga teria disparado por volta do t=40 (20+SCALE_COOLDOWN_
    SECONDS=20). Tráfego normal (~11-17% CPU) roda sem interrupção durante
    toda a simulação, incluindo antes, durante e depois da janela de ataque.

--------------------------------------------------------------------------------
14) main_orchestrator.py — CLI overrides para a janela de ataque
--------------------------------------------------------------------------------
Adicionado:
  --attack-start (config.ATTACK_START_TIME_SECONDS)
  --pulse-duration (config.PULSE_DURATION)

Por quê:
  - Permite que o sweep runner combinado (Fase 2, cenários estilo Tabela 5)
    controle a janela de ataque por execução via CLI, no mesmo padrão dos
    overrides existentes, em vez de depender de valores fixos em config.py.
  - Atenção ao nome: --attack-duration (já existente) continua controlando
    apenas o liga/desliga (ATTACK_DURATION_SECONDS); --pulse-duration é quem
    de fato controla a duração da janela única de ataque (PULSE_DURATION,
    redefinido na Fase 1). São dois flags parecidos no nome com papéis bem
    diferentes — documentado aqui para não confundir no futuro.

Verificado:
  - Execução real de 40s com --attack-start 10 --pulse-duration 20: janela
    corretamente deslocada para [10s, 30s], rótulos 'attack'/'normal' no CSV
    de métricas alinhados corretamente antes/durante/depois da janela.

--------------------------------------------------------------------------------
15) run_experiments_combined.py (novo) — varredura combinada (Tabela 5)
--------------------------------------------------------------------------------
Adicionado:
  - Novo script, irmão de run_experiments.py/run_experiments_normal.py,
    varrendo NORMAL_SCENARIOS (S1-S4: 50/60/70/80 req/s agregado, igual à
    Tabela 4) x ATTACK_INTENSITIES_PCT ([1, 5, 10]) — 12 combinações.
  - Para cada combinação, calcula o RPS agregado de ataque como
    normal_aggregate_rps * pct / 100 (a duração se cancela na conversão de
    "% do total de requisições normais" para RPS), distribui entre
    NORMAL_NUM_CLIENTS=4 / ATTACK_NUM_ATTACKERS=4, e chama
    main_orchestrator.py via --normal-rps/--rps/--attackers/--work-units/
    --normal-work-units/--duration.
  - ATTACK_WORK_UNITS fixado em 100000 (maior valor já calibrado nesta sessão
    que cruza o limiar de scale-up em RPS moderado — ver item 11).
  - Deliberadamente NÃO sobrescreve --attack-start/--pulse-duration: herda
    os valores atuais de config.py (janela [20s, 160s] dentro de uma
    simulação de 180s), mantendo o formato de warm-up/cool-down com tráfego
    só-normal já estabelecido na Fase 1, em vez do desenho original da
    Tabela 5 (ataque cobrindo a janela inteira).
  - Salva em experiment_results/metrics_{cenário}_atk{pct}pct.csv e
    attack_summary_log_{cenário}_atk{pct}pct.csv.

Por quê:
  - Reproduz a Tabela 5 de Sotelo Monge et al.: para cada cenário de tráfego
    normal, sobrepõe ataque como uma fração do volume total de requisições
    normais — o experimento central da estrutura W-EDoS escolhida.

Verificado:
  - Execução real de um cenário (S1 @ 5%, duração reduzida para 40s para
    teste): tráfego normal roda desde t=0, sobreposição de ataque ativa
    corretamente a partir da janela configurada, CPU sobe de ~15% (baseline)
    para ~21-30% e RTT sobe de ~6-8ms para ~42-51ms durante o ataque — sinal
    claramente detectável mesmo numa intensidade baixa (5%) que não chega a
    disparar SCALE_UP, o que é o caso interessante para a análise estatística
    da Fase 3.5.

--------------------------------------------------------------------------------
16) Chegadas de Poisson para tráfego normal e de ataque
--------------------------------------------------------------------------------
Contexto:
  Sotelo Monge et al. geram tráfego normal seguindo um processo de Poisson
  (intervalos entre requisições exponencialmente distribuídos, taxa λ) em vez
  de intervalos fixos — modela clientes independentes de forma mais realista
  e dá à análise de entropia (Fase 3.5) uma variabilidade genuína para
  caracterizar, em vez de principalmente ruído de fila/rede.

Alterado:
  - config.py: novo HTTP_NORMAL_MAX_CONCURRENT_SENDS = 64 (mesmo padrão de
    HTTP_ATTACK_MAX_CONCURRENT_SENDS).
  - traffic_injectorV0.py (http_request_worker): o cálculo de sleep_duration
    deixou de ser "intervalo fixo menos tempo de despacho" e passou a ser um
    sorteio independente de random.expovariate(rps_per_worker) a cada
    iteração — processo de Poisson de taxa rps_per_worker.
  - normal_traffic.py: normal_http_request_worker foi reestruturado para
    seguir exatamente o mesmo padrão open-loop de traffic_injectorV0.py (B2):
    novo _send_pool (ThreadPoolExecutor) e _send_one_normal() executam a
    chamada bloqueante de fato, desacoplados do laço de ritmo; o laço agora
    só decide QUANDO agendar o próximo envio, com o intervalo sorteado de
    random.expovariate(rps_per_worker); ao parar, drena as requisições em
    voo (pending_futures) antes de logar o resumo final, evitando
    subcontagem (mesmo fix do item 4).

Por quê:
  - Chegadas de Poisson exigem que os disparos sejam independentes uns dos
    outros — um laço fechado (que espera a resposta antes de decidir o
    próximo intervalo) distorceria os sorteios sob carga, o mesmo problema
    de auto-limitação já corrigido no injetor de ataque (item 2). Por isso
    normal_traffic.py precisou do mesmo tratamento open-loop, não só da
    troca da fórmula de intervalo.
  - Mantém tráfego normal e de ataque estruturalmente simétricos (mesmo
    padrão open-loop + Poisson dos dois lados), evitando que uma
    regularidade "robótica" de um dos dois vire um sinal de detecção
    artificial não relacionado ao consumo de CPU real.

Verificado:
  - Teste real contra um container de teste (10 rps, 1 worker, 8s cada):
    coeficiente de variação dos intervalos entre chegadas (std/média, que
    deve ser ~1.0 para um processo de Poisson e ~0 para intervalo fixo).
    normal_traffic.py: CV=0.944 (n=76). traffic_injectorV0.py: CV=1.101
    (n=88). Ambos consistentes com chegadas de Poisson.

--------------------------------------------------------------------------------
17) Bug real encontrado em execução do usuário: RuntimeError no shutdown
--------------------------------------------------------------------------------
Problema encontrado (via execução real do usuário com configuração padrão):
  Ao final de uma simulação bem-sucedida, threads de tráfego normal
  (InjectorWorker-N, normal_traffic.py) lançavam
  "RuntimeError: cannot schedule new futures after shutdown" ao tentar
  _send_pool.submit(...). Causa raiz: normal_traffic.stop_http_traffic()
  nunca era chamado no caminho de encerramento bem-sucedido de main() em
  main_orchestrator.py — só existia nos handlers de KeyboardInterrupt/erro.
  Como o tráfego normal roda a simulação inteira por design (nunca parado
  dentro do loop), os workers (daemon threads) continuavam vivos até o
  interpretador Python encerrar. O hook de shutdown automático do
  concurrent.futures (registrado via atexit na primeira vez que o módulo é
  importado, cobre TODAS as ThreadPoolExecutor do processo) finaliza
  _send_pool antes desses workers notarem — daí o erro ao tentar agendar
  mais uma requisição. Isso só passou a se manifestar depois da mudança de
  open-loop/Poisson (item 16): antes não existia _send_pool para o hook de
  shutdown derrubar.

Correção:
  - main_orchestrator.py: adicionada chamada a normal_traffic.stop_http_traffic()
    no caminho de encerramento bem-sucedido de main() (mesmo padrão já usado
    para traffic_injectorV0.stop_http_flood()).
  - normal_traffic.py e traffic_injectorV0.py: a chamada a _send_pool.submit()
    dentro do laço de cada worker agora está protegida por
    try/except RuntimeError, tratando "pool já finalizada" como sinal para
    encerrar o laço graciosamente (break) em vez de propagar uma exceção não
    tratada — cobre a corrida residual que ainda existiria mesmo com o fix
    acima (o join em stop_http_traffic()/stop_http_flood() é curto e não
    bloqueante, então um worker no meio de um sleep sorteado mais longo pode
    ainda não ter notado o sinal de parada).

Verificado:
  - Reproduzido o erro original com uma execução real de 30s (--duration 30
    via script), confirmando o traceback antes da correção.
  - Após a correção, mesma execução: código de saída 0, "Stopping normal
    traffic..." aparece antes da limpeza dos containers, todos os workers
    reportam suas contagens finais e param graciosamente, zero exceções/
    tracebacks no log completo.

Nota:
  - Durante o diagnóstico, ao tentar reproduzir o erro localmente sem sudo,
    um rtt_log.csv de dono root (da execução real do usuário) bloqueou a
    escrita e foi removido para desbloquear o teste — sem querer, isso
    apagou o resultado real da execução de diagnóstico do usuário (que já
    tinha sido apenas um teste, sem dados necessários preservados,
    confirmado com o usuário). Verificações seguintes passaram a usar
    caminhos de saída em /tmp, como já era o padrão estabelecido antes.

--------------------------------------------------------------------------------
19) app/simple_server.py — keep-alive (HTTP/1.1) para evitar esgotamento de conexões
--------------------------------------------------------------------------------
Problema encontrado (via análise de experiment_results/metrics_normal_*.csv):
  8 de 16 execuções de run_experiments_normal.py mostravam um colapso súbito
  e permanente para avg_rtt_ms=0.0 (zero requisições bem-sucedidas por
  janela) que nunca se recuperava — incluindo rps50 (um valor real da
  Tabela 4 de Sotelo Monge et al., não só valores exploratórios extras).
  Quanto maior o RPS agregado, mais cedo e mais severo o colapso.

  Causa raiz: SimpleAppHandler nunca usava HTTP keep-alive (sem
  protocol_version="HTTP/1.1", cai no padrão HTTP/1.0, que sempre fecha a
  conexão após cada resposta, independente do que o cliente pedir). A ~100
  req/s sem nenhum reaproveitamento de conexão, isso significa ~100 conexões
  TCP novas por segundo, cada uma ficando em TIME_WAIT por 60s
  (tcp_fin_timeout) depois de fechada.

  Reproduzido diretamente contra um container de teste (rps=100, WU=5000,
  30s): TIME_WAIT subiu de 2 para 2694 em 30s, e o CPU do container caiu de
  ~56% para 3.3% no mesmo intervalo de amostragem — reprodução direta do
  colapso visto nos dados reais. nf_conntrack_max (65536) e a faixa de
  portas efêmeras (28232) foram descartados como causa direta (não chegaram
  perto do limite), mas o mecanismo — esgotamento por alto churn de conexões
  — ficou claro e diretamente correlacionado.

Correção:
  - SimpleAppHandler ganhou protocol_version = "HTTP/1.1", habilitando o
    keep-alive nativo do BaseHTTPRequestHandler da stdlib.
  - Necessário também: a resposta passou a incluir o header Content-Length
    (calculado a partir do corpo já montado em bytes, antes de enviar os
    headers). Sem isso, keep-alive quebraria a comunicação — o cliente não
    teria como saber onde uma resposta termina numa conexão que continua
    aberta para a próxima requisição (antes, o fechamento da conexão é que
    sinalizava o fim da resposta; com keep-alive isso deixa de existir).

Verificado:
  - Reproduzida a mesma cena (rps=100, WU=5000, 30s) contra um container
    reconstruído com a correção: TIME_WAIT ficou estável em ~4 durante toda
    a execução (em vez de subir a 2694), conexões estabelecidas
    estabilizaram em ~42 (reaproveitadas, consistente com ~10 conexões por
    sessão x 4 workers), CPU ficou estável entre 15-48% o tempo todo, e os
    4 workers reportaram 0 erros ao final.

Nota:
  - Como cada worker usa uma única requests.Session() compartilhada entre
    todos os envios da _send_pool, o keep-alive também limita implicitamente
    a ~10 conexões concorrentes por worker (pool_maxsize padrão do
    urllib3/requests) — um efeito colateral razoável (limita uso de
    recursos), mas vale ter em mente.
  - As execuções de run_experiments_normal.py já coletadas (que mostraram o
    colapso) precisam ser refeitas com esta correção antes de serem usadas
    como dataset final.

--------------------------------------------------------------------------------
20) experiment_results/ — limpeza de resultados obsoletos
--------------------------------------------------------------------------------
Removido (git rm, 42 arquivos):
  - metrics_rps{1,5,10}_att4_WU{5000,20000,50000}.csv e os
    attack_summary_log_{1,5,10}_4_WU{5000,20000,50000}.csv correspondentes
    (18 arquivos): pontos de uma faixa de calibração de WORK_UNITS já
    abandonada — run_experiments.py hoje varre apenas
    WORK_UNITS_VALUES = [100000, 200000, 500000].
  - metrics_S1..S4_atk{1,5,10}pct.csv e os attack_summary_log_S{1-4}_atk*pct
    correspondentes (24 arquivos): saída de run_experiments_combined.py de
    quando o script ainda usava um único ATTACK_WORK_UNITS=100000 fixo. A
    investigação de WU (ver seção 21) mostrou que esse valor não força
    scale-up e não representa um ataque W-EDoS bem-sucedido, então esses
    resultados foram descartados em vez de mantidos como baseline.

Mantido:
  - metrics_normal_rps*_WU*.csv (16 arquivos, baseline de tráfego normal,
    eixo independente da decisão de WU do ataque).
  - metrics_rps{1,5,10}_att4_WU{100000,200000,500000}.csv e respectivos
    attack_summary_log (18 arquivos, batem com o run_experiments.py atual).

Por quê:
  - O diretório estava confuso, misturando resultados de configurações já
    abandonadas com os atuais, dificultando saber quais dados realmente
    valem para o dataset final da tese.

--------------------------------------------------------------------------------
21) run_experiments.py — salvar rtt_log.csv por execução da varredura
--------------------------------------------------------------------------------
Problema encontrado:
  normal_traffic.save_rtt_log() sobrescreve rtt_log.csv (modo "w") ao final
  de cada execução do main_orchestrator.py, mas run_experiments.py nunca
  movia esse arquivo para experiment_results/ como já fazia com
  simulation_metrics.csv e attack_summary_log.csv. Resultado: só o rtt_log
  da última iteração da varredura sobrevivia, o de todas as anteriores era
  perdido silenciosamente.

Alterado:
  - Após cada iteração (mesmo bloco que já move os outros dois logs),
    rtt_log.csv passou a ser movido para
    experiment_results/rtt_log_rps{rps}_att{attackers}_WU{work_units}.csv,
    seguindo a mesma convenção de nome já usada para o metrics_*.csv
    correspondente.

Por quê:
  - rtt_log.csv contém as amostras de RTT (tráfego normal + ataque) que o
    EntCusumZV3.py usa para detectar bursts — sem preservar um arquivo por
    combinação de RPS/attackers/WORK_UNITS, não dava para rodar a detecção
    de bursts em cada cenário da varredura, só no último.

--------------------------------------------------------------------------------
22) EntCusumZV3.py + run_burst_analysis.py (novo) — detecção de bursts em lote
--------------------------------------------------------------------------------
Alterado (EntCusumZV3.py):
  - analisar_bursts_tunavel() ganhou os parâmetros input_file (antes fixo em
    'rtt_log.csv'), output_prefix e show_plot. Os nomes de saída
    (rtt_bursts_*.xlsx / rtt_todos_bursts_*.png) agora são derivados do nome
    do arquivo de entrada e escritos no mesmo diretório dele.
  - Quando chamado sem argumentos (uso manual, python EntCusumZV3.py), o
    comportamento é idêntico ao de antes: lê rtt_log.csv e grava
    rtt_bursts_tunaveis.xlsx / rtt_todos_bursts.png no diretório atual —
    isso é tratado como caso especial, não só "sem sufixo", para não mudar
    o uso interativo já conhecido.
  - plt.show() agora é condicional a show_plot (True por padrão, False no
    uso em lote) e a figura é fechada com plt.close(fig) depois de salva,
    para não acumular janelas/figuras abertas ao processar vários arquivos
    em sequência.

Criado (run_burst_analysis.py):
  - Script novo que varre experiment_results/rtt_log_*.csv (produzidos pela
    mudança da seção 21) e chama analisar_bursts_tunavel() uma vez para
    cada um, com show_plot=False.

Por quê:
  - O pedido era rodar a detecção de bursts em cada rtt_log.csv salvo por
    execução da varredura, não só no último rtt_log.csv solto na raiz do
    projeto. Parametrizar a função em vez de duplicar a lógica de detecção
    evita ter duas implementações do mesmo cálculo (uma para uso manual,
    outra para lote) que poderiam divergir com o tempo.

Verificado:
  - python3 -m py_compile em run_experiments.py, EntCusumZV3.py e
    run_burst_analysis.py, sem erros de sintaxe. Não executado de ponta a
    ponta ainda (precisa dos containers Docker e de sudo para
    main_orchestrator.py); a varredura real fica para quando o usuário
    rodar run_experiments.py em seguida.

--------------------------------------------------------------------------------
23) run_experiments_combined.py — reenquadrar a varredura em WU=300000/500000
--------------------------------------------------------------------------------
Contexto:
  A varredura de calibração de WORK_UNITS (run_experiments.py, ver seção 20)
  mostrou que só WU=300000 (scale-up limítrofe) e WU=500000 (scale-up claro,
  repetido) de fato forçam SCALE_UP mantendo o RPS baixo/stealth — ou seja,
  são os únicos que constituem um ataque W-EDoS bem-sucedido pela definição
  do artigo (custo forçado sem volume que pareça DDoS). WU=100000, usado
  fixo aqui antes, não força scale-up: é estatisticamente detectável via RTT
  em alguns casos, mas não atinge o objetivo econômico do ataque.

Alterado:
  - ATTACK_WORK_UNITS (valor único, 100000) virou
    ATTACK_WORK_UNITS_LIST = [300000, 500000].
  - Novo laço interno sobre ATTACK_WORK_UNITS_LIST (cenário x intensidade x
    WU), então o total de execuções passa de 12 para 24 (4 cenários x 3
    intensidades x 2 valores de WU), ~72+ min de execução real além do
    overhead do Docker.
  - Nomes de saída passaram a incluir o WU, para não sobrescrever entre os
    dois valores: metrics_{cenário}_atk{pct}pct_wu{work_units}.csv e
    attack_summary_log_{cenário}_atk{pct}pct_wu{work_units}.csv (antes não
    tinham WU no nome, porque só existia um valor fixo).
  - Linha de progresso no console passou a mostrar o WU também.

Por quê:
  - Os resultados anteriores desta varredura (S1-S4 x 1/5/10%, todos com
    WU=100000 fixo) foram removidos na limpeza da seção 20 justamente por
    representarem um ataque que não força scale-up — ou seja, não é o "bom"
    exemplo de W-EDoS bem-sucedido que este experimento deveria caracterizar.
    Esta mudança troca o valor fixo pelos dois valores que de fato produzem
    esse resultado.

Verificado:
  - python3 -m py_compile run_experiments_combined.py, sem erros de sintaxe.
    Não executado de ponta a ponta ainda (precisa de Docker + sudo); fica
    para quando o usuário rodar a varredura de verdade.

--------------------------------------------------------------------------------
24) run_experiments_combined.py — salvar rtt_log.csv por execução também aqui
--------------------------------------------------------------------------------
Alterado:
  - Mesmo tratamento da seção 21 (run_experiments.py), aplicado aqui: após
    cada iteração (cenário x intensidade x WU), rtt_log.csv passa a ser
    movido para
    experiment_results/rtt_log_{cenário}_atk{pct}pct_wu{work_units}.csv,
    seguindo a mesma convenção de nome já usada para o metrics_*.csv
    correspondente.

Por quê:
  - Fechava a lacuna apontada na nota da seção 23: sem isso, run_burst_
    analysis.py só enxergava os rtt_log_*.csv da varredura de calibração
    (run_experiments.py), não os dos cenários combinados S1-S4 x WU=
    300000/500000 — que são justamente os cenários mais próximos do
    experimento final (Tabela 5) a caracterizar estatisticamente.

Verificado:
  - python3 -m py_compile run_experiments_combined.py, sem erros de sintaxe.
    Não executado de ponta a ponta ainda; fica para a regeneração completa
    da varredura que o usuário vai rodar em seguida.

--------------------------------------------------------------------------------
25) experiment_results/ — limpeza completa para regeneração do zero
--------------------------------------------------------------------------------
Removido (sudo rm -rf experiment_results/* pelo usuário, 64 arquivos):
  - Todo o conteúdo restante do diretório, incluindo os conjuntos até então
    mantidos na limpeza da seção 20 (baseline de tráfego normal e a
    varredura de calibração WU=100000/200000/500000).

Por quê:
  - Com o fix de keep-alive (seção 19), o reenquadramento de WU (seção 23)
    e o salvamento de rtt_log.csv por execução (seções 21 e 24) todos já
    aplicados, os resultados antigos ficaram desatualizados em relação ao
    código atual de qualquer forma — o usuário optou por regenerar tudo do
    zero em vez de manter dados parcialmente obsoletos misturados com os
    novos.
  - O diretório estava root:root (de execuções antigas com sudo), o que já
    tinha bloqueado tentativas anteriores de limpeza automática; o comando
    também corrigiu o dono para bibie:bibie, evitando o mesmo bloqueio nas
    próximas varreduras.

Verificado:
  - experiment_results/ confirmado vazio (0 arquivos) e com dono bibie:
    bibie após o comando. As 64 remoções aparecem staged no git (git status
    --short mostra "D" para cada arquivo), recuperáveis via histórico do
    git se necessário.

Próximo passo (ainda não executado): rodar, em ordem, run_experiments_
normal.py, run_experiments.py, run_experiments_combined.py e run_burst_
analysis.py para regenerar experiment_results/ do zero com o código atual.

--------------------------------------------------------------------------------
26) EntCusumZV3.py — piso de burst adaptativo por execução (em vez de fixo)
--------------------------------------------------------------------------------
Problema encontrado:
  min_rtt_burst=50 (piso fixo em ms, igual para todo cenário) fazia o
  detector ignorar ataques de baixa intensidade mas reais: rps1/WU500000
  (0 bursts detectados) força scale-up sustentado e RTT bem acima do seu
  próprio baseline (15ms -> 30-44ms), mas nunca cruza 50ms em termos
  absolutos. O RTT "normal" varia com RPS/WU (15-64ms observado entre
  cenários da varredura de baseline), então nenhum valor fixo separa bem
  ataque de tráfego normal em todos os cenários ao mesmo tempo.

  Tentativa descartada antes de implementar: usar média + k*desvio-padrão
  do baseline (as amostras antes do ataque começar). Testado com dados
  reais: o desvio-padrão por amostra bruta é alto (~19ms) mesmo em
  repouso (jitter de rede/agendamento), e só há ~20s de dado limpo antes
  do ataque (ATTACK_START_TIME_SECONDS=20) — uma única janela, amostra
  pequena demais pra estimar desvio-padrão de janela com confiança.
  média+3*desvio chegou a ~72-76ms, PIOR que o piso fixo de 50ms antigo.

Alterado:
  - Parâmetro min_rtt_burst (fixo, 50) substituído por baseline_ratio
    (tunável, padrão 1.8). O piso de burst passa a ser
    baseline_mean * baseline_ratio, onde baseline_mean é a média do RTT
    de CADA execução, calculada só com as amostras de antes do ataque
    começar (df['tempo_rel'] < config.ATTACK_START_TIME_SECONDS). Usa
    razão sobre a média (não média+desvio) porque só precisa de uma
    média estável, que já é confiável com ~750-900 amostras brutas em
    20s, ao contrário do desvio-padrão de janela.
  - Fallback: se houver menos de 10 amostras antes do ataque (config
    atípica), usa o arquivo inteiro como baseline e imprime aviso.
  - CUSUM (kappa, h, e a referência usada em cada incremento) passou a
    usar essa mesma baseline pré-ataque em vez de global_mean/global_std
    calculados sobre o arquivo INTEIRO. Corrige de quebra uma
    inconsistência já documentada no CLAUDE.md: antes, o período de
    ataque contaminava a própria referência de "normal" que o CUSUM usava
    pra se comparar.
  - Baseline/piso calculados são impressos no início de cada análise
    (média, desvio, nº de amostras, piso resultante), e o piso também
    aparece como linha horizontal tracejada no gráfico e no título.

Por quê:
  - Faz o detector reagir ao que É normal PARA AQUELA execução específica,
    em vez de assumir que RTT "normal" é sempre parecido entre cenários
    com RPS/WU bem diferentes — o que os próprios dados de baseline já
    tinham desmentido.

Verificado:
  - Reexecutado run_burst_analysis.py sobre os 9 rtt_log_*.csv reais da
    varredura de calibração (não só um protótipo isolado). Resultado
    bursts detectados, piso fixo (50ms) -> piso novo (1.8x baseline):
      rps1/WU100000:  0 -> 0   (CPU nunca subiu de verdade — sem falso positivo)
      rps1/WU200000:  0 -> 0   (idem)
      rps1/WU500000:  0 -> 14  (caso "limpo" que antes passava batido)
      rps5/WU100000:  0 -> 0   (idem, sem falso positivo)
      rps5/WU200000:  2 -> 14  (estava sub-detectado)
      rps5/WU500000: 16 -> 16  (já óbvio antes, sem mudança)
      rps10/WU100000: 0 -> 13  (outro caso que passava batido)
      rps10/WU200000:15 -> 15  (já óbvio antes, sem mudança)
      rps10/WU500000: 6 -> 6   (já óbvio antes, sem mudança)
    Os três cenários onde a CPU nunca chegou a subir de verdade continuam
    em 0 bursts com o piso novo — a mudança pega os casos reais que
    faltavam sem introduzir falso positivo nos casos claramente sem
    ataque efetivo, pelo menos nesta bateria de 9 arquivos.
  - python3 -m py_compile EntCusumZV3.py, sem erros.

--------------------------------------------------------------------------------
27) run_experiments_combined.py + EntCusumZV3.py — sweep real e achado de diluição
--------------------------------------------------------------------------------
Contexto:
  Rodado o sweep combinado real (S1-S4 x 1/5/10% x WU=300000/500000, 24
  execuções) e run_burst_analysis.py sobre os 24 rtt_log_* resultantes.
  Tendência geral do sweep ficou como esperado: detecção mais forte conforme
  cenário (S1->S4), intensidade (1%->10%) e WU aumentam. Todas as execuções
  em 1% de intensidade não escalaram e não geraram burst; nenhum erro de
  requisição apareceu em nenhum attack_summary_log deste sweep (volumes de
  ataque bem menores que os do sweep de calibração da seção 20/23).

Achado (não é bug, não requer mudança de código):
  S1_atk5pct_wu500000 é a única exceção real à tendência: 2 SCALE_UP de
  verdade ocorreram (CPU pico 81%, RTT médio de ATAQUE 92-208ms nas linhas
  rotuladas 'attack' do metrics_*.csv), mas run_burst_analysis.py reportou
  0 bursts nesse arquivo, mesmo com Z-score e CUSUM disparando quase toda
  janela ('Sim' na maioria das linhas). Investigado: a média de RTT POR
  JANELA nunca passou de ~34ms (o piso adaptativo da seção 26 é 34.1ms
  aqui) — não por o ataque ser fraco, mas porque rtt_log.csv mistura TODAS
  as requisições completadas (normais + ataque) sem rótulo. Em 5% de
  intensidade sobre S1 (50 req/s normal), o ataque agrega só ~2.5 req/s —
  proporção ~20:1 normal:ataque em cada janela de 20s. Uma janela ~95%
  normal (~18ms) + ~5% ataque (~150ms) calcula pra
  0.95*18 + 0.05*150 ≈ 24.6ms, perto do que foi observado — a média da
  janela dilui o sinal do ataque para dentro do ruído da maioria normal,
  independente de onde o piso (RTT ou desvio) esteja ajustado.

Por quê isso importa (sem ser um problema a corrigir):
  - É exatamente o comportamento que a definição de W-EDoS "furtivo"
    (Sotelo Monge et al.) prevê: um ataque que mantém baixo volume
    relativo ao tráfego normal é, por construção, difícil de enxergar com
    estatística agregada simples — não é uma falha do detector, é a
    própria dificuldade que motiva usar ML (o objetivo final do projeto,
    ver project_edos_purpose) em vez de uma regra de limiar fixo. Vale
    citar como limitação da linha de base estatística simples (entropia/
    Z/CUSUM sobre média de RTT) na discussão/limitações da monografia,
    não como algo a "consertar" — não dá pra separar ataque de normal
    dentro de rtt_log.csv sem usar informação que um detector real
    também não teria (rótulo por requisição).

Verificado:
  - Conferido manualmente: reexecutado analisar_bursts_tunavel() sobre
    experiment_results/rtt_log_S1_atk5pct_wu500000.csv isoladamente,
    tabela janela-a-janela confirma média de RTT sempre <= 34.0ms mesmo
    com Z e CUSUM alarmando na maioria das janelas.
  - Comparado numericamente com metrics_S1_atk5pct_wu500000.csv (RTT só
    de ataque, calculado pelo orquestrador): 92-208ms durante o ataque,
    bem acima da média diluída vista em rtt_log.csv — confirma que o
    ataque em si não é fraco, é a mistura normal+ataque no arquivo que
    mascara o sinal.

--------------------------------------------------------------------------------
28) experiment_results/ — organizado em subpastas por varredura
--------------------------------------------------------------------------------
Contexto:
  experiment_results/ tinha os arquivos das três varreduras (baseline
  normal, calibração de WU, sweep combinado S1-S4) todos soltos juntos no
  mesmo diretório, misturados — difícil de navegar e de separar o que é
  relevante pra apresentar.

Alterado:
  - Criadas três subpastas e movidos os arquivos existentes pra elas:
      experiment_results/normal_baseline/  (16 arquivos, de run_experiments_normal.py)
      experiment_results/wu_calibration/   (45 arquivos, de run_experiments.py + run_burst_analysis.py)
      experiment_results/combined_sweep/   (120 arquivos, de run_experiments_combined.py + run_burst_analysis.py)
  - run_experiments_normal.py, run_experiments.py, run_experiments_
    combined.py: RESULTS_DIR de cada um agora aponta direto pra sua
    subpasta correspondente, então as próximas execuções já salvam
    organizado, sem precisar mover nada manualmente depois.
  - run_burst_analysis.py: glob de rtt_log_*.csv passou a ser recursivo
    (glob.glob(..., "**", "rtt_log_*.csv"), recursive=True) pra continuar
    encontrando os arquivos dentro das subpastas. Saída (.xlsx/.png) de
    cada análise continua indo pro mesmo diretório do rtt_log_*.csv de
    entrada (lógica já existente, não mudou) — como as subpastas agora
    separam por varredura, isso já mantém cada subpasta autocontida
    (dado bruto + análise de burst juntos).

Por quê:
  - Pedido do usuário: organizar os dados coletados por varredura antes de
    selecionar o que é relevante pra uma apresentação ao orientador.

Verificado:
  - Nenhum arquivo perdido: contagem em cada subpasta bate com o total
    esperado (16 / 45 / 120), e "find experiment_results -maxdepth 1
    -type f" não retornou nada solto na raiz.
  - python3 -m py_compile nos quatro scripts alterados, sem erros.
  - Reexecutado run_burst_analysis.py de ponta a ponta após a
    reorganização: encontrou os 33 rtt_log_*.csv dentro das subpastas via
    glob recursivo, salvou as saídas no lugar certo, e os 33 números de
    bursts detectados bateram exatamente com os valores de antes da
    reorganização (nenhuma mudança de comportamento, só de local).

--------------------------------------------------------------------------------
29) generate_presentation_graphs.py (novo) — gráficos para a apresentação
--------------------------------------------------------------------------------
Adicionado:
  Script novo que lê os dados já salvos em experiment_results/combined_sweep/
  (varredura combinada) e nas saídas já calculadas por EntCusumZV3.py, e
  gera 3 PNGs em graficos_apresentacao/:
    01_tabela5_heatmap.png — heatmap 4 (cenário) x 3 (intensidade), um
      painel por custo de ataque (WU 300000/500000), célula = nº de
      janelas classificadas como burst.
    02_deteccao_S2_atk10pct_wu500000.png — painel com 6 gráficos
      empilhados (mesmo eixo do tempo) para uma execução real: RTT médio
      por janela, CPU média, instâncias ativas, entropia de Shannon,
      Z-score e CUSUM, com a janela de ataque sombreada e as janelas
      sinalizadas como burst marcadas em vermelho nos 4 gráficos
      estatísticos.
    03_resumo_bursts_por_cenario.png — gráfico de barras resumindo bursts
      detectados por cenário/intensidade/WU.
  Não roda nenhuma simulação nem reimplementa a lógica de detecção — só
  lê os CSVs/XLSX já gerados.

Por quê:
  - Pedido do usuário: gerar gráficos (equivalente à Tabela 5, entropia de
    Shannon, Z-score, CUSUM) para apresentar ao orientador que o
    detector está funcionando.
  - CPU (%) e nº de instâncias ativas foram deixados em dois gráficos
    separados (não sobrepostos com dois eixos Y) porque são grandezas de
    escalas diferentes — um gráfico com dois eixos Y é enganoso (não dá
    pra comparar as curvas de verdade) e foi evitado de propósito.

Verificado:
  - python3 -m py_compile generate_presentation_graphs.py, sem erros.
  - Rodado de ponta a ponta (python3 generate_presentation_graphs.py),
    gerou os 3 PNGs sem erro.
  - Inspecionados visualmente os 3 PNGs gerados: heatmap com números e
    escala de cor legíveis, sem sobreposição de texto/título; gráfico de
    barras sem sobreposição; painel de 6 gráficos com a janela de ataque
    (20-160s) e as janelas de burst corretamente alinhadas visualmente
    entre RTT/CPU/instâncias/entropia/Z-score/CUSUM.

--------------------------------------------------------------------------------
30) run_experiments_combined.py — recalibrar NORMAL_SCENARIOS (S1-S4) para este simulador
--------------------------------------------------------------------------------
Contexto:
  NORMAL_SCENARIOS usava os valores de RPS agregado da Tabela 4 de Sotelo
  Monge et al. (50/60/70/80) direto, sem ajuste. O testbed do artigo é
  maior/mais complexo que este simulador, então esses números não
  representam a mesma coisa aqui — o que importa da Tabela 4 é a ideia de
  uma progressão de carga (leve até perto do limiar de scale-up), não os
  valores absolutos em si. Conferindo contra o baseline já medido deste
  simulador (run_experiments_normal.py, WU=10), os quatro valores antigos
  (50-80) caem todos dentro da MESMA faixa "bem leve" (entre os pontos já
  medidos de 40 e 100 agregado) — não representam uma progressão real de
  proximidade ao limiar neste sistema.

Medição nova:
  Rodado main_orchestrator.py --normal-rps 75 --normal-work-units 10
  --attack-duration 0 --duration 180 (equivalente a um RPS agregado de 300,
  já que --normal-rps é por cliente e há 4 clientes normais fixos) — um
  ponto entre os 200 (estável) e 400 (instável) agregado já medidos
  anteriormente, faltando no baseline original.
  Resultado: pico de CPU 53.6%, média 41.1%, RTT médio 38.6ms, 36/36
  janelas NO_ACTION, nunca saiu de 1 instância — ainda estável, mas bem
  mais perto do limiar de 60% que os pontos anteriores. Salvo em
  experiment_results/normal_baseline/metrics_normal_rps75_WU10.csv.
  Isso restringe a fronteira de instabilidade (neste simulador, WU=10) para
  entre 300 e 400 agregado, mais apertada do que a estimativa anterior
  (entre 200 e 400).

Alterado:
  - NORMAL_SCENARIOS: {"S1":50,"S2":60,"S3":70,"S4":80} ->
    {"S1":40,"S2":100,"S3":200,"S4":300} — os quatro pontos agora vêm de
    medições reais deste simulador (40/100/200 já estavam no baseline
    original, 300 é a medição nova acima), cobrindo uma progressão real de
    ~8% a ~53,6% de CPU, todos confirmadamente sem escalar sozinhos.
  - Comentário do arquivo reescrito pra explicar a recalibração (por que os
    valores da Tabela 4 foram trocados, e com base em que dado).

Por quê:
  - Preserva o ESPÍRITO da Tabela 4 (cenários de carga leve até perto do
    limiar, nenhum escalando sozinho) usando pontos calibrados para a
    curva de capacidade real deste simulador, em vez de números emprestados
    de um testbed diferente que aqui caem todos no mesmo patamar "sem
    efeito".

Nota:
  - Os 24 resultados já coletados em experiment_results/combined_sweep/
    (seções 23/24/27) foram gerados com os valores ANTIGOS de
    NORMAL_SCENARIOS (50-80) e ficam desatualizados a partir desta mudança
    — precisam ser regerados (rodar run_experiments_combined.py de novo)
    antes de serem usados como resultado final. Os gráficos de apresentação
    (seção 29) também precisarão ser regenerados depois disso.

Verificado:
  - python3 -m py_compile run_experiments_combined.py, sem erros.
  - Não executado de ponta a ponta ainda (precisa de Docker + sudo); fica
    para quando o usuário rodar a varredura combinada novamente.

--------------------------------------------------------------------------------
31) Auditoria completa do simulador (pedido do usuário antes de partir pro
    dataset de ML) — 3 bugs reais encontrados e corrigidos
--------------------------------------------------------------------------------
Contexto:
  Releitura completa do código (main_orchestrator.py, config.py,
  stats_collector.py, docker_manager.py, tcpdump_sniffer.py,
  attack_summary_logger.py, cost_calculator.py, app/simple_server.py,
  traffic_injectorV0.py, normal_traffic.py) e verificação estrutural dos 50
  runs já coletados (normal_baseline/wu_calibration/combined_sweep), a
  pedido do usuário, para garantir que o simulador está correto ANTES de
  começar a gerar o dataset real de ML. A maior parte do código já estava
  correta (lógica de escalonamento, geração de tráfego, medição de CPU —
  nenhum problema novo encontrado ali). Três bugs reais encontrados:

Bug 1 — main_orchestrator.py: RTT "vazando" entre fases:
  Problema: durante a janela de ataque, o loop principal só lê
  get_average_rtt_attack_ms() (RTT só do ataque); get_average_rtt_ms()
  (RTT do tráfego normal) só é chamado nas iterações rotuladas 'normal'.
  Só que o tráfego normal nunca para durante o ataque (roda a simulação
  inteira por design) — então normal_traffic.rtt_measurements fica
  acumulando SEM SER DRENADO durante toda a janela de ataque (até ~140s).
  Na primeira iteração depois que o ataque termina, get_average_rtt_ms()
  finalmente é chamado de novo e despeja a média de TODAS essas amostras
  acumuladas numa linha só — que deveria representar um snapshot de 5s como
  qualquer outra linha, mas na prática é uma média sobre um período de até
  ~140s.
  Confirmado nos dados reais: em metrics_S2_atk10pct_wu500000.csv, a
  primeira linha 'normal' depois do ataque mostra avg_rtt_ms=73.1, contra
  ~26-27ms nas linhas logo antes e logo depois. Levantamento em todas as 33
  execuções com ataque (wu_calibration + combined_sweep): distorção de
  0.9x a ~30x (ou indefinida, nos casos de falha total de requisição já
  documentados na seção 27), afetando exatamente 1 linha por execução (33
  de ~1750 linhas no total, ~2% — mas justamente a linha da transição de
  fase, um ponto de alto valor informativo pra um futuro modelo de ML).
  Correção: get_average_rtt_ms() agora é chamado TODA iteração,
  incondicionalmente (pra sempre drenar a lista), e o valor só É USADO
  quando a fase não é de ataque — em vez de só ser chamado condicionalmente.

Bug 2 — traffic_capture.csv (captura de pacotes do tcpdump) nunca era
  salvo por execução:
  Problema: nenhum dos três scripts de varredura (run_experiments.py,
  run_experiments_normal.py, run_experiments_combined.py) movia/renomeava
  traffic_capture.csv como já faziam com simulation_metrics.csv,
  attack_summary_log.csv e rtt_log.csv — mesmo problema que rtt_log.csv
  tinha antes da correção da seção 21/24, nunca corrigido pra este arquivo.
  Resultado: dos 50 runs já coletados, só o ÚLTIMO tem traffic_capture.csv
  preservado — todos os outros 49 tiveram a captura de pacotes (o dado
  bruto rotulado por pacote, candidato a fonte de features mais rica que
  rtt_log.csv pro modelo de ML) sobrescrita e perdida.
  Correção: os três scripts agora movem traffic_capture.csv pro subdiretório
  de resultado correspondente, seguindo a mesma convenção de nome já usada
  pros outros logs (traffic_capture_rps{rps}_att{attackers}_WU{wu}.csv,
  traffic_capture_normal_rps{rps}_WU{wu}.csv,
  traffic_capture_{cenário}_atk{pct}pct_wu{wu}.csv).

Bug 3 — tcpdump_sniffer.py: parâmetro "interface" nunca era usado de
  verdade, e config.TCPDUMP_INTERFACE estava morto:
  Problema: TcpdumpSniffer.start() sempre rodava com "-i any" fixo no
  código, ignorando por completo o parâmetro `interface` recebido no
  construtor (e portanto ignorando config.TCPDUMP_INTERFACE =
  "br-4b3ba7f6ed06", um nome de bridge Docker específico de uma máquina,
  que muda se a rede for recriada). Na prática isso não chegou a
  contaminar dados: o traffic_capture.csv existente (do último run) só
  contém tráfego 127.0.0.1 nas portas esperadas (8080-8083 + portas
  efêmeras de cliente) — mas a captura era estruturalmente mais ampla que
  o necessário (toda interface do host, sem filtro de porta), o que é um
  risco real numa máquina de desenvolvimento geral (não uma VM isolada).
  Investigado ANTES de corrigir: tentar simplesmente "consertar" isso
  passando a interface da bridge Docker seria uma REGRESSÃO — o tráfego do
  simulador (cliente no host conectando em "http://localhost:<porta>")
  trafega pela interface de loopback (lo), não pela bridge Docker (que só
  vê o lado do container). Uma interface específica ficaria cega pra esse
  tráfego.
  Correção: mantido "-i any" (agora de propósito, documentado), mas o
  escopo da captura passou a ser restringido por um filtro BPF de portas
  (tcp and portrange STARTING_HOST_PORT..STARTING_HOST_PORT+MAX_INSTANCES-1)
  em vez de por interface — não depende de nomes de bridge gerados
  dinamicamente pelo Docker, e elimina o risco de capturar tráfego alheio
  do host. TCPDUMP_INTERFACE removido de config.py (estava morto, e
  "consertá-lo" teria sido a regressão acima). TcpdumpSniffer ganhou o
  parâmetro port_range; main_orchestrator.py passa interface="any" e
  port_range=(config.STARTING_HOST_PORT, config.STARTING_HOST_PORT +
  config.MAX_INSTANCES - 1) explicitamente.

Verificação estrutural dos 50 runs já coletados (sem mudança de código):
  - Nenhum arquivo faltando, nenhuma contagem de linha fora do esperado
    (34-38 por execução), nenhum NaN além do campo
    active_containers_names vazio (esperado) na primeira linha (t=0.01,
    antes da primeira leitura de stats) de cada execução.

Achado novo, ainda não investigado (fora do escopo desta correção):
  - attack_summary_log_S3_atk1pct_wu300000.csv mostra 491 erros de
    requisição mesmo com taxa de ataque planejada de só 2 req/s agregado —
    não bate com o padrão "volume alto sobrecarrega o servidor" visto em
    outros casos. Pode ser workers do injetor de ataque presos numa URL de
    alvo que ficou inválida após um SCALE_DOWN, mas não foi confirmado.
    Fica como item pra investigar depois.

Por quê:
  - Pedido explícito do usuário: garantir que o simulador está correto e
    os dados coletados são confiáveis ANTES de partir pra geração do
    dataset de ML — os bugs 1 e 2 em particular afetariam diretamente a
    qualidade de um dataset gerado a partir dos dados atuais (RTT
    distorcido numa linha por execução; e a fonte de dado mais rica pro ML
    praticamente ausente de 49 dos 50 runs já coletados).

Verificado:
  - python3 -m py_compile em main_orchestrator.py, config.py,
    tcpdump_sniffer.py, run_experiments.py, run_experiments_normal.py,
    run_experiments_combined.py — sem erros.
  - grep confirma zero referências restantes a config.TCPDUMP_INTERFACE
    fora do comentário que documenta a remoção.
  - Verificação de ponta a ponta CONFIRMADA (ver seção 33): rodado
    main_orchestrator.py de verdade com --attack-start 10 --pulse-duration
    15 --duration 40, os três fixes checados contra a saída real.

--------------------------------------------------------------------------------
32) apresentacao_professor.txt e CLAUDE.md — atualizados após a auditoria (seção 31)
--------------------------------------------------------------------------------
Contexto:
  Os dois documentos tinham ficado desatualizados em relação ao código e
  aos dados atuais — apresentacao_professor.txt foi escrito antes da
  recalibração de NORMAL_SCENARIOS (seção 30), e CLAUDE.md não era tocado
  desde bem antes desta sessão (ainda descrevia o ataque como pulsado
  estilo Yo-Yo, min_rtt_burst fixo em 50ms, CUSUM sobre o arquivo inteiro,
  EDOS_PULSE_*/EDOS_IDLE_*/TCPDUMP_INTERFACE como se ainda existissem em
  config.py, e rtt_log.csv como não sendo salvo por execução).

Alterado (apresentacao_professor.txt):
  - Item 3: adicionado item (f) resumindo a auditoria e os 3 bugs
    corrigidos na seção 31.
  - Item 4.3: corrigida a alegação "nenhuma das 24 execuções teve falha de
    requisição" (falsa depois da recalibração — S3/S4 em intensidade/custo
    altos têm falhas reais, até ~3800 erros em S4@10%/500mil) e adicionada
    uma nota sobre a recalibração de S1-S4 (40/100/200/300, não mais
    50/60/70/80 da Tabela 4).
  - Item 5, Achado 2: exemplo trocado de S1@5%/500mil (0 detecções antes
    da recalibração) para S2@1%/500mil (0 detecções, ainda válido nos
    dados atuais) — o exemplo antigo não reflete mais os dados depois da
    seção 30.
  - Item 5: adicionado Achado 3 (novo, desta auditoria) — o detector perde
    cobertura justamente no cenário mais grave (S4@10%/500mil) por causa
    das falhas reais de requisição lá, não porque o ataque enfraqueceu.
  - Item 6: mencionado que traffic_capture.csv agora é salvo por execução.
  - "28 itens" -> "31 itens" (contagem de changes.txt).

Alterado (CLAUDE.md):
  - Seção "Running the simulator": comandos atualizados (os três scripts
    de varredura e seus subdiretórios de saída, run_burst_analysis.py e
    generate_presentation_graphs.py adicionados).
  - Descrição do tráfego de ataque: de "pulsado, agendado por
    SCALE_COOLDOWN_SECONDS" pra "janela única, sem repetição" (o desenho
    Yo-Yo foi retirado há muito tempo, seção 13, mas o CLAUDE.md nunca
    tinha sido atualizado pra refletir isso).
  - Descrição do simple_server.py: de "ThreadingHTTPServer single-
    threaded-per-request" pra mencionar o ProcessPoolExecutor persistente
    e o keep-alive HTTP/1.1.
  - Descrição dos três scripts de varredura: subdiretórios de saída
    (normal_baseline/wu_calibration/combined_sweep), e corrigido "rtt_log.csv
    não é salvo por execução" pra refletir que rtt_log.csv E
    traffic_capture.csv agora SÃO salvos pelos três scripts.
  - Descrição do EntCusumZV3.py: piso fixo (50ms) e CUSUM sobre o arquivo
    inteiro substituídos pela descrição do piso adaptativo (seção 26);
    adicionada menção a run_burst_analysis.py; achados de diluição e perda
    de cobertura (seção 27) documentados como caveats conhecidos.
  - "Known inconsistencies": removida a entrada sobre EDOS_PULSE_*/
    EDOS_IDLE_*/EDOS_SATURATION_* (não existem mais em config.py, foram
    removidos de verdade na seção 13, não só "não usados" como a entrada
    antiga dizia). Adicionadas entradas sobre TCPDUMP_INTERFACE removido,
    attack_summary_log.csv só ter linhas de ataque, e o achado ainda não
    explicado do S3_atk1pct_wu300000 (ver seção 31).

Por quê:
  - Documentação desatualizada que afirma algo FALSO sobre o comportamento
    atual do código (ex: "ataque é pulsado", "nenhuma falha de requisição
    em 24 execuções") é pior que não ter documentação — engana quem lê
    depois, seja o orientador seja uma sessão futura do Claude Code neste
    projeto.

Verificado:
  - Releitura completa dos dois arquivos depois da edição, conferindo que
    nenhuma referência a EDOS_PULSE_*/TCPDUMP_INTERFACE/"pulsado" continua
    presente fora do contexto histórico (changes.txt, docs/*.md).
  - Números citados nos dois documentos (contagem de itens do changes.txt,
    valores de S1-S4, contagem de erros por cenário) conferidos contra os
    dados reais em experiment_results/combined_sweep/ antes de escrever,
    não copiados de memória.

--------------------------------------------------------------------------------
33) Verificação de ponta a ponta dos 3 fixes da seção 31 (confirmada)
--------------------------------------------------------------------------------
Execução real:
  sudo python3 main_orchestrator.py --rps 5 --attackers 4 --work-units
  300000 --attack-start 10 --pulse-duration 15 --duration 40
  (run curto: ataque de t=10 a t=25, 15s de tráfego só-normal depois, o
  suficiente pra observar a transição fim-de-ataque sem esperar os 180s
  padrão.)

Resultado — Fix 1 (RTT vazando entre fases):
  Linha de transição (t=30, primeira rotulada 'normal' depois do ataque)
  mostrou avg_rtt_ms=42.65 — elevado frente ao baseline (~18ms) mas um
  valor plausível de janela de 5s, não a distorção de até ~30x (ou
  indefinida) vista nos dados antigos antes da correção.

Resultado — Fix 2 (traffic_capture.csv por execução):
  traffic_capture.csv presente com 9312 linhas de dado real (562KB) —
  antes da correção esse arquivo simplesmente não seria salvo por nenhum
  dos scripts de varredura.

Resultado — Fix 3 (filtro de porta do tcpdump):
  0 dos 9312 pacotes capturados caem fora da faixa de porta alvo
  (8080-8083) tanto no src_port quanto no dst_port — confirmado via
  `awk -F, '!(($3>=8080 && $3<=8083) || ($5>=8080 && $5<=8083))'`. As
  portas altas observadas na coluna dst_port (ex.: 58818) são o lado
  efêmero do cliente nos pacotes de RESPOSTA do servidor (src_port=8080),
  não vazamento — captura bidirecional funcionando como esperado com
  "portrange 8080-8083".

Verificado:
  - Os 4 arquivos de saída (simulation_metrics.csv, rtt_log.csv,
    traffic_capture.csv, attack_summary_log.csv) apareceram na raiz do
    projeto como esperado (chamada direta a main_orchestrator.py, fora de
    um script de varredura) e foram removidos depois de conferidos (só
    lixo de verificação, não fazem parte do dataset real).

--------------------------------------------------------------------------------
34) run_experiments*.py — removido "sudo" fixo (obsoleto) e rtt_log.csv
    faltando em run_experiments_normal.py (2 bugs reais, corrigidos direto)
--------------------------------------------------------------------------------
Contexto:
  Início de uma nova sessão (12/08, tarde), caracterização exaustiva de
  W-EDoS. Usuário mudou a política de trabalho no meio da sessão: bugs reais
  encontrados agora são corrigidos direto, sem pedir permissão antes (antes
  disso, a sessão da madrugada 11→12/08 tinha ficado só no "relatar, não
  alterar" -- ver RESUMO_MADRUGADA_12_08.txt). Uma auditoria estrutural
  somente-leitura (wedos_structural_audit.py, novo script, Fase 0.2 do plano
  desta sessão) rodou sobre os 53 arquivos metrics_*.csv já coletados e
  achou o bug 2 abaixo; o bug 1 já estava documentado como item em aberto na
  seção 9 do RESUMO_MADRUGADA_12_08.txt.

Bug 1 — "sudo" fixo em run_experiments.py, run_experiments_normal.py e
  run_experiments_combined.py:
  Problema: os 3 scripts de varredura chamavam subprocess.run(["sudo",
  "python3", "main_orchestrator.py", ...]). Isso já não é necessário desde
  que o binário tcpdump ganhou a capability cap_net_raw,cap_net_admin via
  setcap (sessão da madrugada 11→12/08) e o usuário já está no grupo docker
  -- rodando de forma não interativa (como um script de varredura faz),
  "sudo" trava esperando senha num terminal que não existe, em vez de só
  ser redundante.
  Correção: removido "sudo" da lista de argumentos nos 3 scripts, com
  comentário explicando o porquê (evita que alguém "conserte" isso de
  volta sem saber da mudança do setcap).

Bug 2 — run_experiments_normal.py nunca salvava rtt_log.csv por cenário:
  Problema: mesmo bug estrutural das seções 21/24 (rtt_log.csv sobrescrito
  a cada execução do main_orchestrator.py), corrigido em run_experiments.py
  e run_experiments_combined.py na época, mas nunca replicado em
  run_experiments_normal.py -- só attack_summary_log.csv (descartado de
  propósito, sem dado útil sem ataque) e traffic_capture.csv eram tratados;
  rtt_log.csv ficava implicitamente perdido a cada iteração, sobrevivendo
  só o da última combinação RPS x WORK_UNITS da varredura. Confirmado: none
  dos 20 arquivos metrics_normal_*.csv já coletados em
  experiment_results/normal_baseline/ tem um rtt_log_normal_* correspondente.
  Correção: adicionado o mesmo bloco de shutil.move já usado nos outros
  dois scripts (rtt_log_normal_rps{rps}_WU{work_units}.csv), na mesma
  posição relativa (depois do attack_summary_log, antes do traffic_capture).

Por quê:
  Bug 1 bloquearia toda e qualquer execução real dos 3 scripts originais
  fora de um terminal interativo (exatamente por isso a sessão anterior
  criou os overnight_*.py como cópias sem sudo, em vez de consertar o
  original). Bug 2 é perda silenciosa de dado real (RTT do baseline normal,
  usado por EntCusumZV3.py) -- mesma classe de problema já corrigida nos
  outros dois scripts, só não tinha sido replicada aqui.

Verificado:
  - python3 -m py_compile nos 3 arquivos, sem erro de sintaxe.
  - grep confirma zero '"sudo"' como argumento de comando restante nos 3
    arquivos (só em comentários explicando a remoção).
  - Não executados de ponta a ponta ainda nesta sessão (os 3 scripts
    originais não fazem parte do plano desta caracterização, que usa os
    scripts novos wedos_*.py -- mas ficam corrigidos para uso futuro).

--------------------------------------------------------------------------------
35) normal_traffic.py/main_orchestrator.py — taxa de erro do tráfego normal
    era completamente invisível (achado real, corrigido: novo log)
--------------------------------------------------------------------------------
Contexto:
  Durante a Fase 0.3 (teste de reprodutibilidade, wedos_repro_test.py,
  candidato S4=300 agregado/WU=10) desta sessão, o console mostrou taxas de
  erro altas para os workers de tráfego NORMAL: rep1 ~16.3% (7307 erros em
  37481+7307 tentativas), rep2 ~26.7% (11019 erros em 30282+11019). RTT das
  requisições bem-sucedidas continuou baixo o tempo todo (~37-58ms, longe
  do timeout de 2s) -- ou seja, não é lentidão geral por saturação, parece
  falha binária (uma fração das requisições falha rápido, o resto passa
  normal).

Achado real: essa taxa de erro sempre existiu e nunca foi medida.
  attack_summary_log.csv só recebe linhas de workers de ATAQUE, por design
  (ver CLAUDE.md) -- normal_traffic.py conta request_count/error_count por
  worker (normal_http_request_worker) mas só IMPRIME no console
  ("[Normal_Injector Worker N] Stopped. Total requests: X, Errors: Y"),
  nunca grava em nenhum CSV. Ou seja: TODA execução já feita neste projeto
  (normal_baseline, combined_sweep, incluindo a recalibração de S1-S4 na
  seção 30) teve esses números só passando pelo console, nunca persistidos
  nem analisados -- inclusive as conclusões "36/36 janelas NO_ACTION" da
  seção 30 nunca verificaram taxa de erro do tráfego normal, só CPU/RTT de
  sucesso. Isso pode inclusive estar relacionado ao "mistério da
  reprodutibilidade" documentado em RESUMO_MADRUGADA_12_08.txt seção 3: se
  uma fração variável de requisições normais falha silenciosamente (nunca
  chega a ser processada), isso reduz a carga de CPU real aplicada de forma
  não determinística entre execuções -- possível causa raiz ainda não
  confirmada, fica para investigação com os dados novos (Fase 0/1 desta
  sessão, ver relatório final RELATORIO_WEDOS_*.txt).

  Nota lateral encontrada durante a investigação: normal_traffic.py e
  traffic_injectorV0.py usam timeout=2 (hardcoded) nas chamadas
  session.get(), e NÃO usam config.HTTP_REQUEST_TIMEOUT_SECONDS=10.0 (essa
  constante só é referenciada em traffic_injector.py, o módulo antigo já
  documentado como morto/não importado em CLAUDE.md) -- config "documenta"
  um timeout que o código vivo não usa. Não alterado ainda (mudar o timeout
  é uma decisão de metodologia experimental com impacto em todos os dados
  já coletados, não um bug óbvio de corrigir sozinho -- ver conversa com o
  usuário).

Correção aplicada (a parte inequívoca: dar visibilidade ao dado que já
  existia, sem mudar nenhum comportamento de rede):
  - Novo módulo normal_traffic_summary_logger.py, espelhando
    attack_summary_logger.py exatamente (mesma estrutura de linhas
    start/stop), mas escrevendo em
    config.NORMAL_TRAFFIC_SUMMARY_LOG_FILE = "normal_traffic_summary_log.csv"
    (arquivo novo e separado de attack_summary_log.csv, para não quebrar a
    convenção já documentada em CLAUDE.md de que aquele arquivo só recebe
    linhas de ataque).
  - normal_traffic.py: normal_http_request_worker agora captura
    worker_start_time e chama log_worker_stop() no fim (mesmo padrão de
    traffic_injectorV0.py); start_http_traffic() chama log_traffic_start()
    logo após o guard de "já em progresso" (não duplica linha nas chamadas
    subsequentes que só reiniciam o log de intenção, já que o tráfego
    normal não é reiniciado a cada scale como o de ataque).
  - main_orchestrator.py: chama
    normal_traffic_summary_logger.init_normal_traffic_summary_log() no
    mesmo bloco que já inicializa attack_summary_logger.
  - config.py: nova constante NORMAL_TRAFFIC_SUMMARY_LOG_FILE.
  - wedos_s3_mystery.py, wedos_combined_grid.py, wedos_failure_sweep.py,
    wedos_repro_test.py (scripts novos desta sessão): passaram a mover
    normal_traffic_summary_log.csv pro subdiretório de resultado
    correspondente, mesma convenção dos outros logs. run_experiments*.py
    (os 3 scripts originais) NÃO foram tocados por esta mudança específica
    -- ficam para quando/se o dataset final for regerado com eles.

Por quê:
  Dado real relevante (taxa de erro do tráfego "normal", usado como
  baseline de comparação em toda a tese) estava sendo descartado
  silenciosamente desde o início do projeto. Corrigir a visibilidade é uma
  mudança segura e inequívoca (só adiciona um log, não muda nenhum
  comportamento de envio de requisição); decidir SE o timeout de 2s
  hardcoded é "o problema" e se deve mudar é uma decisão de metodologia
  experimental, deixada para investigação com dado real antes de agir.

Verificado:
  - python3 -m py_compile em config.py, normal_traffic_summary_logger.py,
    normal_traffic.py, main_orchestrator.py, traffic_injectorV0.py e nos 4
    scripts wedos_*.py listados acima -- sem erro de sintaxe.
  - Ainda não verificado em execução real com o código novo (a
    verificação de ponta a ponta acontece nas próprias execuções da Fase 0/1
    desta sessão que já estavam em andamento quando esta correção foi
    aplicada -- os runs restantes do lote A não capturam esse log ainda,
    por já estarem com o processo Python pai em execução antes da correção;
    lote B e todos os runs daqui em diante capturam.

--------------------------------------------------------------------------------
36) regenerate_graficos_04_05_media.py (novo) — gráficos 04 e 05 com CPU MÉDIA
    em vez de CPU DE PICO
--------------------------------------------------------------------------------
Contexto:
  Discussão com o usuário sobre por que os gráficos 04
  (graficos_apresentacao/04_curva_capacidade_reproducibilidade.png) e 05
  (graficos_apresentacao/05_wu_calibration_resumo_atual.png) mostravam
  comportamento aparentemente anômalo: no 04, CPU "caindo" depois de 300
  RPS agregado; no 05, WU=200000 ultrapassando WU=500000 em rps=10, e
  WU=100000 ultrapassando WU=200000 em rps=1 — inversões contra-intuitivas
  (mais trabalho por requisição deveria sempre significar mais CPU, não
  menos). Investigação (não código, só leitura/análise dos CSVs já
  existentes) mostrou que ambos os gráficos usavam CPU DE PICO (máximo de
  uma única execução, ~28-36 amostras de 5s) como estatística de
  comparação — uma estatística ruim para esse fim: é dominada por outliers
  isolados de amostragem (ex.: rps=1/WU=100000 tinha uma única amostra de
  33.6% cercada de vizinhas em ~10-20%) e por transientes de escalonamento
  (ex.: WU=200000/rps=10 atingia seu pico de 239% durante uma janela de
  transição de SCALE_UP com só 2 instâncias ativas, não no regime estável).
  Recalculando com CPU MÉDIA (restrita às janelas do label relevante —
  'attack' para wu_calibration, para não diluir com o período de
  warmup/idle antes do tráfego estabilizar) as duas inversões do gráfico 05
  desaparecem e a ordem fica monotônica (mais WU → mais CPU média, como
  esperado fisicamente): rps=1: WU=100k 16.9% < WU=200k 23.9%; rps=10:
  WU=100k 61.1% < WU=200k 110.4% < WU=500k 192.9%. No gráfico 04, o desvio-
  padrão entre repetições no ponto de 300 agregado cai de 18.2pp (pico,
  seção anterior) para 8.8pp (média) — confirma que média é uma estatística
  bem mais estável entre execuções nominalmente idênticas.

Criado (não modifica nenhum script/gráfico existente):
  - regenerate_graficos_04_05_media.py: lê os CSVs já existentes em
    experiment_results/normal_baseline/ e experiment_results/wu_calibration/
    (nenhuma simulação nova rodada) e gera:
    - graficos_apresentacao/04_curva_capacidade_reproducibilidade_MEDIA.png
      — mesmos pontos de dados do gráfico 04 original (40/100/200/400
      agregado, 1 amostra cada; 250 agregado, 3 repetições), mas com CPU
      média em vez de pico. O ponto de 300 agregado passou a usar TODAS as
      repetições reais disponíveis para essa config (a reexecução salva em
      metrics_normal_rps75_WU10.csv + as 6 repetições de reprodutibilidade
      loteA/loteB de wedos_repro_test.py = 7 no total, média=41.1%,
      desvio=8.8pp) em vez de um único ponto — a 1ª amostra isolada
      original (pico 53.6%, mencionada no RESUMO_MADRUGADA_12_08.txt) foi
      sobrescrita em disco antes desta sessão e não pôde ser recuperada.
    - graficos_apresentacao/05_wu_calibration_resumo_MEDIA.png — painel
      esquerdo (CPU) trocado de pico para média das janelas com
      label=='attack'; painel direito (total de erros de requisição)
      mantido idêntico ao original, já era uma boa evidência de saturação
      antes desta mudança.
  - Os arquivos antigos (04_curva_capacidade_reproducibilidade.png e
    05_wu_calibration_resumo_atual.png) NÃO foram apagados — pedido
    explícito do usuário, para comparar lado a lado com as versões novas.

Por quê:
  Pico de uma única execução é estatisticamente frágil (sensível a outlier
  isolado e a transiente de escalonamento) para comparar configurações
  entre si; média é uma estatística mais robusta e reduz variância com o
  número de amostras, resolvendo as duas inversões contra-intuitivas dos
  gráficos sem precisar de nenhum dado novo. Pico continua tendo uso
  operacional (é o gatilho real do autoscaler, limiar de 60%), só não deve
  ser usado para ranquear/comparar intensidade entre cenários — combinado
  com o usuário que os próximos gráficos (grid clientes×RPS ainda a
  implementar) já nascem usando média±desvio-padrão como métrica primária.

Verificado:
  - python3 regenerate_graficos_04_05_media.py rodado de ponta a ponta, sem
    erro; os 2 PNGs novos foram gerados e inspecionados visualmente —
    curva 04 mostra um platô mais suave perto do teto (em vez de
    subida-queda), gráfico 05 mostra ordem monotônica em CPU média nos dois
    painéis esquerdos (rps=1 e rps=10) que antes tinham inversão.

--------------------------------------------------------------------------------
37) host_stats.py (novo) + main_orchestrator.py — coluna host_cpu_percent;
    run_graph4_refined.py + plot_graph4_refined.py (novos) — gráfico 04
    refinado com CPU do host + taxa de erro
--------------------------------------------------------------------------------
Contexto:
  Usuário pediu prova de que o teto de capacidade observado (seção 36 e
  discussão anterior) é saturação real do host/Docker, e que o gráfico 04
  passasse a mostrar erro/média de erro (como o painel direito do gráfico
  05), com estatística simples (média±desvio-padrão, sem testes de
  hipótese formais). Auditoria dos dados que alimentam o gráfico 04 mostrou
  que quase nenhum tem taxa de erro registrada: normal_traffic_summary_logger.py
  só existe desde a seção 35, e de ~16 arquivos por trás do gráfico 04 só 3
  (as repetições loteB do ponto de 300 agregado) têm esse log — o resto foi
  rodado antes da correção e a contagem de erro só foi pro console, perdida
  para sempre. Não dava pra "adicionar" erro a dado antigo; era preciso
  reexecutar.

Criado:
  - host_stats.py: lê /proc/stat (sem depender de psutil, que não está no
    ambiente) e calcula a % de CPU do HOST inteiro (todos os núcleos) desde
    a última chamada — complementa o CPU% por container que
    stats_collector.py já media. Padrão stateful (mesmo estilo de
    get_average_rtt_ms em normal_traffic.py): 1ª chamada de cada execução
    não tem leitura anterior pra comparar, retorna 0.0.
  - run_graph4_refined.py: reexecuta os 6 pontos do eixo X do gráfico 04
    (40/100/200/250/300/400 agregado, WU=10, sem ataque) com 5 repetições
    cada (30 execuções no total) usando main_orchestrator.py como está hoje
    (já grava taxa de erro desde a seção 35). Salva com sufixo
    "_refined_repN", nomes novos que não colidem com nenhum arquivo
    existente em experiment_results/normal_baseline/ — nada foi
    sobrescrito ou apagado.
  - plot_graph4_refined.py: lê os CSVs novos, calcula média±desvio-padrão
    (entre as 5 repetições) de CPU do container, CPU do host e taxa de
    erro por ponto de RPS agregado, e plota tudo num único gráfico com dois
    eixos Y (CPU à esquerda, taxa de erro à direita) — mais um coeficiente
    de correlação de Pearson simples (RPS agregado × taxa de erro; CPU do
    host × taxa de erro) impresso no console e no título, como pedido
    (estatística simples, não teste de hipótese). Salva em
    graficos_apresentacao/04_curva_capacidade_REFINADA.png, sem tocar nos
    dois gráficos 04 já existentes (original e a versão _MEDIA da seção 36).

Alterado:
  - main_orchestrator.py: importa host_stats; log_metrics_to_csv() ganhou o
    parâmetro/coluna host_cpu_percent (no cabeçalho do CSV escrito no
    início da execução e na função de log); a chamada real (dentro do laço
    principal) agora calcula host_stats.get_host_cpu_percent() a cada tick,
    mesmo cadenciamento das outras métricas. simulation_metrics.csv de toda
    execução daqui em diante ganha essa coluna nova; arquivos já coletados
    antes desta mudança simplesmente não a têm (scripts de análise leem por
    nome de coluna via pandas/DictReader, não By posição, então isso não
    quebra leitura de CSVs antigos).

Por quê:
  Nenhum container tem limite de CPU configurado em docker_manager.py (sem
  nano_cpus/cpu_quota/cpuset) — ou seja, todo processo pesado compete
  livremente pelos núcleos do host via o escalonador do SO. MAX_INSTANCES=4
  × INSTANCE_MAX_CONCURRENT_REQUESTS=2 = 8 processos concorrentes no
  máximo, e o host tem nproc=8 -- coincidência estrutural forte, mas só
  correlação numérica até medirmos a CPU do host de verdade durante a
  execução. host_cpu_percent é a evidência direta que faltava: se ela
  colar perto de 100% exatamente quando a taxa de erro dispara, é prova
  causal, não só estrutural.

Verificado:
  - python3 -m py_compile em host_stats.py, main_orchestrator.py,
    run_graph4_refined.py e plot_graph4_refined.py -- sem erro.
  - python3 host_stats.py (self-test, 5 amostras) rodado isoladamente --
    1ª amostra 0.0%, amostras seguintes na faixa 0.9-1.5% (host
    majoritariamente ocioso), como esperado.
  - Teste de ponta a ponta real: python3 main_orchestrator.py --normal-rps
    10 --normal-work-units 10 --attack-duration 0 --duration 15 (RPS
    agregado 40, WU=10, ~15s) rodado até o fim sem erro; simulation_metrics.csv
    resultante confirmado com a coluna host_cpu_percent populada
    corretamente (0.0 na 1ª linha, depois 6.19%/6.91%) e as 4 colunas
    anteriores intactas. Artefatos desse teste (simulation_metrics.csv,
    rtt_log.csv, traffic_capture.csv, normal_traffic_summary_log.csv,
    attack_summary_log.csv na raiz do projeto) apagados depois de
    conferidos -- eram só lixo de teste de 15s, não dado de experimento.
  - run_graph4_refined.py (30 execuções, ~2-2.5h) disparado em background
    após esse teste; resultado/gráfico final a conferir quando terminar.

--------------------------------------------------------------------------------
38) main_orchestrator.py — flag --normal-clients; run_wu400000_only.py,
    plot_graph05_pow2.py, run_experiments_clients_rps.py,
    plot_clients_rps_invariance.py, run_all_pipeline.py (novos) — pipeline
    automatizado completo (etapas 3-6)
--------------------------------------------------------------------------------
Contexto:
  Usuário pediu automação de ponta a ponta: WU=400000 (substituindo 500000
  como topo da progressão de WU, para seguir potência de 2 -- 100k/200k/400k),
  gráfico 05 refeito com esses valores, e o grid clientes×RPS (seção
  "Parte 2" combinada em conversa anterior) -- tudo encadeado numa esteira
  só, disparada automaticamente assim que a etapa 1 (seção 37,
  run_graph4_refined.py, já rodando em background) terminar sozinha.

Alterado:
  - main_orchestrator.py: novo argumento --normal-clients (mesmo padrão de
    --normal-rps), sobrescreve config.HTTP_NORMAL_NUM_CLIENTS (antes fixo
    em 4, sem exposição via CLI) — necessário para o grid clientes×RPS
    poder variar nº de clientes independente do RPS por cliente.

Criado:
  - run_wu400000_only.py: só WU=400000 (rps=1,5,10 × attackers=4), mesma
    convenção de nome de run_experiments.py -- não mexe nesse script nem
    nos dados já coletados de WU=100000/200000/500000.
  - plot_graph05_pow2.py: gráfico 05 (CPU média + erros) com
    WU={100000,200000,400000} em vez de {100000,200000,500000} -- salvo
    como 05_wu_calibration_resumo_POW2.png, não sobrescreve os gráficos 05
    anteriores (original e _MEDIA da seção 36).
  - run_experiments_clients_rps.py: grid clientes×RPS combinado
    anteriormente com o usuário -- agregados {16,32,64,128,256,512} ×
    clientes {1,2,4,8,16} × 5 repetições, WU=10 fixo, 150 execuções, salva
    em experiment_results/clients_rps_grid/ (pasta nova).
  - plot_clients_rps_invariance.py: lê o grid acima, calcula média±desvio-
    padrão de CPU do container, CPU do host e taxa de erro por
    (agregado, nº de clientes), plota em eixo X log2 (potências de 2) --
    testa visualmente se pontos do mesmo RPS agregado com decomposições
    diferentes coincidem. Estatística simples (média±desvio-padrão), sem
    teste de hipótese formal, como já combinado. Salva
    graficos_apresentacao/11_clients_rps_invariancia.png e um CSV-resumo
    em experiment_results/clients_rps_grid/resumo_clients_rps_grid.csv.
  - run_all_pipeline.py: encadeador mestre -- espera os 30 arquivos da
    etapa 1 (seção 37) aparecerem em experiment_results/normal_baseline/,
    depois roda em sequência: plot_graph4_refined.py → run_wu400000_only.py
    → plot_graph05_pow2.py → run_experiments_clients_rps.py →
    plot_clients_rps_invariance.py. Para o pipeline inteiro (sys.exit) se
    qualquer etapa retornar código de erro, em vez de seguir com dado
    incompleto. Loga "PIPELINE_COMPLETO" na última linha quando tudo
    termina.

Por quê:
  Pedido explícito do usuário: rodar tudo automaticamente (WU=400000, grid
  clientes×RPS) do menor tempo de execução para o maior, sem precisar de
  aprovação manual em cada etapa intermediária, com aviso só no final.

Verificado:
  - python3 -m py_compile em todos os arquivos novos/alterados desta seção
    -- sem erro.
  - --normal-clients testado indiretamente (mesmo padrão de --normal-rps,
    já validado em execução real na seção 35/37); não houve tempo de rodar
    um teste isolado antes de disparar o pipeline, mas a implementação é
    estruturalmente idêntica ao --normal-rps já comprovado funcional.
  - run_all_pipeline.py disparado em background (dessa vez com
    run_in_background nativo do harness, não nohup manual como na seção
    37 -- para garantir notificação automática de conclusão). Ainda não
    concluído no momento deste registro; resultado final a conferir.

--------------------------------------------------------------------------------
39) Pipeline (seção 38) concluído -- etapas 1-5 ok, bug corrigido na etapa 6
--------------------------------------------------------------------------------
Resultado das 150+3+30 execuções: todas concluídas sem erro (etapa 5, o
grid clientes×RPS de 150 execuções, levou ~7h45, taxa real ~3min05s/execução,
estável do início ao fim -- sem sinal de degradação de ambiente ao longo da
sessão longa, ao contrário do que a seção 3.2 do RESUMO_MADRUGADA_12_08.txt
havia levantado como hipótese para uma sessão anterior). run_all_pipeline.py
travou (exit 1) só na última etapa (plot_clients_rps_invariance.py), num bug
de verdade:

Bug encontrado:
  plot_clients_rps_invariance.py chamava statistics.stdev()/mean() direto
  em cima de error_rate_mean sem checar por NaN. 1 dos 150 runs
  (agg512_clients2_WU10_rep3, o canto mais extremo do grid: 2 clientes a
  256 rps cada) ficou com normal_traffic_summary_log.csv só com a linha de
  início, sem nenhuma linha de worker-stop -- as threads de tráfego normal
  (daemon=True) não conseguiram drenar o backlog de requisições pendentes e
  escrever o resumo antes do processo principal encerrar, então a taxa de
  erro daquele run específico não foi persistida (perdida, igual ao que
  já tinha acontecido com os dados antigos do gráfico 04 antes da seção
  35/37 -- mesma classe de problema, escala menor aqui). Isso não afeta a
  métrica de CPU (vem de simulation_metrics.csv, fonte separada, intacta
  para esse run) -- só a taxa de erro desse 1 run ficou NaN, e
  statistics.stdev() não sabe lidar com NaN (AttributeError, não um erro
  claro).

Corrigido:
  plot_clients_rps_invariance.py: filtra valores NaN de error_rate ANTES
  de calcular média/desvio (v == v é False só para NaN, sem precisar de
  import extra), imprime aviso quando descarta alguma repetição, e guarda
  quantas repetições válidas entraram no cálculo (coluna error_rate_n no
  CSV-resumo). CPU (container e host) não precisou desse tratamento -- não
  tinha NaN.

Executado manualmente (dado da etapa 5 já estava completo em disco, não
  precisou rodar as 150 execuções de novo): python3
  plot_clients_rps_invariance.py -- gerou
  graficos_apresentacao/11_clients_rps_invariancia.png e
  experiment_results/clients_rps_grid/resumo_clients_rps_grid.csv com
  sucesso.

Resultado (resumo, ver PNGs para o gráfico completo):
  - Gráfico 04 refinado: CPU média sobe de 7.7% (40 agregado) até um platô
    em torno de 44-52% entre 250-400 agregado (nenhum ponto passa o limiar
    de 60%) -- confirma a leitura de "platô perto do teto" da seção 36,
    agora com CPU do host também crescendo de forma consistente (5.5% ->
    30.7%) e taxa de erro desprezível na maioria dos pontos (exceto 250
    agregado, que teve 1 repetição com erro bem mais alto que as outras 4 --
    desvio grande, n=5, vale investigar isolado depois).
  - Gráfico 05 com WU em potência de 2 (100k/200k/400k): ordem
    monotônica confirmada em todos os RPS (ex.: rps=10: 61.1% -> 110.4% ->
    192.7%, erros 0 -> 1747 -> 5035) -- mesmo padrão da seção 36, agora sem
    o salto "esquisito" 200k->500k.
  - Grid clientes×RPS (hipótese "RPS agregado é o que importa"): CONFIRMADA
    no regime estável -- de 16 a 128 agregado, as 5 decomposições (1/2/4/8/16
    clientes) ficam praticamente coincidentes em CPU média (curvas
    sobrepostas no gráfico). A partir de 256 agregado (perto/acima do teto
    de ~205-210 já conhecido) as curvas começam a divergir, com destaque
    para "8 clientes" ficando consistentemente abaixo das outras E com taxa
    de erro média muito mais alta (~11-12%, desvio enorme ~25-26pp) que
    todas as outras decomposições (que ficam perto de 0% mesmo em 256/512)
    -- padrão específico de "8 clientes" ainda sem explicação, discutir com
    o usuário antes de tirar conclusão (não é o que a hipótese original
    previa: seria de esperar que TODAS as decomposições degradassem juntas
    perto do teto, não uma isolada).

Verificado:
  - python3 -m py_compile plot_clients_rps_invariance.py -- sem erro.
  - python3 plot_clients_rps_invariance.py rodado de ponta a ponta,
    terminou sem erro desta vez, PNG e CSV gerados e inspecionados.
  - Pipeline completo (etapas 1-6) considerado concluído neste ponto,
    mesmo com run_all_pipeline.py tendo saído com código 1 na primeira
    tentativa -- a etapa 6 foi completada manualmente fora do
    encadeador depois do fix, não precisou re-rodar nada anterior.

--------------------------------------------------------------------------------
40) Investigação da anomalia "8 clientes" (seção 39) -- causa raiz encontrada;
    run_extra_reps_clients8.py (novo) -- repetições extras pra confirmar
    se é soluço isolado ou padrão sistemático
--------------------------------------------------------------------------------
Causa raiz encontrada (não é um efeito real de "8 clientes"):
  Comparando o log de resumo por worker das 2 execuções ruins
  (agg256_clients8_rep2 e agg512_clients8_rep5) com uma execução normal do
  mesmo ponto: nas ruins, só 3/8 e 1/8 threads de tráfego normal
  conseguiram logar o fim (o resto -- threads daemon=True -- sumiu sem
  registro), as poucas que logaram levaram ~210s em vez dos ~180s do alvo
  (30s a mais), e tiveram 55-58% de taxa de erro contra 0% nas execuções
  saudáveis do mesmo ponto. Padrão consistente com um soluço transitório de
  ambiente NO MEIO da execução (rajada de timeouts reais, não um bug de
  lógica) -- mesma classe de anomalia já documentada neste projeto
  (changes.txt §31, wedos_s3_mystery.py) em vez de uma degradação real
  ligada ao número de clientes. São 2 runs de 150 puxando a média/desvio
  do grupo inteiro no gráfico.

Criado:
  - run_extra_reps_clients8.py: 5 repetições extras (rep6-rep10, não
    sobrescreve rep1-5) para os 2 pontos afetados (agg=256/clientes=8,
    agg=512/clientes=8) -- 10 execuções no total, para ver se o problema
    se repete.

Alterado:
  - plot_clients_rps_invariance.py: run_stats() agora também sinaliza
    "incompleto" quando o nº de linhas de worker-stop no log de resumo é
    menor que o nº de clientes configurados (mesmo sintoma das 2 execuções
    ruins) -- runs incompletos são EXCLUÍDOS do cálculo de
    média/desvio-padrão do ponto (não só da taxa de erro, como no fix
    anterior da seção 39), com aviso no console e contagem
    (n_incomplete_excluded) salva no CSV-resumo, em vez de silenciosamente
    diluir a média com dado de um run que não terminou de verdade.

Verificado:
  - python3 -m py_compile em run_extra_reps_clients8.py e
    plot_clients_rps_invariance.py -- sem erro.
  - run_extra_reps_clients8.py (10 execuções, ~35-40min) disparado em
    background (run_in_background nativo); resultado a conferir quando
    terminar, junto com o replot final do gráfico 11.

--------------------------------------------------------------------------------
41) Investigação do outlier de 250 agregado no gráfico 04 refinado -- causa
    raiz encontrada e corrigida em plot_graph4_refined.py;
    run_extra_reps_agg250.py (novo, na fila, não executado ainda)
--------------------------------------------------------------------------------
Causa raiz encontrada (mesma classe da seção 40, não é efeito real de 250
  agregado):
  Comparando as 5 repetições de agg=250: rep1 teve só 3/4 threads de
  tráfego normal logando o fim (correção: na mensagem ao usuário eu tinha
  dito "as 4 threads conseguiram logar", o que estava errado -- o script
  automatizado, ao checar de verdade, confirmou 3/4, o mesmo tipo de
  sintoma da seção 40, só que menos severo), com duração de ~190s (10s a
  mais que o alvo de 180s) e ~14% de taxa de erro combinada, contra ~0%
  nas outras 4 repetições (que ficaram nos ~180.3-180.6s esperados). A
  CPU dessa mesma execução (fonte separada, simulation_metrics.csv escrito
  pelo orquestrador) ficou normal -- sem pico, RTT nunca passou de 65ms --
  confirmando que o problema é isolado ao lado cliente/threads de log, não
  um efeito real de carga.

Corrigido:
  plot_graph4_refined.py: error_rate() virou error_rate_and_quality(),
  que sinaliza um run como "suspeito" se (a) menos linhas de worker-stop
  que clientes configurados, ou (b) duração de alguma thread > alvo +
  10s de tolerância -- mesma lógica de completude da seção 40, adaptada
  pra esse script. Diferença importante em relação ao fix da seção 40:
  aqui só a TAXA DE ERRO do run suspeito é excluída do cálculo do ponto
  -- a CPU (container e host) continua entrando normalmente, porque já
  foi verificada como não afetada nesse caso (fonte de dado separada).
  Print de aviso no console quando algum run é excluído, e contagem
  (n_error_suspect_excluded) salva no CSV-resumo novo
  (experiment_results/normal_baseline/resumo_graph4_refined.csv, também
  novo -- esse script não salvava CSV-resumo antes).

Resultado:
  Com rep1 excluída do cálculo de erro, o ponto de 250 agregado cai de
  error_rate_mean=3.6%/std=7.98 para error_rate_mean=0.027%/std=0.034 --
  em linha com todos os outros pontos do gráfico (que já estavam
  ~0-0.06%). O pico de erro gigante que dominava o eixo direito do
  gráfico 04 refinado desapareceu. graficos_apresentacao/04_curva_capacidade_REFINADA.png
  regenerado com o fix.

Criado (na fila, NÃO executado ainda -- combinado com o usuário rodar só
  depois que o lote da seção 40 (run_extra_reps_clients8.py) terminar, pra
  não ter 2 lotes de experimentos Docker competindo por recurso ao mesmo
  tempo e confundir o próprio fenômeno sendo investigado):
  - run_extra_reps_agg250.py: 3 repetições extras (rep6-rep8, não
    sobrescreve rep1-5) só do ponto de 250 agregado, mesmos parâmetros de
    run_graph4_refined.py.

Verificado:
  - python3 -m py_compile em plot_graph4_refined.py e
    run_extra_reps_agg250.py -- sem erro.
  - python3 plot_graph4_refined.py rodado de ponta a ponta (dado já
    existia em disco, não precisou Docker) -- aviso de exclusão apareceu
    corretamente no console, PNG e CSV-resumo novos gerados e
    inspecionados, confirmando o pico de erro sumiu do gráfico.
  - Confirmado via `ps aux` que run_extra_reps_agg250.py NÃO foi
    executado -- só criado e compilado, conforme combinado.

--------------------------------------------------------------------------------
42) run_extra_reps_clients8.py concluído -- anomalia REPRODUZ (não é soluço
    raro isolado); replot do gráfico 11 confirma hipótese depois de excluir
--------------------------------------------------------------------------------
Resultado das 10 execuções extras (rep6-rep10, agg=256 e agg=512,
  clientes=8): a anomalia se repetiu -- 3 das 10 novas execuções ficaram
  incompletas (mesmo sintoma da seção 40: menos linhas de worker-stop que
  clientes configurados, duração maior que o alvo, erro alto):
    agg=256 clientes=8 rep7: 0/8 threads logaram
    agg=256 clientes=8 rep10: 5/8 threads, ~207.5s, 58.8% erro
    agg=512 clientes=8 rep7: 1/8 threads, ~204.4s, 20.9% erro
  Combinando com as 2 já conhecidas da seção 39 (rep2 e rep5 originais),
  fica em 5 execuções ruins de 20 (25%) nesses 2 pontos específicos --
  bem acima da taxa vista no resto do grid original (só 1 caso em 148
  outras execuções, agg512_clients2_rep3). CONCLUSÃO: não é mais razoável
  chamar isso de soluço raro isolado (padrão do §31/wedos_s3_mystery) --
  reproduz com frequência real nesses pontos. Replotando o gráfico 11
  completo (todos os 160 arquivos agora, script já filtra os incompletos)
  também revelou um 3º ponto com o mesmo sintoma que não tinha sido notado
  antes: agg=16 clientes=16, 3 das 5 repetições ORIGINAIS incompletas.
  Juntando os 3 pontos afetados (256/8, 512/8, 16/16), o padrão comum não é
  o RPS agregado (um é leve, dois são pesados) -- é sempre um nº alto de
  threads de cliente (8 ou 16). Hipótese revisada: a causa provável está
  ligada a QUANTIDADE DE THREADS concorrentes fazendo stop/log ao mesmo
  tempo (mais chances de pelo menos uma não conseguir escrever a tempo),
  não ao nível de carga -- ainda não confirmada a nível de SO/rede, fica
  como próximo passo se o usuário quiser investigar mais fundo.

  Depois de excluir os runs incompletos (lógica já existente na seção 40,
  automática), a hipótese "RPS agregado é o que importa" se sustenta bem
  em TODO o grid, incluindo os pontos antes contaminados: agg=256
  clientes=8 (n=7 válidas) CPU média=47.2% -- praticamente igual às outras
  decomposições do mesmo agregado (46.1-48.0%); agg=512 clientes=8 (n=8
  válidas) CPU média=38.9%, um pouco abaixo das outras (41.9-52.3%) mas
  dentro da mesma faixa, e consistente com seu RPS agregado ALCANÇADO
  também ser o mais próximo dos vizinhos (397 vs 375-408).
  graficos_apresentacao/11_clients_rps_invariancia.png regenerado -- as 5
  curvas ficam visualmente sobrepostas em quase todo o grid.

Próximo passo (combinado com o usuário): disparar run_extra_reps_agg250.py
  (seção 41, criado mas não executado) agora que este lote terminou.

--------------------------------------------------------------------------------
43) run_extra_reps_agg250.py concluído -- anomalia reproduz de novo; achado
    novo: run TOTALMENTE vazio também tem CPU anormal (diferente do
    parcial); plot_graph4_refined.py corrigido de novo
--------------------------------------------------------------------------------
Resultado das 3 execuções extras (rep6-rep8, agg=250 do gráfico 04):
    rep6: 0/4 threads logaram (mesmo sintoma "totalmente vazio" já visto em
      agg512_clients2_rep3 e agg256_clients8_rep7 -- seção 39/42)
    rep7: 4/4 threads, ~180.3s, 0% erro -- normal
    rep8: 4/4 threads, ~180.6s, 0% erro -- normal
  Mais 1 reprodução em 3 tentativas -- juntando com a rep1 original,
  fica em 2 execuções ruins de 8 no total pra esse ponto (25%), a mesma
  ordem de grandeza da taxa achada na seção 42 para os pontos de 8/16
  clientes. Reforça a conclusão de lá: não é soluço raro isolado.

Achado novo (importante, corrige uma suposição da seção 41): comparando a
  série temporal de CPU da rep6 (0 linhas de worker-stop) com a da rep1
  (3/4 linhas, parcial) -- a rep1 tinha CPU normal (correto assumir fonte
  não afetada), mas a rep6 tem CPU ANORMAL do início ao fim (fica em
  ~15-25%, nunca escala, contra ~44-45% esperado pro ponto de 250 -- olha
  como um run bem mais leve que o normal, não como o baseline saudável com
  só um problema de log no final). Ou seja: run TOTALMENTE vazio (0
  linhas) não é só falha de log -- o tráfego real parece não ter sido
  gerado direito desde o começo. Run PARCIAL (algumas linhas faltando)
  continua parecendo só falha de log tardio, CPU não afetada.

Corrigido:
  plot_graph4_refined.py: error_rate_and_quality() agora retorna também
  fully_empty (bool), separado de suspect (bool) -- runs totalmente vazios
  são excluídos de TUDO (CPU e erro), runs parciais continuam só excluídos
  do cálculo de erro (CPU mantida). Coluna nova n_fully_empty_excluded no
  CSV-resumo, junto com n_error_suspect_excluded (já existia). Antes desse
  fix, a rep6 (CPU anormal) tinha entrado no cálculo de CPU do ponto de
  250 e inflado o desvio-padrão de 3.65 (valor correto, depois do fix)
  para 8.91 -- diagnosticado e corrigido nesta mesma sessão, sem precisar
  rodar nada de novo (dado já em disco).

Resultado final do gráfico 04 refinado (todos os pontos, 5-8 repetições):
  CPU sobe de 7.7% (40 agregado) a um platô de 44.6-51.9% entre 250-400,
  nunca cruza os 60%; CPU do host sobe suave e consistente (5.5%->30.7%);
  taxa de erro fica desprezível em todos os pontos (<0.06% em todos, sem
  nenhum pico dominando o eixo). graficos_apresentacao/04_curva_capacidade_REFINADA.png
  regenerado com o resultado final.

Verificado:
  - python3 -m py_compile em plot_graph4_refined.py -- sem erro.
  - Rodado de ponta a ponta, avisos de exclusão (parcial E totalmente
    vazio) apareceram corretamente no console, PNG e CSV-resumo
    inspecionados -- desvio-padrão de CPU do ponto 250 voltou a um valor
    plausível (3.65, em linha com os outros pontos do gráfico).

--------------------------------------------------------------------------------
44) Relatório consolidado da sessão (Artifact, fora do repo)
--------------------------------------------------------------------------------
Publicado um relatório HTML consolidando as seções 36-43 (curva de
  capacidade refinada, calibração de WU em potência de 2, grid clientes×RPS
  e a investigação do soluço transitório) a pedido do usuário -- não é um
  arquivo do repositório, é um Artifact publicado (link entregue ao
  usuário na conversa). Nenhum código do simulador alterado nesta seção;
  registrado aqui só para manter o histórico completo da sessão.

--------------------------------------------------------------------------------
45) plot_graph4_refined.py e plot_clients_rps_invariance.py -- eixo Y da
    taxa de erro fixado (estava com auto-scale enganoso)
--------------------------------------------------------------------------------
Contexto:
  Usuário notou, olhando o gráfico 11, que o painel de taxa de erro
  parecia mostrar erro alto e grande variação em alguns pontos (16
  agregado/8 clientes; 64 agregado) sem nenhum efeito correspondente no
  painel de CPU ao lado -- e perguntou por que.

Causa (não era bug nos dados, era escala do eixo):
  Conferido: o maior error_rate_mean em todo o grid clientes×RPS (já
  limpo dos runs contaminados pelo soluço, seção 42) é 0.11%, e o maior
  (média+desvio) é 0.26% -- ex.: 16 agregado/8 clientes teve 3 de 5
  repetições com ZERO erro e 2 com 8 erros cada em ~2.900 requisições OK
  (0.27-0.28%). O eixo Y desses painéis usava auto-scale do matplotlib,
  que dava zoom numa faixa de fração de ponto percentual (~-0.05% a
  0.26%) -- nessa escala, a diferença entre "0 erros" e "8 erros em
  2.900" vira uma barra de erro gigante visualmente, sem relação com
  nenhum efeito real de CPU. Mesmo problema conferido e confirmado em
  plot_graph4_refined.py (maior média+desvio ali: 0.11%).

Corrigido:
  Eixo Y da taxa de erro fixado em 0-1% (ax2.set_ylim(0, 1.0)) nos dois
  scripts, em vez de auto-scale -- escala honesta e comparável entre os
  dois gráficos, deixando visualmente óbvio que a taxa de erro real é
  desprezível em toda a grade testada (perto de zero, não "alta com
  grande variação").

Verificado:
  - python3 -m py_compile nos dois scripts -- sem erro.
  - Ambos rodados de ponta a ponta (dado já em disco, sem Docker) --
    painéis de erro inspecionados visualmente, agora ficam achatados perto
    de zero em toda a faixa, como esperado dado os números reais.

--------------------------------------------------------------------------------
46) Por que a dispersão aumenta em 256/512 (gráfico 11); novo gráfico
    plot_individual_client_rps.py -- prova a decomposição por cliente
    individual (não sobrescreve nem apaga o gráfico 11)
--------------------------------------------------------------------------------
Pergunta do usuário: por que os pontos do gráfico 11 (CPU média por
  decomposição) começam a se dispersar em 256 e principalmente em 512
  agregado, e se dava pra provar visualmente que a decomposição
  clientes×RPS foi realizada com o RPS individual esperado por cliente
  (não só o agregado somado).

Causa raiz da dispersão (RPS agregado ALCANÇADO diverge por decomposição
  em cargas altas, não é ruído):
  Calculado achieved_aggregate_mean/target para cada ponto do grid
  (resumo_clients_rps_grid.csv). Até 128 agregado, todas as decomposições
  ficam em 95-101% do alvo. A partir de 256, e sobretudo em 512, o déficit
  cresce e piora quanto MENOS clientes: em 512, 1 cliente atinge só 65%
  do alvo, 16 clientes atingem 80% -- nenhuma decomposição chega perto de
  512 de verdade. Confirmado com contagem de portas de origem (mesma
  técnica da seção 42): agg16/16 clientes usa 22 portas (saudável, ~1 por
  thread); agg512/1 cliente usa 394 portas para 1 thread só. CONCLUSÃO: é
  o MESMO mecanismo do soluço transitório (seção 42), só que em versão
  GRADUAL em vez de catastrófica -- perda progressiva de reaproveitamento
  de keep-alive conforme a taxa de disparo por conexão sobe, não um
  interruptor binário. Como a CPU reflete o RPS alcançado (não o alvo), a
  CPU também diverge nesses pontos -- não é "mais clientes usam mais
  CPU", é "decomposições com mais clientes mantêm mais conexões vivas e
  por isso entregam mais RPS de verdade".

Criado (novo gráfico, não mexe no 11):
  plot_individual_client_rps.py -- lê real_rps por WORKER individual (não
  só a soma) de normal_traffic_summary_log_*.csv, calcula razão
  alcançado/alvo por cliente individual, plota média±desvio por
  decomposição MAIS uma nuvem de pontos de fundo com cada worker
  individual de cada repetição (prova visual literal de que cada cliente
  isoladamente ficou perto do seu alvo individual, não só a soma). Mesmo
  critério de exclusão de run incompleto do gráfico 11. Salva
  graficos_apresentacao/12_rps_individual_por_cliente.png e
  experiment_results/clients_rps_grid/resumo_rps_individual.csv.
  Resultado: curvas e números praticamente idênticos aos do agregado
  somado (consistência interna confirmada) -- linha de 1 cliente cai a
  0.65 em 512 agregado, 16 clientes fica em 0.80, as demais decomposições
  ficam escalonadas entre essas duas, mesmo padrão da tabela de agregado.

Verificado:
  - python3 -m py_compile plot_individual_client_rps.py -- sem erro.
  - Rodado de ponta a ponta (dado já em disco) -- PNG e CSV-resumo
    inspecionados, números batem com resumo_clients_rps_grid.csv.

--------------------------------------------------------------------------------
47) plot_graph4_pow2_peak.py (novo) -- versão "por pico" do gráfico 04, em
    potências de 2, reaproveitando o grid clientes×RPS
--------------------------------------------------------------------------------
Contexto:
  Usuário pediu uma versão do gráfico 04 igual ao original (estatística de
  pico) mas com RPS agregado em potência de 2. O grid clientes×RPS (seção
  38-42) já cobre exatamente essa faixa (16-512, WU=10, sem ataque -- mesma
  configuração base do gráfico 04) -- não precisou rodar nada novo, só usar
  o subconjunto de 4 clientes (mesma convenção de nº de clientes do
  gráfico 04 original) para manter a comparação 1:1: 5 repetições limpas
  em cada um dos 6 pontos, nenhuma execução incompleta a excluir.

Criado:
  plot_graph4_pow2_peak.py -- para cada agregado (16/32/64/128/256/512),
  calcula o pico de CPU de cada uma das 5 repetições e reporta média dos
  picos ± desvio-padrão (não o pico de 1 amostra só, como o gráfico
  original) + a faixa min-max sombreada + % de repetições que de fato
  dispararam SCALE_UP naquele ponto (reforça a discussão pico vs média
  desta sessão -- pico alto = escalonamento real, não ruído). Salva
  graficos_apresentacao/13_curva_capacidade_pico_pow2.png e
  experiment_results/clients_rps_grid/resumo_graph4_pico_pow2.csv.

Resultado:
  16/32/64/128 agregado: pico médio 4.5%/7.9%/13.7%/30.5%, 0% das
  repetições escalou. 256 agregado: pico médio 68.4% (desvio 7.1pp,
  min-max 59.6-75.6%), 80% das repetições escalou de verdade. 512
  agregado: pico médio 94.5%, 100% das repetições escalou. A transição do
  limiar de 60% fica localizada com precisão entre 128 e 256 agregado --
  mais nítido que o gráfico 04 original, que não tinha ponto nenhum nessa
  faixa (pulava de 200 para 250/300/400).

Não coberto (fica para se o usuário pedir):
  RPS agregado = 8 não existe no grid (menor ponto coletado é 16) -- não
  reaproveitável de dado já existente, precisaria de 5 execuções novas
  (carga bem leve, rápido).

Verificado:
  - python3 -m py_compile plot_graph4_pow2_peak.py -- sem erro.
  - Rodado de ponta a ponta (dado já em disco, sem Docker) -- PNG e
    CSV-resumo inspecionados.

--------------------------------------------------------------------------------
48) plot_wu_calibration_instances_pow2.py (novo) -- série temporal de
    CPU + nº de instâncias, complemento ao gráfico 05
--------------------------------------------------------------------------------
Contexto:
  Discussão com o usuário sobre por que WU=200.000/rps=10 gera bastante
  erro (1.747) tendo CPU MÉDIA mais baixa que WU=400.000/rps=10 (110% vs
  193%) -- resposta foi que os dois cruzam o mesmo limiar de sobrecarga no
  início do ataque (janela de 15-20s antes do autoscaler reagir), só que
  200k se RECUPERA depois de escalar (CPU volta a 55-130%) e 400k NUNCA se
  recupera (fica travado em ~185-217% o resto do run) -- a média do run
  inteiro esconde isso. Usuário pediu um gráfico que deixasse esse
  mecanismo visível, com informação de nº de instâncias.

Criado:
  plot_wu_calibration_instances_pow2.py -- grade 3x3 (rps=1/5/10 ×
  WU=100k/200k/400k), cada painel com a série temporal de CPU média
  (linha colorida por WU) e nº de instâncias (linha tracejada cinza, eixo
  direito) ao longo dos 180s, faixa sombreada marcando a janela de ataque
  (lida direto do dado via label=='attack', não hardcoded de config.py),
  limiar de 60% marcado, e total de erros daquela combinação no título de
  cada painel (destacado em vermelho/verde quando >200 erros). Salva
  graficos_apresentacao/14_wu_calibration_instancias_pow2.png.

Resultado visual: fica nítido nos 3 painéis de rps=10 (linha de baixo) --
  WU=100.000 sobe a ~155%, cai rápido e fica baixo o resto do tempo, 0
  erros; WU=200.000 sobe a ~240%, cai depois de escalar mas ainda oscila
  bastante (55-130%), 1.747 erros; WU=400.000 sobe a ~217% e FICA LÁ,
  travado, pelo resto dos 180s, 5.035 erros -- confirma visualmente a
  explicação dada ao usuário.

Verificado:
  - python3 -m py_compile plot_wu_calibration_instances_pow2.py -- sem
    erro.
  - Rodado de ponta a ponta 2x (1ª vez com a legenda cortada embaixo da
    figura, corrigido aumentando a altura da figura e ajustando
    bbox_to_anchor/tight_layout) -- PNG final inspecionado, legenda e 9
    painéis legíveis.

--------------------------------------------------------------------------------
49) plot_graph05_pow2_peak.py (novo) -- gráfico 05 potência de 2, versão
    por PICO (não sobrescreve a versão média)
--------------------------------------------------------------------------------
Pedido do usuário: mesmo gráfico 05 pow2, mas com estatística de pico em
  vez de média (a versão que já existe, 05_wu_calibration_resumo_POW2.png).
  Criado plot_graph05_pow2_peak.py, mesma estrutura de
  plot_graph05_pow2.py trocando mean_cpu()->peak_cpu() (df.max() em vez de
  df.mean()), salvo como graficos_apresentacao/15_wu_calibration_resumo_pico_pow2.png
  (arquivo novo, não mexe nos outros 2 gráficos 05).

Resultado (1 amostra/ponto, mesma limitação do gráfico 05 original -- sem
  repetição neste dado):
    rps=1:  WU=100k pico=33.6  WU=200k pico=30.5  WU=400k pico=66.1
    rps=5:  WU=100k pico=74.3  WU=200k pico=134.9 WU=400k pico=205.2
    rps=10: WU=100k pico=156.4 WU=200k pico=239.4 WU=400k pico=216.2
  Nota: em rps=10, WU=200.000 (239.4) volta a superar WU=400.000 (216.2)
  no pico -- é a MESMA inversão discutida no início desta sessão (seção
  "por que o verde ultrapassa o vermelho"), esperada e não é bug: pico é
  sensível ao timing da transição de SCALE_UP (janela de 2 instâncias),
  não ao nível sustentado -- reaparece aqui porque este gráfico usa pico
  de propósito, a pedido do usuário, para comparação com o estilo do
  gráfico 05 original.

Verificado:
  - python3 -m py_compile plot_graph05_pow2_peak.py -- sem erro.
  - Rodado de ponta a ponta (dado já em disco) -- PNG inspecionado.

--------------------------------------------------------------------------------
50) plot_graph4_reproducibilidade_refinada.py (novo) -- gráfico 04 original
    (por pico) refeito com o dado completo, sem os marcadores especiais
    de 250/300
--------------------------------------------------------------------------------
Pedido do usuário: refazer o gráfico 04 original (04_curva_capacidade_reproducibilidade.png,
  estatística de pico) usando o dado completo já coletado (5-8 repetições
  por ponto de run_graph4_refined.py, seção 37/41/43), removendo os
  marcadores especiais de 250 (X vermelho, 3 reps) e 300 (losango
  amarelo, reexecução) -- esses só existiam porque, na época, só esses 2
  pontos tinham repetição; agora todo ponto tem, então uma curva única e
  consistente é mais correta que destacar 2 pontos.

Criado:
  plot_graph4_reproducibilidade_refinada.py -- mesmo critério de exclusão
  de run totalmente vazio da seção 43 (CPU genuinamente anormal nesses
  casos, não só falha de log), calcula pico por repetição e reporta
  média±desvio + faixa min-max sombreada, uma cor só, sem destaque
  especial por ponto. Salvo graficos_apresentacao/16_curva_capacidade_reproducibilidade_refinada.png
  -- não sobrescreve o gráfico original nem o _REFINADA (por média).

Resultado: curva sobe suave de 10.0% (40 agregado) a 93.8% (400 agregado),
  cruzando o limiar de 60% por volta de 200 agregado (pico médio 58.9%,
  min-max 49.9-71.6% -- a única repetição que cruza os 60% de verdade
  ali, dentro da faixa observada). Bem mais legível que o gráfico
  original: uma curva só, sem inversão nem destaque de pontos isolados,
  com barra de erro real em todo ponto.

Verificado:
  - python3 -m py_compile plot_graph4_reproducibilidade_refinada.py -- sem
    erro.
  - Rodado de ponta a ponta (dado já em disco) -- aviso de exclusão da
    rep6/250 (já conhecida, seção 43) apareceu corretamente; PNG
    inspecionado.

--------------------------------------------------------------------------------
51) Relatório consolidado (Artifact) atualizado com gráficos 12-16 e
    avaliação para o TCC
--------------------------------------------------------------------------------
Usuário pediu material pra apresentar progresso ao orientador. Atualizado
  o mesmo Artifact da seção 44 (mesmo link) com: explicação host vs.
  container CPU (seção 01), seção nova de validação cruzada por pico
  (gráficos 13 e 16, tabela dos 12 pontos combinados), seção nova do
  mecanismo de fila/recuperação (gráfico 14, menção ao gráfico 15 como
  reprodução esperada do artefato de pico), seção "Estado atual" reescrita
  respondendo diretamente as 2 perguntas centrais (RPS agregado importa? /
  dá pra explicar o limite do simulador -- 3 mecanismos distintos), e
  seção nova "Base científica para o TCC" com avaliação e sugestões de
  melhoria. Nenhum código do simulador alterado; registrado só para manter
  o histórico completo da sessão.

--------------------------------------------------------------------------------
52) plot_grid_heatmap.py (novo) -- heatmap agregado×clientes, visualização
    célula a célula do grid inteiro
--------------------------------------------------------------------------------
Usuário pediu uma visualização que mostrasse o grid inteiro (todas as 30
  células, agregado × clientes) de uma vez, célula a célula, complementar
  às linhas resumidas do gráfico 11. Criado plot_grid_heatmap.py -- lê
  resumo_clients_rps_grid.csv (já calculado, nenhum experimento novo),
  monta uma matriz 6x5 (linhas=RPS agregado 16-512, colunas=nº de
  clientes 1/2/4/8/16), cor + texto = CPU média do container em cada
  célula. Salvo graficos_apresentacao/17_grid_heatmap_cpu.png.

Resultado: linhas de 16 a 128 ficam visualmente uniformes (variação
  <1 ponto percentual entre colunas da mesma linha) -- confirma a
  hipótese de forma direta e literal (mesma linha = mesma cor,
  independente da coluna). Linha de 256 quase uniforme (45.2-48.0%).
  Linha de 512 mostra gradiente visível (52.3% em 1 cliente até 38.9% em
  8 clientes) -- é a mesma ressalva já documentada (seção 46), agora
  visível de forma direta numa única imagem.

Verificado:
  - python3 -m py_compile plot_grid_heatmap.py -- sem erro.
  - Rodado de ponta a ponta (dado já em disco) -- PNG inspecionado.

Ajuste seguinte (mesmo item 52): usuário pediu para adicionar o RPS/cliente
  em cada célula do heatmap, não só o agregado (que já estava nos eixos).
  Anotação de cada célula agora tem 2-3 linhas: CPU%, "N rps/cli"
  (calculado como agregado/clientes daquela célula, a decomposição real
  usada naquela execução) e, quando a contagem de repetições válidas é
  diferente de 5, "(n=N)". Reexecutado (dado já em disco) e PNG
  reinspecionado.

--------------------------------------------------------------------------------
53) Relatório consolidado (Artifact) atualizado com o gráfico 17 (heatmap)
--------------------------------------------------------------------------------
Adicionado o gráfico 17 (heatmap agregado×clientes, seção 52) na seção
  "RPS agregado é o que importa?" do relatório consolidado (mesmo link da
  seção 44/51), logo depois do gráfico 12, com um parágrafo explicando a
  leitura ("cada linha deveria ter cor uniforme"). Nenhum código do
  simulador alterado.

--------------------------------------------------------------------------------
54) plot_graph4_reproducibilidade_refinada.py -- gráfico 16 refinado com
    marcação de SCALE_UP real por repetição
--------------------------------------------------------------------------------
Contexto: usuário perguntou por que o desvio-padrão do gráfico 16 é alto
  em 200/250 agregado e cai em 300 -- resposta (dada em chat, ver
  histórico): em 200, 3 de 5 repetições nunca escalaram (pico 50-55%) e 2
  escalaram (pico 63-72%) -- é uma mistura de 2 comportamentos diferentes,
  não ruído em torno de 1 comportamento só. Em 250, 6 de 8 escalaram, 2
  não. Em 300, as 5 de 5 escalam sempre, sempre 3 vezes -- comportamento
  uniforme, desvio baixo. Pedido: refinar o gráfico pra mostrar isso
  visualmente, de forma simples.

Alterado:
  plot_graph4_reproducibilidade_refinada.py -- para cada repetição
  individual, verifica se `decision=='SCALE_UP'` aconteceu de verdade em
  algum tick daquele run, e plota um ponto (verde preenchido = escalou,
  cinza vazado = não escalou) além da linha de média±desvio já existente.
  Anota "N/total escalou" acima de cada agregado. Mesmo arquivo de saída
  (16_curva_capacidade_reproducibilidade_refinada.png), não criou gráfico
  novo -- é um refinamento do mesmo.

Resultado: fica visualmente óbvio que 200 e 250 agregado têm pontos
  cinzas e verdes misturados (às vezes escala, às vezes não -- por isso o
  desvio alto), enquanto 40/100 (nunca escala) e 300/400 (sempre escala)
  ficam com pontos de uma cor só (desvio baixo nos dois extremos, alto só
  na transição).

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado 2x (1ª vez com anotação "2.0/5.0 escalou", corrigido pra
    "2/5" com formatação :.0f) -- PNG final inspecionado.

--------------------------------------------------------------------------------
55) plot_graph4_reproducibilidade_media_scaleup.py (novo) -- gráfico 18,
    mesma estrutura do gráfico 16 (16 clientes fixo, colorido por
    SCALE_UP real) mas com CPU MÉDIA em vez de pico -- pra comparação lado
    a lado
--------------------------------------------------------------------------------
Contexto: depois de explicar por que execuções que escalam têm pico alto
  (o pico É o instante que disparou o escalonamento, não um efeito
  colateral dele -- causa e efeito), usuário pediu a mesma estrutura de
  gráfico (pontos coloridos por SCALE_UP real) só que usando CPU MÉDIA da
  execução inteira em vez de pico, pra comparar visualmente os dois
  pontos de vista lado a lado.

Criado: plot_graph4_reproducibilidade_media_scaleup.py, cópia estrutural
  de plot_graph4_reproducibilidade_refinada.py trocando pico por média
  (mesmo critério de exclusão de run vazio das seções 43/54). Salvo
  graficos_apresentacao/18_curva_capacidade_reproducibilidade_media_scaleup.png
  -- não sobrescreve o gráfico 16.

Resultado (confirma a expectativa): por média, os pontos verdes
  (escalou) e cinzas (não escalou) ficam completamente misturados em
  TODOS os agregados, sem separação visível -- diferente do gráfico 16
  por pico, onde ficavam claramente separados em 200/250. A curva de
  média nem chega perto do limiar de 60% (máximo ~52-57% em 400
  agregado), porque a média dilui o pico pontual que disparou o
  escalonamento dentro dos 180s inteiros. Confirma visualmente, numa
  comparação direta e lado a lado, a explicação de causa/efeito dada ao
  usuário.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta (dado já em disco) -- PNG inspecionado.

--------------------------------------------------------------------------------
56) plot_host_saturacao_prova.py (novo) -- prova direta de que o erro
    catastrófico coincide com a saturação real dos 8 núcleos do host
--------------------------------------------------------------------------------
Usuário pediu um gráfico que comprovasse a teoria "o erro explode quando
  bate no teto de 8 núcleos do host" (estrutural: MAX_INSTANCES=4 ×
  INSTANCE_MAX_CONCURRENT_REQUESTS=2 = 8, coincidindo com nproc=8).
  Único conjunto de dados com host_cpu_percent E ataque pesado o
  suficiente para testar isso é wu_calibration/WU=400000 (coletado depois
  da seção 37) -- os 3 níveis de rps/atacante (1/5/10).

Criado: plot_host_saturacao_prova.py -- plota host_cpu_percent (não
  container) ao longo do tempo, 1 linha por rps/atacante, com o total de
  erros de cada uma na legenda, e linha de referência em 100% ("os 8
  núcleos saturados"). Salvo graficos_apresentacao/19_host_saturacao_prova.png.

Resultado (prova direta, tipo dose-resposta):
    rps=1  -- host fica em ~10-14% o tempo todo -- 3 erros
    rps=5  -- host sobe a 80-91% mas oscila, nunca gruda no teto -- 71 erros
    rps=10 -- host GRUDA em 99-100% a partir de t=80s até o fim -- 5.035 erros
  A única linha que satura de verdade em ~100% (todos os 8 núcleos
  lógicos ocupados, sem folga, por >80s seguidos) é exatamente a que tem
  o colapso catastrófico de erro -- correlação direta entre saturação
  real do host e explosão de erro, não só coincidência de nível de carga.

Verificado:
  - python3 -m py_compile plot_host_saturacao_prova.py -- sem erro.
  - Rodado de ponta a ponta (dado já em disco) -- PNG inspecionado.

--------------------------------------------------------------------------------
57) Relatório consolidado (Artifact) atualizado com os gráficos 18 e 19
--------------------------------------------------------------------------------
Adicionado o gráfico 18 (mesma estrutura do 16, por média, seção 55) logo
  depois do gráfico 16 na seção de validação cruzada por pico, com
  parágrafo explicando a mistura de pontos verde/cinza. Adicionado o
  gráfico 19 (prova de saturação do host, seção 56) na seção do mecanismo
  do limite, depois do callout do gráfico 15, amarrando de volta ao
  argumento estrutural da seção 01 (MAX_INSTANCES×INSTANCE_MAX_CONCURRENT_REQUESTS=8
  = nproc). Mesmo link do Artifact (seção 44/51/53). Nenhum código do
  simulador alterado.

--------------------------------------------------------------------------------
58) Lote de 6 tarefas combinado com o usuário -- 4 rápidas (feitas) + 2
    lotes grandes de Docker (disparados em background)
--------------------------------------------------------------------------------
Tarefa 1 -- gráfico 14: "erro"->"timeout" + porcentagem
  Alterado plot_wu_calibration_instances_pow2.py: título de cada painel
  agora mostra "N timeouts (X.X%)" em vez de "N erros" -- taxa calculada
  como errors/(total_requests+errors) do attack_summary_log, mesma
  convenção usada no resto da sessão. PNG regenerado.

Tarefa 2 -- novo gráfico: pico só ANTES do primeiro escalonamento
  Criado plot_graph4_pico_pre_escalonamento.py: usa o mesmo dado do
  gráfico 16, mas corta cada repetição na linha do 1º SCALE_UP (inclusive)
  em vez de usar o pico do run inteiro -- percebido que o pico do run
  inteiro às vezes vem de um SEGUNDO evento de escalonamento tardio (ex.:
  agg200 rep1 escalou em t=5s [pico 65%] E de novo em t=170s [pico 71.6%,
  o maior dos dois]), então "pico do run inteiro" nem sempre é "pico que
  causou o PRIMEIRO SCALE_UP". Salvo
  graficos_apresentacao/20_pico_pre_escalonamento_1_instancia.png.
  Resultado: valores um pouco mais baixos e com desvio um pouco menor que
  o gráfico 16 em 200-300 agregado (ex.: 250 cai de 72.8%±7.7 pra
  66.2%±4.7), como esperado ao isolar só a janela de 1 instância.

Tarefa 3 -- gráfico 17: barra de cor invertida + nº máximo de instâncias
  Alterado plot_grid_heatmap.py: cbar.ax.invert_yaxis() (maior calor
  embaixo da barra, menor em cima -- não muda o mapeamento cor<->valor,
  só a orientação visual da legenda) + cada célula ganhou uma 3ª linha de
  anotação "máx N inst." (maior nº de instâncias visto entre as
  repetições daquela célula, calculado direto dos CSVs brutos, não
  estava no resumo). PNG regenerado -- revela, por ex., que em 512
  agregado "1 cliente" só chegou a 2 instâncias enquanto "4/8 clientes"
  chegaram a 3 -- consistente com menos clientes = menos RPS alcançado =
  menos pressão de escalonamento (seção 46).

Tarefa 4 -- novo heatmap estilo 17, com dado de ataque (wu_calibration,
  já existente, nenhum experimento novo)
  Criado plot_grid_heatmap_ataque.py: linhas=rps/atacante, colunas=WU,
  cor=CPU média (janelas de ataque), cada célula anota CPU%, RPS agregado
  (rps×4 atacantes), taxa de timeout, e nº máximo de instâncias. Salvo
  graficos_apresentacao/21_grid_heatmap_ataque.png. Só 1 amostra/célula
  (mesma limitação do wu_calibration original) -- rotulado como
  descritivo, não teste de invariância (diferente do 17, aqui rps E WU
  contribuem pra severidade, não dá pra esperar linha uniforme).

Tarefa 5 -- ajuste de código + ataque isolado (sem tráfego normal)
  Alterado normal_traffic.py: start_http_traffic() agora retorna sem
  iniciar nenhuma thread se rps_per_worker_override<=0 ou
  num_clients_override<=0, em vez de cair no fallback de
  normal_http_request_worker (sleep_duration=1.0s fixo), que ainda gerava
  ~1 req/s por cliente mesmo com --normal-rps 0 -- esse fallback existe
  faz tempo pra evitar ZeroDivisionError, mas nunca tinha sido usado pra
  desligar tráfego normal de propósito antes. Testado isoladamente
  (--normal-rps 0, --duration 15) -- confirmado "[Normal_Injector] ...
  tráfego normal desligado de propósito, não iniciado", nenhuma thread
  criada. Artefatos do teste apagados depois de conferidos.
  Criado run_ataque_isolado.py (9 execuções: rps=1/5/10 × WU=100k/200k/400k,
  --normal-rps 0, mesmos parâmetros de rps/WU/duração do gráfico 14) e
  plot_ataque_isolado.py (mesma estrutura do gráfico 14, lendo
  experiment_results/ataque_isolado/, salva
  graficos_apresentacao/22_ataque_isolado_instancias.png) -- pra comparar
  lado a lado com o gráfico 14 (que sempre teve 40 agregado de tráfego
  normal padrão junto, sem ninguém ter pedido isso).

Tarefa 6a -- completar a série do gráfico 16/18/20 pra 10 repetições/ponto
  Criado run_graph4_completar_150_350.py: +5 repetições nos 6 pontos já
  existentes (40/100/200/300/400: rep1-5 -> rep1-10; 250: rep1-8 ->
  rep1-13) e 2 pontos novos, 150 e 350, com 10 repetições cada (rep1-10).
  Mesma metodologia/diretório/convenção de nome de run_graph4_refined.py.
  45 execuções novas. AGGREGATE_TARGETS atualizado em
  plot_graph4_reproducibilidade_refinada.py,
  plot_graph4_reproducibilidade_media_scaleup.py e
  plot_graph4_pico_pre_escalonamento.py para incluir 150 e 350.

Tarefa 6b -- série nova em potência de 2 (base 20)
  Criado run_graph4_pow2_base20.py: 20, 80, 160, 320, 640 (10 repetições
  cada, 50 execuções novas) -- "40" reaproveitado da tarefa 6a (mesmo
  arquivo/convenção, decisão do usuário), não roda de novo. Criado
  plot_graph4_pow2_base20.py: mesma estrutura do gráfico 16 (pico por
  repetição, colorido por SCALE_UP real), eixo X em escala log2,
  aggregate targets [20,40,80,160,320,640]. Salvo
  graficos_apresentacao/23_curva_capacidade_pow2_base20.png.

Encadeamento: criado run_all_pipeline_v2.py -- roda em sequência
  run_ataque_isolado.py -> plot_ataque_isolado.py ->
  run_graph4_completar_150_350.py -> replot dos 3 gráficos 16/18/20 ->
  run_graph4_pow2_base20.py -> plot_graph4_pow2_base20.py, parando tudo se
  qualquer etapa falhar. Total: 9+45+50=104 execuções novas, ~7-8h
  estimadas. Disparado em background (run_in_background nativo) logo após
  este registro.

Verificado:
  - python3 -m py_compile em todos os arquivos novos/alterados desta seção
    -- sem erro.
  - Tarefas 1-4 rodadas de ponta a ponta (dado já em disco, sem Docker) --
    4 PNGs inspecionados visualmente.
  - Ajuste de normal_traffic.py testado isoladamente antes de disparar o
    lote grande (ver acima).
  - Lote de Docker (tarefas 5/6a/6b) ainda rodando no momento deste
    registro -- resultado a conferir quando o pipeline terminar.

--------------------------------------------------------------------------------
59) Pipeline v2 (seção 58) concluído -- 104/104 execuções, sem falha
--------------------------------------------------------------------------------
PIPELINE_COMPLETO. Tarefa 5 (9 execuções, ~28min), tarefa 6a (50
  execuções -- corrigindo a seção 58: são 6 pontos×5 + 2×10 = 50, não 45
  como escrito lá por engano) e tarefa 6b (50 execuções) rodaram sem
  nenhuma falha. ~5h40 no total (20:37 a 02:17).

Resultado gráfico 16 (agora com 40/100/150/200/250/300/350/400, 9-13
  reps/ponto): zona de transição fica bem mais resolvida --
  40:0/10, 100:0/10, 150:2/10, 200:3/10, 250:11/12, 300:10/10, 350:9/9,
  400:10/10 repetições escalando. 150 confirma um novo ponto intermediário
  genuíno na zona de transição (antes só tínhamos 100 e 200 nessa região).

Resultado gráfico 23 (pow2 base 20, novo): 20/40/80 -- 0/10 escala; 160 --
  2/10 (mesma zona de transição); 320 -- 10/10 escala, desvio alto
  (89.8%±13.8); 640 -- 7/7 válidas escalam, desvio muito alto
  (97.6%±17.0), com 3 de 10 repetições excluídas por soluço transitório
  (30% de taxa nesse ponto -- mais uma reprodução do fenômeno, dessa vez
  no extremo superior da faixa testada).

Resultado tarefa 5 (ataque isolado, sem tráfego normal) -- ACHADO NÃO
  ESPERADO, precisa de mais dado antes de concluir algo: comparando com o
  wu_calibration original (mesmos rps/WU, mas COM 40 agregado de tráfego
  normal padrão):
    rps=5  WU=400000: isolado=1.133 timeouts (42.3%) vs combinado=71 erros
    rps=10 WU=200000: isolado=845 timeouts (15.4%) vs combinado=1.747 erros
    rps=10 WU=400000: isolado=5.349 timeouts (97.8%) vs combinado=5.035 erros
  Não é uma direção consistente (às vezes isolado piora bastante, às vezes
  melhora) -- e como AMBOS os lados dessa comparação são de 1 amostra só
  (wu_calibration original nunca teve repetição, e ataque_isolado também
  rodou só 1 rep por célula, mesma convenção), não dá pra distinguir ainda
  se isso é um efeito real de "tráfego normal muda a dinâmica do ataque"
  ou só a mesma variância de amostra única (ou até soluço transitório) já
  documentada exaustivamente nesta sessão. Fica como hipótese em aberto,
  não como conclusão -- precisaria de repetições dos dois lados pra
  confirmar.

Verificado:
  - Todos os 3 scripts de replot (16/18/20) e o plot da tarefa 6b rodaram
    sem erro, tabelas impressas conferidas linha a linha.
  - graficos_apresentacao/16, /18, /20, /22, /23 inspecionados
    visualmente.
  - Contagem de arquivos finais em experiment_results/normal_baseline/
    conferida por agregado -- bate exatamente com o planejado (10 cada,
    13 em 250).

--------------------------------------------------------------------------------
60) Investigação do "isolado pior que combinado" (não resolvida, mas
    esclarecida) + plot_normal_referencia.py (novo, painel único)
--------------------------------------------------------------------------------
Usuário notou (corretamente) que "isolado" ter comportamento pior que
  "combinado" é contraintuitivo -- isolado tem, em tese, MENOS carga total
  (só ataque, sem o tráfego normal extra por cima). Investigado a fundo:

  - Comparando CPU do HOST (não só contagem de erro) célula a célula:
    rps=5/WU=400k -- isolado fica saturado (host>=90%) em 17/36 janelas
    (47%) contra só 1/28 (4%) no combinado, mesmo com a média geral de
    host quase idêntica (67.3% vs 67.4%) -- a diferença é a FORMA da
    curva (isolado trava no topo por muito mais tempo), não o nível médio.
    Em rps=10/WU=400k a direção INVERTE (combinado mais saturado: 64% vs
    56% das janelas). Em rps=10/WU=200k não dá pra comparar CPU do host --
    o arquivo original do wu_calibration é de antes da coluna existir.
  - Não foi encontrado nenhum mecanismo técnico plausível pelo qual
    REMOVER tráfego normal (WU=10, ~40.000x mais leve que WU=400.000)
    pioraria o ataque -- tráfego normal não disputa de forma relevante os
    2 slots de processamento por container. A ausência de mecanismo é, em
    si, evidência a favor da hipótese alternativa (abaixo), não de um
    efeito causal real.
  - Achado decisivo: os dois lados da comparação foram coletados com
    **15 dias de diferença** (wu_calibration original: 19/ago;
    ataque_isolado: 02/set) -- exatamente o tipo de intervalo em que este
    projeto já documentou (RESUMO_MADRUGADA_12_08.txt, seção 3.2) variação
    de ambiente WSL2/Docker sessão a sessão, sem relação com o parâmetro
    testado. O próprio gráfico 23 (coletado nesta mesma sessão) mostra
    desvio de até ±17pp e até 30% de soluço em cargas comparáveis, mesmo
    SEM trocar o dia da coleta.
  - CONCLUSÃO (não fechada): o padrão observado é real e mensurável nesse
    par específico, mas atribuir a causa a "ausência de tráfego normal" em
    vez de "sessões diferentes, 15 dias de intervalo" não tem sustentação
    -- falta o experimento de desempate (combinado vs. isolado, MESMA
    sessão, com repetições) para decidir. Proposto ao usuário, ainda não
    executado.

Criado: plot_normal_referencia.py -- painel único (não uma grade 3x3,
  porque tráfego normal isolado não tem eixo rps/WU de ataque pra variar:
  é um cenário fixo, HTTP_NORMAL_RPS_PER_CLIENT=10 × HTTP_NORMAL_NUM_CLIENTS=4
  = 40 agregado, NORMAL_WORK_UNITS=10), mesmo estilo visual dos gráficos
  14/22 (CPU + nº de instâncias), usando rep1 de
  experiment_results/normal_baseline/metrics_normal_agg40_WU10_refined_rep1.csv
  (dado já existente, nenhum experimento novo). Salvo
  graficos_apresentacao/24_normal_referencia_instancias.png -- pedido do
  usuário de ter o "terceiro" cenário (normal) no mesmo estilo visual dos
  outros dois (combinado=14, isolado=22), pra comparação completa dos 3.

Verificado:
  - python3 -m py_compile plot_normal_referencia.py -- sem erro.
  - Rodado 2x (1ª vez com legenda cortada embaixo da figura, corrigido
    aumentando a altura da figura e ajustando bbox_to_anchor/tight_layout,
    mesmo tipo de fix da seção 48) -- PNG final inspecionado, achatado e
    baixo o tempo todo, como esperado (0 timeouts, nunca escala).

--------------------------------------------------------------------------------
61) plot_comparacao_completa.py (novo) -- figura única com os 3 cenários
--------------------------------------------------------------------------------
Pedido do usuário: juntar normal (24)/combinado (14)/isolado (22) numa
  figura só. Criado plot_comparacao_completa.py -- painel único do normal
  no topo (GridSpec, sem repetir 3x já que não tem eixo rps/WU) + grade de
  18 painéis abaixo (3 rps × 3 WU × 2 condições), cada par
  COMBINADO/ISOLADO do mesmo rps/WU empilhado verticalmente pra comparação
  direta. Reaproveita os 3 conjuntos de dados já existentes, nenhum
  experimento novo. Salvo graficos_apresentacao/25_comparacao_completa_instancias.png.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta -- aviso de tight_layout com eixos "off"
    (decorativos, pra preencher os cantos do topo) não afeta o resultado
    visual, PNG inspecionado, os 19 painéis renderizam corretamente.

--------------------------------------------------------------------------------
62) Experimento de desempate combinado × isolado (seção 60) -- disparado
--------------------------------------------------------------------------------
Pedido do usuário: investigar a fundo a discrepância da seção 60 rodando
  combinado e isolado NA MESMA SESSÃO, com repetições, pra separar efeito
  real de deriva de sessão/ambiente (as coletas comparadas antes tinham 15
  dias de diferença).

Criado:
  - run_desempate_combinado_isolado.py: rps={5,10} × WU={100k,200k,400k}
    (rps=1 excluído -- já mostrou comportamento trivial idêntico nos dois
    lados antes, não justifica o tempo de Docker) × 5 repetições ×
    2 condições (combinado: --normal-rps 10, default; isolado:
    --normal-rps 0) -- 60 execuções. Ordem INTERCALADA por repetição
    (combinado rep1, isolado rep1, combinado rep2, isolado rep2, ...)
    dentro de cada célula, pra controlar também deriva de curto prazo
    dentro da própria sessão, não só entre sessões. Salva em
    experiment_results/desempate_combinado_isolado/.
  - plot_desempate_combinado_isolado.py: por célula, média±desvio de (a)
    taxa de timeout e (b) fração de janelas com CPU do host ≥90%, gráfico
    de barras com barra de erro lado a lado (combinado vs isolado) pras
    duas métricas -- se as barras de erro se sobrepõem, a diferença é
    compatível com ruído; se ficarem separadas mesmo na mesma sessão, é
    evidência de efeito real. Salva
    graficos_apresentacao/26_desempate_combinado_isolado.png e
    experiment_results/desempate_combinado_isolado/resumo_desempate.csv.
  - run_all_desempate.py: encadeia os dois scripts acima, para se
    qualquer etapa falhar.

Verificado:
  - python3 -m py_compile nos 3 scripts -- sem erro.
  - Disparado em background (run_in_background nativo) logo após este
    registro -- 60 execuções, ~3-3.5h estimadas. Resultado a conferir
    quando terminar.

--------------------------------------------------------------------------------
63) Desempate combinado × isolado (seção 62) concluído -- VEREDITO: a
    discrepância da seção 60 NÃO se repete na mesma sessão
--------------------------------------------------------------------------------
60/60 execuções sem falha (~3h09, 09:22 a 12:32). Resultado
  (resumo_desempate.csv, gráfico 26):

    rps=5  WU=100k:  combinado=0.15%±0.11  isolado=0.21%±0.15  -- iguais
    rps=5  WU=200k:  combinado=0.10%±0.12  isolado=0.13%±0.11  -- iguais
    rps=5  WU=400k:  combinado=34.1%±8.2   isolado=9.3%±19.2   -- ver abaixo
    rps=10 WU=100k:  combinado=0.02%±0.05  isolado=0.05%±0.05  -- iguais
    rps=10 WU=200k:  combinado=24.5%±5.1   isolado=23.6%±3.5   -- iguais
    rps=10 WU=400k:  combinado=97.3%±0.3   isolado=96.8%±0.4   -- iguais

  4 das 6 células ficam estatisticamente indistinguíveis (barras de erro
  totalmente sobrepostas) -- inclusive rps=10/WU=200k, que na comparação
  original (sessões de 15 dias de diferença) tinha mostrado 1.747 vs 845
  erros (2x), e agora dá 24.5% vs 23.6% (praticamente igual) com dado da
  mesma sessão.

  A única célula com diferença de médias (rps=5/WU=400k) na verdade
  INVERTEU de direção em relação à comparação original: antes isolado
  tinha MAIS erro (42.3% vs 2.6%); agora, com repetição, é o COMBINADO que
  tem mais erro na média (34.1% vs 9.3%) -- e o isolado tem desvio-padrão
  (19.2) quase do tamanho da própria média, sinal claro de que essa célula
  específica está numa zona de transição instável (mesmo fenômeno já
  documentado pra 150-250 agregado no tráfego normal, seção 59/63) --
  varia muito de repetição pra repetição independente da condição, não é
  um efeito estável de "ter ou não tráfego normal".

  CONCLUSÃO FECHADA: a discrepância observada na seção 60 era mesmo
  deriva de sessão/ambiente (15 dias de intervalo entre as coletas
  originais), não um efeito causal real da presença/ausência de tráfego
  normal -- confirmado experimentalmente, não só por falta de mecanismo
  plausível. Reforça a lição geral desta sessão inteira: comparações de
  amostra única, ainda mais entre datas diferentes, não são confiáveis
  neste simulador -- repetição na MESMA sessão é necessária pra qualquer
  conclusão causal.

Verificado:
  - python3 plot_desempate_combinado_isolado.py rodado sem erro (dentro
    do pipeline), tabela e gráfico inspecionados.
  - graficos_apresentacao/26_desempate_combinado_isolado.png confirma
    visualmente: barras de erro sobrepostas em 5 das 6 células (a 6ª,
    rps=5/WU=400k, tem barra do isolado gigante, consistente com zona de
    transição instável, não com efeito sistemático).

--------------------------------------------------------------------------------
64) "Isolado não diminui a CPU depois de escalar" -- checado e refutado
    com repetição; plot_comparacao_completa_v2.py (novo, gráfico 27, NÃO
    substitui o 25)
--------------------------------------------------------------------------------
Usuário notou que, no gráfico 25 (amostra única antiga), a curva do
  isolado em rps=5/WU=400k parecia nunca cair depois de escalar pra 4
  instâncias, enquanto a do combinado caía -- contraintuitivo, já que mais
  instância deveria sempre reduzir CPU.

Checado com as 5 repetições do desempate (seção 62/63): comparando CPU
  média na fase de rampa (2-3 instâncias, t<=65s) vs. fase estável (4
  instâncias, t>65s), a CPU cai da rampa pra fase estável em TODAS as 10
  repetições (5 combinado + 5 isolado) -- sem exceção. O padrão "isolado
  não cai" do gráfico 25 era específico daquela ÚNICA amostra antiga (a
  mesma de 15 dias atrás), não uma característica real da condição
  isolado -- mais uma confirmação de que amostra única engana mesmo numa
  série temporal, não só em contagem de erro.

Criado (não sobrescreve plot_comparacao_completa.py / gráfico 25):
  plot_comparacao_completa_v2.py -- mesma estrutura da v1, mas pras
  células cobertas pelo desempate (rps=5 e rps=10, os 3 WU) plota as 5
  repetições sobrepostas (linhas finas, alpha reduzido) em vez de 1 linha
  só, com o timeout no título como média±desvio (n=5) em vez de valor
  único. rps=1 (não coberto pelo desempate) e o painel de normal
  continuam com amostra única (rotulado explicitamente "amostra única" no
  título, pra não confundir com as células com repetição). Salvo
  graficos_apresentacao/27_comparacao_completa_v2.png.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta (dado já em disco, sem Docker) -- PNG
    inspecionado: nas células rps=5/10, as 5 linhas de cada condição
    aparecem espalhadas de forma parecida nos dois lados (combinado e
    isolado), sem o padrão "uma condição sempre no topo" que a amostra
    única antiga sugeria -- confirma visualmente a checagem numérica
    acima.

--------------------------------------------------------------------------------
65) plot_comparacao_completa_v2.py (mesmo gráfico 27, atualizado in-place)
    -- 1 repetição representativa em vez de 5 linhas sobrepostas
--------------------------------------------------------------------------------
Pedido do usuário: a versão com as 5 linhas sobrepostas ficou poluída --
  trocar por 1 linha só por painel nas células rps=5/10, escolhida por
  critério objetivo (não a melhor nem a pior, a mais "típica"). Dessa vez
  o usuário pediu "atualize" (não "crie um novo") -- interpretado como
  atualizar o gráfico 27 em vez de criar um 28, diferente do padrão
  anterior desta sessão.

Alterado: plot_comparacao_completa_v2.py -- para cada célula com desempate,
  calcula a taxa de timeout das 5 repetições, acha a MEDIANA, e escolhe
  pra plotar a repetição cujo valor fica mais perto dela (critério
  objetivo, não escolha visual). Título de cada painel agora mostra
  "repN (mais próx. da mediana)" além da média±desvio das 5 (mantém o
  contexto estatístico mesmo mostrando só 1 linha). Removida a função
  plot_multi() (não usada mais) e o import de glob (não usado mais).
  Sobrescreve graficos_apresentacao/27_comparacao_completa_v2.png (mesmo
  arquivo, atualizado -- gráfico 25 continua intocado).

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta -- PNG inspecionado, painéis limpos (1 linha
    cada), rep escolhida varia por célula (não é sempre a mesma rep),
    consistente com o critério de mediana por célula.

--------------------------------------------------------------------------------
66) Completando rps=1 no desempate (30 execuções) -- disparado
--------------------------------------------------------------------------------
Usuário notou a assimetria (rps=1 sem repetição, rps=5/10 com 5) e pediu
  pra completar. Criado run_desempate_rps1.py -- mesmo padrão de
  run_desempate_combinado_isolado.py (seção 62), só rps=1, 3 WU, 5
  repetições x 2 condições = 30 execuções, salvando no MESMO diretório
  (experiment_results/desempate_combinado_isolado/) pra somar ao dado já
  existente. Atualizado RPS_VALUES em plot_desempate_combinado_isolado.py
  (de [5,10] para [1,5,10]) e DESEMPATE_RPS em
  plot_comparacao_completa_v2.py (de {5,10} para {1,5,10}), pra esses 2
  scripts passarem a tratar rps=1 como célula com repetição também.
  Criado run_all_desempate_rps1.py encadeando os 3 (execução + os 2
  replots).

Verificado:
  - python3 -m py_compile nos 4 arquivos -- sem erro.
  - Disparado em background (run_in_background nativo) -- 30 execuções,
    ~1h35-1h40 estimadas. Resultado a conferir quando terminar.

--------------------------------------------------------------------------------
67) Seção 66 concluída -- desempate agora completo nas 9 células (rps=1/5/10)
--------------------------------------------------------------------------------
30/30 execuções sem falha (~1h32, 13:15 a 14:47). rps=1 confirma o que já
  se esperava: combinado e isolado praticamente idênticos e desprezíveis
  nos 3 WU (0-0.5% de timeout, desvios pequenos, barras de erro totalmente
  sobrepostas) -- decisão original de pular rps=1 por "comportamento
  trivial" (seção 62) se confirma corretamente, não escondia nenhuma
  diferença real. Tabela completa (9 células) e
  graficos_apresentacao/26_desempate_combinado_isolado.png e
  27_comparacao_completa_v2.png regenerados com as 9 células, todas agora
  com n=5 (nenhuma mais como "amostra única").

Verificado:
  - Tabela resumo_desempate.csv conferida linha a linha.
  - Gráficos 26 e 27 inspecionados -- rps=1 aparece com barras/painéis
    consistentes com o resto do desempate, sem mais assimetria.

--------------------------------------------------------------------------------
68) Discussão sobre remoção de outlier por quartis (IQR) -- decisão: NÃO usar
--------------------------------------------------------------------------------
Usuário perguntou se dava pra usar quartis (IQR/Tukey) pra eliminar
  outliers automaticamente, já que vários gráficos desta sessão mostraram
  alta variância. Testado ao vivo com dado real (picos de CPU das 10
  repetições de agregado=150, gráfico 16/20): a regra de IQR
  (Q1-1.5×IQR, Q3+1.5×IQR) marcaria como outlier exatamente as 2
  repetições que dispararam SCALE_UP de verdade -- ou seja, apagaria o
  sinal que PROVA que esse ponto está na zona de transição, um dos
  achados centrais da investigação.

Explicado ao usuário: o dataset tem 2 tipos de "outlier" com causas
  diferentes, que IQR não distingue (só olha dispersão numérica, não
  causa): (1) variância bimodal real perto do limiar de 60% -- dois
  comportamentos genuínos (escala/não escala), não ruído gaussiano; IQR
  marca a minoria como outlier e apaga sinal real; (2) o soluço
  transitório -- esse sim é ruído de verdade, mas já é excluído por
  critério MECÂNICO (linhas de log faltando, duração além do esperado,
  zero requisições completadas) em vez de desvio estatístico, o que é
  mais defensável e já está implementado em vários scripts desta sessão.

DECISÃO (não aplicada, guardada para o futuro -- ver memória
  feedback_no_iqr_outliers.md): manter o critério de exclusão mecânico já
  usado, nunca trocar por corte estatístico (IQR/z-score) em cima da
  própria métrica. IQR só seria aceitável como ferramenta de TRIAGEM
  (levantar candidatos pra investigar manualmente), nunca como regra de
  exclusão automática.

--------------------------------------------------------------------------------
69) plot_grid_heatmap_isolado.py (novo, gráfico 28) -- completa o trio de
    heatmaps (normal/ataque isolado/combinado)
--------------------------------------------------------------------------------
Usuário pediu um heatmap estilo 21 (ataque), mas para ataque ISOLADO (sem
  tráfego normal) -- pra ter os 3 grids completos: 17=só tráfego normal
  (clientes×RPS), 21=combinado (ataque+40 agregado normal, wu_calibration,
  1 amostra/célula), 28=NOVO, só ataque.

Fonte escolhida (decisão minha, não perguntada -- justificada abaixo):
  experiment_results/desempate_combinado_isolado/ (seção 62/66, condição
  "isolado"), que já tem 5 REPETIÇÕES por célula nas 9 combinações
  (rps=1/5/10 × WU=100k/200k/400k) -- MELHOR que a amostra única de
  experiment_results/ataque_isolado/ (tarefa 5 original, seção 58), que
  também existia mas só tinha 1 amostra/célula. Escolhido o dado com
  repetição, consistente com a linha desta sessão inteira de preferir
  repetição a amostra única sempre que ambas existem.

Criado: plot_grid_heatmap_isolado.py -- mesma estrutura visual de
  plot_grid_heatmap_ataque.py (linhas=rps/atacante, colunas=WU, cor=CPU
  média), mas cada célula mostra média±desvio das 5 repetições (não 1
  valor só), RPS agregado, taxa média de timeout, e nº máximo de
  instâncias observado entre as repetições. Salvo
  graficos_apresentacao/28_grid_heatmap_isolado.png.

Resultado: rps=1 fica leve em todos os WU (8.3-31.5%, quase sem timeout);
  rps=5/WU=400k já mostra desvio grande (115.6%±28.9, zona de transição
  instável, mesmo padrão já visto no desempate); rps=10/WU=400k satura
  quase por completo (195.2%±0.6, 96.8% timeout, desvio baixíssimo --
  célula "sempre colapsa", sem ambiguidade).

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado 2x (1ª vez com título cortado na borda da figura, corrigido
    reduzindo fontsize e deixando fig.tight_layout() sem rect customizado)
    -- PNG final inspecionado, título e 9 células legíveis.

--------------------------------------------------------------------------------
70) Fechando o cenário COMBINADO com repetição -- gráficos 29 e 30 (novos,
    não substituem 14/21)
--------------------------------------------------------------------------------
Pedido do usuário: aplicar ao cenário COMBINADO (ataque + 40 agregado de
  tráfego normal padrão) o mesmo tratamento já dado ao isolado (gráficos
  22/28 com 5 repetições) -- hoje só existe em amostra única
  (wu_calibration original, gráficos 14/21). Confirmado antes de agir:
  experiment_results/desempate_combinado_isolado/ (seção 62/66) já tem 5
  repetições de "combinado" nas 9 células (mesma definição -- 40 agregado
  de tráfego normal, condição "combinado" do desempate) -- NENHUM
  experimento novo foi necessário, só leitura do dado já coletado.

Criado:
  - plot_combinado_repetido_instancias.py: mesma estrutura do gráfico 22
    (série temporal, grade 3x3, CPU+instâncias), lendo
    experiment_results/desempate_combinado_isolado/ condição "combinado".
    Mesmo critério do gráfico 27/29-isolado: por célula, escolhe a
    repetição mais próxima da MEDIANA das 5 (não sobrepõe todas, não é a
    melhor/pior), com timeout no título como média±desvio (n=5). Salvo
    graficos_apresentacao/29_combinado_repetido_instancias.png.
  - plot_grid_heatmap_combinado.py: mesma estrutura do gráfico 28
    (heatmap, média±desvio por célula, RPS agregado, timeout, máx.
    instâncias), lendo a mesma fonte. Salvo
    graficos_apresentacao/30_grid_heatmap_combinado.png. Nota: escrito à
    mão em vez de copiado com sed a partir de plot_grid_heatmap_isolado.py
    -- uma tentativa inicial com `sed 's/isolado/combinado/g'` trocou
    também o nome do diretório de dados (desempate_combinado_isolado ->
    desempate_combinado_combinado, inexistente) e teria falhado
    silenciosamente se não tivesse sido conferida antes de rodar.

Resultado (agora com trio completo 17/28/30, todos com repetição):
    rps=1  WU=100k-400k: 16.2%-40.1%, quase sem timeout
    rps=5  WU=400k: 143.1%±28.7, 34.1% timeout -- zona de transição instável
    rps=10 WU=200k: 153.0%±15.7, 24.5% timeout -- zona de transição instável
    rps=10 WU=400k: 192.4%±1.4, 97.3% timeout -- colapso consistente, desvio baixo
  Comparando com o par isolado (gráfico 28, seção 69): os valores de CPU
  ficam sistematicamente um pouco mais altos no combinado (esperado, é
  ataque + tráfego normal extra), mas o PADRÃO de onde fica a zona de
  transição instável (WU=400k a partir de rps=5) é o mesmo nos dois --
  consistente com a conclusão já fechada na seção 63 (presença de
  tráfego normal não muda o comportamento qualitativo do ataque).

Verificado:
  - python3 -m py_compile nos 2 scripts -- sem erro.
  - Rodados de ponta a ponta (dado já em disco, sem Docker) -- os 2 PNGs
    inspecionados visualmente, painéis limpos e legíveis.

--------------------------------------------------------------------------------
71) EntCusumZV3.py -- auditoria e correção dos 3 detectores (Entropia,
    Z-score, CUSUM); início da análise estatística formal (plano combinado
    com o usuário)
--------------------------------------------------------------------------------
Contexto: usuário pediu para auditar EntCusumZV3.py (o detector de burst
  por entropia de Shannon + Z-score + CUSUM sobre janela deslizante) antes
  de usá-lo pra análise em escala, e montar um plano organizado.

Achados da auditoria:
  1. BUG REAL no CUSUM: o laço `for rtt in janela['rtt']: cusum_atual =
     max(0, cusum_atual + ...)` reprocessava a janela inteira a cada
     avanço do laço principal -- com overlap_percent=50 (padrão), cada
     amostra aparece em 2 janelas consecutivas, então era somada 2x no
     acumulador. Isso inflava o CUSUM e tornava sua taxa de crescimento
     dependente de overlap_percent (parâmetro que deveria só controlar
     granularidade da janela, não sensibilidade de detecção).
  2. Entropia calculada mas NUNCA usada na decisão final (só Z ou CUSUM
     decidem 'DDoS Burst') -- coluna diagnóstica, não bug, mas decisão
     implícita nunca declarada. Limiar dela (delta_entropia_threshold=1.5)
     hardcoded, diferente de z_threshold/baseline_ratio (parâmetros).
  3. Faixa dos bins do histograma de entropia recalculada a cada janela a
     partir do próprio min/max da janela -- torna a entropia não
     comparável entre janelas diferentes.
  4. Função hardcoded para colunas 'timestamp'/'rtt' -- não dava pra
     reusar pra analisar CPU (simulation_metrics.csv usa
     'elapsed_time_s'/'average_cpu_percent').

Decisões combinadas com o usuário (AskUserQuestion): aplicar as 3
  correções técnicas; manter entropia só como diagnóstico (não entrar na
  decisão de burst, por enquanto -- evita piorar taxa de falso-positivo
  sem medir cada sinal isoladamente primeiro).

Alterado em EntCusumZV3.py (confirmado antes: 'Status Burst', 'Média RTT',
  'Janela (MM:SS.s)' são nomes de coluna lidos literalmente por
  generate_presentation_graphs.py, wedos_graphs.py,
  wedos_ent_cusum_report.py -- mantidos idênticos com os defaults):
  - CUSUM precomputado 1x por amostra bruta em ordem temporal (array
    `_cusum_running` no df), ANTES do laço de janelas -- cada janela só
    LÊ o valor já acumulado na última amostra dentro dela, não reprocessa.
  - Faixa do histograma de entropia fixada no min/max do ARQUIVO INTEIRO
    (não da janela individual, e não só do baseline -- usar só baseline
    faria np.histogram descartar silenciosamente picos de RTT/CPU fora da
    faixa "normal" durante o ataque, distorcendo a entropia bem no
    momento que mais interessa medir).
  - Função generalizada: novos parâmetros time_col='timestamp',
    value_col='rtt', value_label='RTT', value_unit='ms',
    min_samples_per_window=10 (defaults idênticos ao comportamento
    anterior) -- permite reusar para CPU passando
    time_col='elapsed_time_s', value_col='average_cpu_percent' (ou
    'host_cpu_percent'), com janela maior (~50-60s) e
    min_samples_per_window menor, já que CPU tem só 1 amostra a cada
    ~5s (MONITOR_INTERVAL_SECONDS) contra uma linha por requisição do RTT.
  - Função agora RETORNA df_res (antes só salvava em disco) -- permite uso
    em lote/scoring sem reabrir o xlsx logo em seguida.

Achado adicional durante o teste (antes de rodar em escala): rtt_log.csv
  na verdade MISTURA RTT de tráfego normal E de ataque no mesmo arquivo
  (traffic_injectorV0.py importa e grava no mesmo `rtt_measurements_total`
  de normal_traffic.py) -- bom pra realismo (é o que um detector real
  veria, misturado), mas revelou um problema estrutural pro cenário
  ISOLADO (--normal-rps 0, sem tráfego normal): como nada roda antes do
  ataque começar nesse cenário, o PRIMEIRO timestamp do arquivo já é do
  próprio ataque -- e como EntCusumZV3 computa "tempo relativo" a partir
  do primeiro timestamp do PRÓPRIO ARQUIVO (não do início real da
  simulação), a janela que deveria ser "baseline pré-ataque" (tempo_rel <
  ATTACK_START_TIME_SECONDS) acaba sendo, na verdade, já os primeiros
  segundos do ataque -- contaminando a baseline (ex.: baseline
  "pré-ataque" medida em 1071ms de RTT, quando deveria ser baixa) e
  inflando o piso de burst a ponto de nunca mais ser cruzado. Confirmado
  lado a lado: mesma célula (rps=10/WU=400000), isolado deu 0 bursts
  detectados, combinado deu 6/7 -- com o mesmo detector corrigido.
  DECISÃO (combinada com o usuário): análise de RTT restrita a cenários
  com tráfego contínuo desde t=0 (combinado, normal_baseline,
  clients_rps_grid) -- isolado fica de fora da análise de RTT (não é
  bug a corrigir, é limitação estrutural do próprio dado: não existe RTT
  "de antes" quando não há tráfego nenhum antes do ataque). CPU
  (simulation_metrics.csv) não sofre desse problema, pois é gravada a
  cada tick independente de haver tráfego.

Criado -- Fase 2+4 do plano (TPR no cenário combinado, 5 reps/célula,
  dado já existente do experimento de desempate seção 62/66):
  - run_burst_detection_combinado.py: roda o detector corrigido sobre as
    9 células x 5 repetições de experiment_results/desempate_combinado_isolado/
    (condição "combinado"), calcula taxa de detecção dentro da janela real
    de ataque (proxy de TPR) e fora dela (proxy de FPR, versão inicial).
  - rescore_deteccao_combinado.py: reprocessa o resultado (lê os .xlsx já
    salvos, não roda o detector de novo) com uma métrica mais honesta --
    achado ao inspecionar o resultado bruto: quase toda repetição tinha 1
    "falso positivo" pré-ataque que na verdade não era falso positivo
    nenhum -- cada janela do detector tem window_seconds=20s de largura e
    desliza só stride_seconds=10s (overlap 50%), e o "tempo" reportado é a
    MÉDIA do tempo_rel das amostras dentro da janela, não o início dela --
    então uma janela cujo centro reportado cai a t=17s já fisicamente
    contém uma fração real de tráfego de ataque (que começa em t=20s).
    Reclassificado em 3 categorias (dentro do ataque / zona de transição
    de ±window_seconds das bordas / falso positivo real fora da
    transição) em vez de 2. Achado adicional: a métrica de "falso
    positivo real" ficou estruturalmente vazia (NaN) em quase toda
    célula -- não por erro, mas porque janela de ataque [20,160] + margem
    de ±20s cobre [0,20]∪[140,180], ou seja, a simulação inteira de 180s
    já está dentro do ataque ou da vizinhança imediata dele; não sobra
    nenhum trecho "seguramente livre de contaminação" nesse dataset pra
    medir FPR de verdade -- por isso o gráfico final ficou só com o
    painel de TPR (removido o painel de FPR vazio), com a contagem de
    "janelas na cauda pós-ataque" anotada como contexto (efeito de
    recuperação lenta, não erro do detector). Salvo
    graficos_apresentacao/31_deteccao_combinado_tpr_fpr.png e
    experiment_results/desempate_combinado_isolado/resumo_deteccao_combinado_v2.csv.

  Resultado TPR (cenário combinado, 5 reps/célula):
    rps=1  WU=100k/200k: 0% (esperado -- CPU nunca cruza o piso de burst,
      ataque leve demais nesses WU)
    rps=1  WU=400k: 24%±33 (zona de transição, consistente com o resto da
      sessão -- às vezes escala/colapsa, às vezes não)
    rps=5  WU=100k: 8%±9 (ataque fraco, quase não detectável -- esperado)
    rps=5  WU=200k/400k, rps=10 (todos WU): 98-100% -- detecção quase
      perfeita nos cenários onde o sistema realmente colapsa.

Criado -- Fase 3 do plano (FPR de verdade, tráfego 100% normal sem
  ataque, dado já existente do grid clients_rps_grid seção 38-42):
  - run_burst_detection_clients_grid.py: roda o detector corrigido sobre
    os 160 rtt_log_*.csv de experiment_results/clients_rps_grid/, cruza
    com o critério MECÂNICO de detecção de soluço já usado em
    plot_clients_rps_invariance.py (menos linhas de worker-stop no
    normal_traffic_summary_log do que clientes configurados) -- pergunta
    central: o detector confunde o soluço transitório (não é ataque) com
    um burst real? Salvo
    graficos_apresentacao/32_falso_positivo_soluco.png e
    experiment_results/clients_rps_grid/resumo_deteccao_clients_grid.csv.

  Resultado (achado forte, favorável ao detector):
    execuções LIMPAS (151 de 160): 0% tiveram qualquer alarme falso --
      especificidade perfeita em tráfego saudável.
    execuções COM SOLUÇO (9 de 160): só 11.1% (1 de 9) disparou algum
      alarme -- na maioria dos casos (8/9) nem o soluço engana o
      detector.

Verificado:
  - python3 -m py_compile em EntCusumZV3.py e nos 4 scripts novos -- sem
    erro.
  - Teste dirigido (2-3 casos conhecidos, isolado vs combinado da mesma
    célula) rodado ANTES da análise em escala, revelou o problema de
    baseline do isolado a tempo de ajustar o escopo, em vez de rodar tudo
    e descobrir depois.
  - run_burst_detection_combinado.py (45 execuções do detector, sem
    Docker) e run_burst_detection_clients_grid.py (160 execuções do
    detector, ~2min35 de processamento) rodados de ponta a ponta, sem
    erro -- CSVs e PNGs finais inspecionados.
  - Pendente (próxima etapa combinada com o usuário): Fase 5 (re-rodar
    sobre wedos_grid/combined_sweep, o dataset original do achado "burst
    não detectado" citado no CLAUDE.md) e Fase 6 (consolidação final).

--------------------------------------------------------------------------------
72) Fase 5+6 do plano de análise estatística concluídas -- detecção por
    métodos estatísticos consolidada
--------------------------------------------------------------------------------
Criado run_burst_detection_wedos_final.py -- re-roda o detector CORRIGIDO
  (seção 71) sobre TODOS os 106 rtt_log_*.csv de
  experiment_results/wedos_grid/ (82) e experiment_results/combined_sweep/
  (24), o dataset original do achado documentado no CLAUDE.md ("W-EDoS
  attack overlaid on normal traffic... completely undetected"). Para cada
  execução: lê o SCALE_UP real e a janela real de ataque direto do
  simulation_metrics.csv (coluna 'label'=='attack' -- mais confiável que
  confiar em config.ATTACK_START_TIME_SECONDS/PULSE_DURATION hardcoded,
  que já mudaram entre sessões), compara o nº de bursts do detector NOVO
  com o nº já salvo em disco pelo detector ANTIGO (com o bug do CUSUM,
  rtt_bursts_*.xlsx pré-existentes). Salvo
  experiment_results/resumo_deteccao_wedos_combined_final.csv e
  graficos_apresentacao/33_deteccao_wedos_antes_depois.png.

ACHADO PRINCIPAL DA FASE 5 (resultado limpo e decisivo): das 106
  execuções comparadas, a correção do bug do CUSUM não mudou o nº de
  bursts detectados em NENHUMA delas (0 diferenças, conferido célula a
  célula). Investigada a causa: inspecionando um caso concreto
  (S3_atk1pct_wu300000_stage1a_rep1, SCALE_UP real mas 0 bursts nos dois
  detectores) -- o Z-score dispara 'Sim' em quase toda janela (valores de
  6-23, bem acima do limiar 3.5), ou seja, o "(Z OR CUSUM)" da decisão já
  estava satisfeito de sobra; o que bloqueia 'DDoS Burst' é o OUTRO termo
  do AND -- a Média RTT da janela (34-50ms) nunca ultrapassa o piso de
  burst (baseline×1.8). CONCLUSÃO: o "burst não detectado" documentado no
  CLAUDE.md NÃO era causado pelo bug do CUSUM -- é um limite estrutural
  genuíno do critério combinado (RTT médio > piso E (Z OU CUSUM)): quando
  o ataque é diluído o suficiente pra não elevar a MÉDIA da janela acima
  do piso (efeito de diluição do W-EDoS furtivo, já documentado como
  característica esperada, não bug, em changes.txt §27), nenhuma correção
  de CUSUM resolve, porque o "AND" com o piso de RTT bloqueia a decisão
  independente do CUSUM estar certo. Resultado final:
    Detector ANTIGO (com bug): 37/77 execuções com SCALE_UP real
      completamente perdidas (0 bursts).
    Detector NOVO (corrigido): 37/77 -- EXATAMENTE igual.
  Isso reforça (com mais rigor metodológico, detector agora auditado e
  corrigido) que a furtividade do W-EDoS diluído em tráfego normal é um
  limite estrutural do detector clássico baseado em média de RTT por
  janela -- não um artefato de implementação -- fortalecendo, não
  enfraquecendo, o achado central da tese.

Fase 6 -- consolidação final da análise estatística (CUSUM/Entropia/
  Z-score), resumo dos 3 resultados obtidos nas seções 71-72:
  1. TPR no cenário combinado (gráfico 31, desempate, 5 reps/célula):
     98-100% de detecção nos cenários que realmente colapsam (rps=5-10,
     WU=200k-400k); 0-24% nos cenários leves demais pra cruzar o piso de
     burst (esperado, não é falha).
  2. FPR em tráfego 100% limpo (gráfico 32, clients_rps_grid, 160
     execuções): 0% de alarme falso em 151 execuções saudáveis; só 11%
     (1/9) das execuções com soluço transitório engana o detector.
  3. Robustez do achado de furtividade (gráfico 33, wedos_grid/
     combined_sweep, 106 execuções, antes×depois da correção): 37/77
     execuções com SCALE_UP real continuam completamente invisíveis ao
     detector mesmo corrigido -- não é bug, é o efeito de diluição do
     RTT médio já conhecido.
  EntCusumZV3.py está, portanto, CONSOLIDADO: auditado, com o bug real
  corrigido, testado em 3 datasets diferentes (311 execuções no total
  entre as 3 fases, nenhuma nova no Docker) cobrindo os 3 ângulos
  relevantes (sensibilidade a ataque real, especificidade contra tráfego
  limpo/soluço, e validação do achado de furtividade documentado). Pronto
  para uso como baseline de comparação e/ou fonte de features no dataset
  de ML (próxima etapa, combinada com o usuário).

Verificado:
  - python3 -m py_compile run_burst_detection_wedos_final.py -- sem erro.
  - Rodado de ponta a ponta (106 execuções do detector, ~1min20, sem
    Docker) -- CSV e PNG finais inspecionados; diferença antigo×novo
    conferida numericamente (0 linhas diferentes) antes de reportar a
    conclusão, não só por inspeção visual do gráfico.

--------------------------------------------------------------------------------
73) plot_wedos_antes_depois_v2.py (novo, gráfico 34) -- gráfico 33 tinha
    77 barras ilegíveis, refeito como heatmap agregado
--------------------------------------------------------------------------------
Usuário pediu para melhorar a apresentação dos resultados de detecção
  estatística antes de seguir pra ML. Achado: o gráfico 33
  (run_burst_detection_wedos_final.py) tinha 77 barras individuais no
  eixo X, rótulos em fontsize=6, ilegível numa apresentação -- problema
  de FORMA (não de cor), corrigido agregando por cenário em vez de
  reduzir fonte.

Criado plot_wedos_antes_depois_v2.py -- lê o mesmo CSV já calculado
  (resumo_deteccao_wedos_combined_final.csv, nenhum reprocessamento do
  detector), agrupa por (cenário de fundo S1-S4) × (intensidade do ataque
  1%/5%/10%, extraída do nome do cenário via regex) -- mesmo estilo visual
  dos heatmaps 21/28/30. Cada célula mostra % de ataques com SCALE_UP real
  detectados, com anotação explícita "idêntico ao antigo" célula a célula
  (não só um resumo geral no título). Salvo
  graficos_apresentacao/34_wedos_antes_depois_agregado.png -- NÃO
  sobrescreve o gráfico 33 original.

Resultado (muito mais legível, e o padrão de furtividade salta aos olhos):
  intensidade 1% (a coluna furtiva): 0%, 0%, 43%, 17% detectado em
  S1/S3/S2/S4 respectivamente -- quase sempre vermelho no heatmap.
  Intensidade 5-10%: 56-100% detectado -- quase sempre verde. O contraste
  visual entre as colunas é a prova mais direta e imediata do achado
  central (furtividade é sobre proporção, não sobre o ataque em si).

Nota sobre paleta: tentei rodar o validador de paleta da skill dataviz
  (scripts/validate_palette.js) mas node não está instalado neste
  ambiente -- segui com a paleta categórica já usada em 30+ gráficos
  desta sessão (aprovada implicitamente pelo uso repetido), e para este
  heatmap especificamente usei RdYlGn (sequencial semântico, vermelho=
  ruim/verde=bom), apropriado para a natureza "está indo bem ou mal" da
  métrica (% detectado), não uma paleta categórica.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta (dado já em disco) -- PNG inspecionado,
    12 células legíveis, nenhuma sobreposição de texto.

--------------------------------------------------------------------------------
74) Relatório consolidado (Artifact) atualizado com a seção completa de
    detecção por métodos estatísticos
--------------------------------------------------------------------------------
Segunda parte do pedido do usuário (seção 73): trazer a investigação
  inteira do EntCusumZV3.py (seções 71-72, e o gráfico 34 da seção 73)
  pro Artifact consolidado -- até então essa investigação só existia no
  histórico de chat, nunca tinha virado parte do documento visual.

Alterado (mesmo Artifact das seções 44/51/53/57/61/65):
  - Nova seção 09 "Detecção por métodos estatísticos": o que cada método
    mede (entropia=forma da distribuição, Z-score=mudança brusca,
    CUSUM=desvio sustentado), resumo da auditoria/correção do
    EntCusumZV3.py (bug do CUSUM, faixa de entropia, achado do rtt_log
    misturando normal+ataque que quebra o isolado), os 3 gráficos
    (31=sensibilidade, 32=especificidade, 34=robustez da furtividade
    agregada -- não o 33 com as 77 barras ilegíveis), e a interpretação
    do que isso significa pro TCC.
  - Seção "Estado atual" (08): removido o item "cenário combinado ainda
    não executado" (já resolvido, seção 70), substituído por um ponteiro
    pra nova seção 09.
  - Seção "Onde investir se der tempo": removido o item do cenário
    combinado (já feito), adicionado item sobre treinar/comparar ML
    contra o baseline clássico (próxima etapa combinada com o usuário).
  - Seção "Base científica para o TCC" (renumerada de 08 para 10):
    adicionado item sobre o baseline clássico consolidado (311 execuções
    cobrindo os 3 ângulos) antes de virar comparação para o ML.
  - Footer atualizado (fontes de dados + changes.txt §36-§73).

Verificado:
  - As 3 imagens novas (31, 32, 34) embutidas em base64, nenhum marcador
    "{{IMG_*}}" faltando (checado programaticamente antes de publicar).
  - Artifact republicado no mesmo link (histórico de versões preservado).

--------------------------------------------------------------------------------
75) plot_deteccao_por_metodo.py (novo, gráficos 35/36/37) -- sensibilidade
    de cada método (Entropia/Z-score/CUSUM) ISOLADAMENTE, sem o piso de RTT
--------------------------------------------------------------------------------
Ideia do usuário: em vez de só a decisão combinada (Status Burst = piso de
  RTT E (Z OU CUSUM), gráficos 31/34), fazer um gráfico por método,
  medindo a sensibilidade de cada um SOZINHO. Viável sem reprocessar nada:
  os .xlsx já salvos (desempate_combinado_isolado + wedos_grid/
  combined_sweep, 151 arquivos) já têm as colunas 'Al. Ent.', 'Al. Z',
  'Al. CUSUM' calculadas separadamente -- só nunca tinham sido usadas
  isoladamente.

Criado plot_deteccao_por_metodo.py -- para cada um dos 3 sinais, calcula
  TPR (janelas de ataque real marcadas 'Sim' NAQUELA coluna, sem exigir
  RTT acima do piso) nos dois datasets já usados (desempate combinado
  rps×WU, e wedos_grid+combined_sweep agregado por S1-S4×intensidade).
  Salvo graficos_apresentacao/35_sensibilidade_alent.png,
  36_sensibilidade_alz.png, 37_sensibilidade_alcusum.png.

ACHADO PRINCIPAL (revisa a interpretação da furtividade -- mais forte, não
  mais fraco, para o TCC): sem o piso de RTT, Z-score e CUSUM sozinhos
  detectam MUITO mais que a decisão combinada nos casos furtivos:
    Entropia sozinha: ~0% em quase toda célula (confirma que sozinha não
      serve pra decidir nada -- decisão de mantê-la só diagnóstica estava
      certa).
    Z-score sozinho: 79-100% em quase toda célula do wedos, INCLUSIVE na
      coluna de 1% de intensidade (93%/97%/87%/93% em S1-S4) -- onde a
      decisão combinada (gráfico 34) só dava 0%/0%/43%/17%.
    CUSUM sozinho: 23-100%, também muito mais sensível que a decisão
      combinada nos casos furtivos, um pouco mais fraco que Z-score em
      alguns cenários (S1/1%=29%, S3/1%=23%).
  CONCLUSÃO: a furtividade do W-EDoS diluído não é porque o sinal
  estatístico desaparece no RTT -- Z-score e CUSUM continuam captando o
  desvio bem, mesmo diluído. A "cegueira" documentada nas seções 71-72
  vem especificamente do PISO DE RTT (média da janela precisa cruzar um
  valor absoluto antes de qualquer alarme contar) -- um freio conservador
  demais que descarta sinais que os outros 2 métodos já tinham capturado
  corretamente. Isso aponta uma melhoria concreta e testável no próprio
  detector clássico (relaxar/remover o piso), não só "o ML é melhor".

Ressalva (não medida ainda, próximo passo natural se quiser aprofundar):
  isso é só sensibilidade (TPR) -- falta medir se remover o piso de RTT
  aumenta o falso-positivo em tráfego limpo (rodar os mesmos sinais
  isolados contra clients_rps_grid, que já tem o gráfico 32 de FPR com a
  decisão combinada).

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta (151 arquivos .xlsx já existentes, ~28s, sem
    Docker nem reprocessamento do detector) -- 3 PNGs inspecionados
    visualmente.

================================================================================
§76. FPR por método individual (sem piso de RTT) contra tráfego 100% limpo
================================================================================
Contexto: fechando a ressalva deixada em aberto na §75 -- os sinais
  individuais (Al. Z / Al. CUSUM, sem o piso de RTT) são muito mais
  sensíveis que a decisão combinada em cenários furtivos, mas isso tem
  custo em falso-positivo?

Criado plot_fpr_por_metodo.py -- reaproveita os mesmos 160 .xlsx já
  salvos em experiment_results/clients_rps_grid/ (tráfego 100% normal,
  sem nenhum ataque real -- o mesmo dataset do gráfico 32), cruza com o
  soluço mecânico já identificado (comparação de contagem de linhas de
  parada de worker em normal_traffic_summary_log_*.csv contra o número
  de clientes esperado), e mede, para cada método isolado, o % de
  execuções com pelo menos 1 alarme falso (df[col]=='Sim' em qualquer
  janela). Nenhum reprocessamento do detector, nenhum experimento novo.
  Salvo graficos_apresentacao/41_fpr_por_metodo.png (3 painéis:
  Entropia, Z-score, CUSUM; verde=limpo, vermelho=soluço, mesma
  convenção de cor do gráfico 32).

RESULTADO:
                Al. Ent.     Al. Z  Al. CUSUM
    limpo (n=151)   0.0%     42.4%      19.9%
    soluço (n=9)     0.0%    100.0%       0.0%

  Comparar com a decisão combinada original (gráfico 32): 0% limpo /
  11.1% soluço.

INTERPRETAÇÃO:
  - Entropia sozinha: 0% nos dois grupos -- inerte também para falso
    positivo, mesmo problema já visto na §75 (não reage a nada).
  - Z-score sozinho: custo MUITO alto -- 42.4% de falso-positivo em
    tráfego 100% limpo, e 100% nas runs com soluço (o soluço é quase
    sempre lido como ataque por esse sinal isolado). Confirma que o
    piso de RTT não está só bloqueando detecção furtiva (§75) -- ele é
    o que mantém o Z-score isolado utilizável no dia a dia.
  - CUSUM sozinho: custo moderado (19.9% limpo), mas com padrão
    INVERTIDO no soluço (0%, contra 100% do Z-score) -- sugere que o
    soluço é um salto abrupto/pontual (aciona a comparação janela-a-
    janela do Z-score) mas não um desvio acumulado sustentado (não
    aciona o CUSUM). Leitura plausível, não comprovada de forma
    definitiva -- vale citar no TCC como pista sobre a natureza do
    soluço, com essa ressalva.

CONCLUSÃO: remover o piso de RTT não é ganho gratuito -- é um trade-off
  genuíno entre sensibilidade (TPR furtivo, §75) e especificidade (FPR
  em tráfego limpo, aqui). Isso NUANCIA a conclusão da §75 (o piso
  continua sendo o gargalo estrutural da cegueira furtiva), mas não a
  reverte: um meio-termo (piso relaxado, ou regra que exija concordância
  entre Z-score e CUSUM) é o próximo candidato de design a explorar, se
  for decidido ir nessa direção -- não implementado, decisão em aberto.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta (160 arquivos .xlsx já existentes, sem
    Docker nem reprocessamento do detector) -- PNG inspecionado
    visualmente, cores corrigidas para manter a convenção
    verde=bom/limpo, vermelho=ruim/soluço (mapeamento por categoria via
    pandas .map, não por posição no eixo x -- o groupby ordena False
    antes de True, então cor-por-posição teria invertido a convenção).

================================================================================
§77. Reorganização de graficos_apresentacao/ em subpastas por tema
================================================================================
Contexto: 42 PNGs acumulados soltos na raiz de graficos_apresentacao/ ao
  longo de várias sessões, sem nenhuma estrutura -- difícil de navegar.

Criadas 3 subpastas e movidos os 42 arquivos, por tema (não por número):
  - 01_curva_capacidade_baseline/ (17 arquivos): caracterização do
    simulador SEM ataque -- curva de capacidade, calibração de WU,
    invariância RPS×clientes, prova de saturação do host. Gráficos
    04(×3 variantes)/05(×3)/11-20/23.
  - 02_caracterizacao_ataque_wedos/ (15 arquivos): comportamento do
    simulador SOB ataque -- mapeamento de fronteira de falha, W-EDoS
    (fase 3 da suíte wedos_*.py), isolado×combinado, desempate. Gráficos
    01/06-10/21/22/24-30.
  - 03_deteccao_estatistica/ (10 arquivos): entropia/Z-score/CUSUM,
    sensibilidade e falso-positivo. Gráficos 02/03/31-37/41.
  Numeração dos arquivos preservada (só a pasta mudou) -- referência
  cruzada com este changes.txt continua válida.

Atualizado OUT_DIR em ~30 scripts geradores (plot_*.py,
  run_burst_detection_*.py, wedos_graphs.py, rescore_deteccao_combinado.py,
  regenerate_graficos_04_05_media.py) para apontar para a subpasta
  correta -- para que reruns futuros não voltem a salvar solto na raiz.
  Caso especial: generate_presentation_graphs.py gera 3 gráficos em 2
  pastas diferentes (01 -> ataque_wedos, 02/03 -> deteccao_estatistica),
  então virou OUT_DIR_ATAQUE + OUT_DIR_DETECCAO em vez de um OUT_DIR só;
  também corrigida a f-string do print final, que ainda referenciava o
  OUT_DIR antigo (teria quebrado com NameError na próxima execução).

Dois arquivos sem gerador no repo hoje (04_curva_capacidade_reproducibilidade.png
  e 05_wu_calibration_resumo_atual.png, mencionados só em comentários de
  outros scripts como "o original que não sobrescrevemos") foram movidos
  como estão para 01_curva_capacidade_baseline/, sem nenhum OUT_DIR para
  atualizar.

Não alterado: run_burst_analysis.py (escreve .xlsx dentro de
  experiment_results/, não em graficos_apresentacao/ -- fora do escopo
  desta reorganização). O Artifact consolidado já publicado não é afetado
  (imagens embutidas como base64 no HTML publicado, não referenciam o
  caminho do arquivo em disco) -- só as legendas em texto (figcaption)
  ainda citam o caminho antigo (cosmético, não quebra nada).

Verificado:
  - ls graficos_apresentacao/*.png (raiz) -- vazio, 0 arquivos.
  - Contagem por pasta: 17 + 15 + 10 = 42, bate com o total original.
  - python3 -m py_compile em todos os ~30 scripts tocados -- sem erro.
  - grep confirmando que nenhum script ainda tem
    OUT_DIR = "graficos_apresentacao" (raiz, sem subpasta).

================================================================================
§78. Gráfico exploratório: quartis (IQR) sobrepostos ao gráfico 23
================================================================================
Contexto: usuário pediu uma demonstração visual de como ficaria o gráfico
  23 (curva de capacidade pow2 base 20) com quartis desenhados por cima,
  "só para ver como fica" -- depois de já ter concordado (changes.txt §76
  e memória feedback_no_iqr_outliers) em não usar IQR como filtro
  automático.

Criado plot_graph4_pow2_base20_quartis.py -- reaproveita a mesma leitura
  de dados do gráfico 23 (normal_baseline, mesmo critério de exclusão de
  runs totalmente vazias), adiciona um boxplot de Tukey (Q1/Q3/mediana/
  bigodes 1.5xIQR) por agregado, mantém o scatter colorido por SCALE_UP
  real do gráfico 23, e desenha um anel vermelho em cada ponto que o IQR
  classificaria como outlier. Não sobrescreve o gráfico 23. Salvo
  graficos_apresentacao/01_curva_capacidade_baseline/
  42_curva_capacidade_pow2_base20_quartis.png.

RESULTADO: IQR marcaria 4 outliers no total (de 57 pontos válidos):
  agg=40 (13.88%, não escalou -- único caso de ruído puro), agg=160
  (79.05%, escalou=True), agg=320 (127.82%, escalou=True), agg=640
  (60.03%, escalou=True). 3 dos 4 já tinham SCALE_UP real -- confirma
  visualmente, com um dataset ainda não testado antes (pow2 base 20),
  o mesmo padrão da memória feedback_no_iqr_outliers: a "cauda" que o
  IQR marca como estranha é, na maioria das vezes, a própria distribuição
  bimodal real perto do limiar de 60%, não erro de medição.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta (dados já existentes, nenhum experimento
    novo) -- PNG inspecionado visualmente.

================================================================================
§79. Auditoria de robustez + legibilidade da seção de detecção estatística
================================================================================
Contexto: usuário pediu para "deixar robusto" e "sem pontas soltas" antes
  de seguir pra ML -- garantia de que os dados e gráficos da seção de
  métodos estatísticos (31-37, 41, 45) estão corretos e legíveis.

AUDITORIA -- spot-check independente (recalculado do zero, sem reusar
  código dos scripts originais):
  - Gráfico 37 (CUSUM isolado), célula S3/1%: recalculado = 23.2%,
    gráfico mostra 23% -- BATE.
  - Gráfico 34 (v2), célula S4/10%: recalculado pela métrica "janela
    dentro do ataque" = 0.0%, gráfico mostrava 100% -- DIVERGIU.
    Investigado: não é bug, é que o gráfico 34 usa uma métrica diferente
    dos gráficos 35-37 (execução inteira: "≥1 alarme em qualquer lugar
    do run de 180s" vs. janela: "% de janelas dentro do período real de
    ataque"). As duas são internamente corretas, mas misturar as duas
    definições de "detectado" na mesma seção sem aviso é uma ponta solta
    real -- risco de comparação incorreta entre gráficos numa banca.

CORREÇÃO (aprovada pelo usuário via pergunta explícita: "recalcular com
  a métrica rigorosa"):
  - run_burst_detection_wedos_final.py: adicionado cálculo de
    `tpr_antigo` (mesma lógica de `tpr_novo`, já existente, só não
    salva) a partir do old_df (detector com bug), usando a mesma janela
    real de ataque. Recolunado no CSV
    experiment_results/resumo_deteccao_wedos_combined_final.csv.
  - Criado plot_wedos_antes_depois_v3.py -- gráfico 45, heatmap S1-S4 x
    intensidade usando tpr_novo/tpr_antigo (métrica por janela, IDÊNTICA
    à dos gráficos 35-37) em vez da métrica por execução do gráfico 34.
    Não sobrescreve nem o 33 nem o 34.
  - RESULTADO: com a métrica rigorosa, a maior diferença antigo×novo em
    todas as 11 células comparáveis = 0.00 pontos percentuais -- o
    achado "correção do CUSUM não muda o resultado" (§72) se CONFIRMA
    de novo, agora numa base metodológica consistente com 35-37.

LEGIBILIDADE -- pedido explícito do usuário: inverter a barra de cor dos
  heatmaps para o valor mais alto/escuro ficar embaixo (em vez do padrão
  do matplotlib, que põe o valor mais alto em cima). Aplicado
  `cbar.ax.invert_yaxis()` em:
  - rescore_deteccao_combinado.py (gráfico 31)
  - plot_deteccao_por_metodo.py (gráficos 35, 36, 37 -- 2 colorbars cada)
  - plot_wedos_antes_depois_v3.py (gráfico 45, já criado com a inversão)

Também fechada a ambiguidade dos eixos S1-S4 apontada em conversa
  anterior: os heatmaps que usam esse eixo agora mostram
  "S1 (40 rps normal)" etc. diretamente no rótulo do eixo Y, e os
  títulos/subtítulos dos painéis "rps/atacante" vs. "S1-S4" deixam
  explícito que são grandezas diferentes (um é intensidade do próprio
  atacante, o outro é o nível de fundo de tráfego normal).

Gráfico 34 (v2, métrica por execução) e 33 (original, 77 barras) NÃO
  foram apagados -- ficam como registro histórico, mas o gráfico 45 é a
  versão a citar no TCC daqui pra frente por usar a métrica mais
  rigorosa e consistente com o resto da seção.

Verificado:
  - python3 -m py_compile em todos os scripts tocados -- sem erro.
  - Todos os scripts rodados de ponta a ponta, sem Docker, reaproveitando
    dados já existentes.
  - PNGs inspecionados visualmente -- colorbars confirmadas invertidas
    (0 em cima, 100 embaixo) em 31/35/36/37/45.

================================================================================
§80. Gráfico 36 (e 31, 35, 37): "sem dado" → n_escalou/n_total explícito
================================================================================
Contexto: usuário achou confuso o "sem dado" do gráfico 36 misturado com
  os números de repetição -- pediu pra mostrar quantas repetições do
  total realmente escalaram em vez de um "sem dado" opaco.

Ao implementar, achada uma inconsistência adicional: o painel esquerdo
  (desempate rps×WU) dos gráficos 35-37 NÃO filtrava por SCALE_UP real
  -- media sensibilidade mesmo em células onde o ataque nunca chegou a
  causar dano de verdade (ex.: rps=1/WU=100.000, 0/5 repetições
  escalaram, mas a célula mostrava "11% (n=5)" como se fosse um
  resultado válido). O painel direito (wedos) já filtrava. O mesmo
  problema existia no gráfico 31 (nunca filtrava por SCALE_UP real).

CORRIGIDO (aprovado pelo usuário em 2 confirmações):
  - plot_deteccao_por_metodo.py: build_desempate_matrix e
    build_wedos_matrix agora retornam n_escalou E n_total separados
    (antes só um "n" ambíguo). draw_heatmap trocou "sem dado" por
    "X/Y escalaram\n(sem dano real)" nas células sem SCALE_UP real, e
    toda célula com dado passou a mostrar "Z% (X/Y escalaram)" em vez de
    "Z% (n=X)". Painel esquerdo agora também filtra por SCALE_UP real,
    igual ao direito -- os dois painéis respondem à mesma pergunta.
  - rescore_deteccao_combinado.py (gráfico 31): mesmo tratamento --
    n_escalou/n_total calculados e anotados, célula rps=1/WU=100k passa
    de "0%±0 (n=5)" para "0/5 escalaram (sem dano real)". Figura alargada
    (9x7 -> 10x7.3) e título encurtado/refeito em 2 linhas -- o título
    antigo estava sendo cortado na borda esquerda com o texto mais longo.

Verificado:
  - python3 -m py_compile -- sem erro nos 2 scripts.
  - Rodados de ponta a ponta (dados já existentes, sem Docker).
  - PNGs de 31, 35, 36, 37 inspecionados visualmente -- anotação nova
    confirmada em toda célula, título do 31 sem corte.

================================================================================
§81. Gráfico 46 -- trade-off sensibilidade × falso-positivo (scatter, não heatmap)
================================================================================
Contexto: usuário perguntou "por que não trocar o E por OU na decisão
  combinada" e pediu confirmação de que os testes já cobrem falso-
  positivo -- pediu um gráfico único mostrando sensibilidade x falso-
  positivo pras 5 regras discutidas (combinado atual, entropia sozinha,
  CUSUM sozinho, Z-score sozinho, e a regra nova "Z OU CUSUM" -- a
  combinação exata proposta pelo usuário, nunca testada como par antes,
  só cada sinal isoladamente).

Calculado pela primeira vez o TPR/FPR da regra "Z-score OU CUSUM" (sem
  piso de RTT nenhum) -- não testada antes como combinação, só isolada:
  TPR médio (1% intensidade, 30 execuções com dano real confirmado) =
  91.9% (contra 90.7% do Z-score sozinho -- ganho marginal); FPR em
  tráfego limpo = 49.7% (contra 42.4% do Z-score sozinho -- piora);
  FPR com soluço = 100%. Confirma que "OU" nunca é estritamente melhor
  que o mais permissivo dos dois sinais isolados -- só acumula os falso-
  positivos de ambos.

Criado plot_tradeoff_tpr_fpr.py -- gráfico de dispersão (não heatmap,
  forma certa pra comparar 2 métricas contínuas entre poucas categorias):
  eixo X = falso-positivo (tráfego limpo, n=151), eixo Y = sensibilidade
  (execuções furtivas com dano real, n=30), 1 ponto colorido por regra
  (5 regras), com um segundo marcador vazado ligado por linha mostrando
  o FPR nas execuções com soluço (n=9) -- mostra a faixa, não só um
  número. Zona "ideal" (canto superior-esquerdo) e "ruim" (inferior-
  direito) sombreadas como referência de leitura. Salvo
  graficos_apresentacao/03_deteccao_estatistica/
  46_tradeoff_sensibilidade_fpr.png.

Ajustes de legibilidade durante a criação: rótulos de método com posição
  manual (dx, dy, alinhamento) por ponto -- o posicionamento automático
  colidia nos 2 pares de pontos mais próximos (Z-score x Z OU CUSUM no
  topo; Combinado atual x Entropia embaixo); subtítulo reduzido pra não
  cortar na borda direita da figura; legenda reposicionada pra não
  encostar no texto "zona ruim".

RESUMO (os 5 pontos do gráfico):
  Combinado atual:  TPR=2.2%   FPR_limpo=0.0%   FPR_soluço=11.1%
  Entropia sozinha: TPR=0.0%   FPR_limpo=0.0%   FPR_soluço=0.0%
  CUSUM sozinho:    TPR=36.8%  FPR_limpo=19.9%  FPR_soluço=0.0%
  Z-score sozinho:  TPR=90.7%  FPR_limpo=42.4%  FPR_soluço=100.0%
  Z OU CUSUM:       TPR=91.9%  FPR_limpo=49.7%  FPR_soluço=100.0%

CONCLUSÃO visual: nenhuma das 5 regras booleanas fica na "zona ideal"
  (canto superior-esquerdo) -- é sempre um trade-off ao longo de uma
  curva, nunca um ganho livre. Reforça, com um gráfico só em vez de
  vários números soltos em texto, por que uma regra fixa (E ou OU) não
  resolve o problema e por que ML é o próximo passo natural.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta (dados já existentes, sem Docker) -- PNG
    inspecionado visualmente 3 vezes até resolver as colisões de rótulo.

================================================================================
§82. Artifact consolidado: seção "Achado 5" -- limitação teórica na prática
================================================================================
Contexto: usuário pediu pra formalizar, no relatório consolidado, a
  explicação teórica de por que Z-score/CUSUM/entropia se comportam como
  se comportam sob diluição -- não só o "o quê" (achados 3-4), mas o
  "por quê" (fórmula exata + dados reais de execução).

Adicionado "Achado 5" na seção 09 do report_template.html:
  - Tabela com as 19 janelas reais de S3_atk1pct_wu300000 (Média RTT,
    Entropia, Δ Ent., Max Z, Al.Z, CUSUM, Al.CUSUM) -- mostra Status
    Burst "Normal" nas 19/19, mesmo com Z disparando em 17/19.
  - Parágrafos explicando cada método a partir da fórmula exata do
    código (não uma descrição aproximada): Z-score é o MÁXIMO por
    amostra individual contra a janela anterior (não comparação de
    médias); CUSUM reseta a zero via max(0, ...) sempre que a soma
    tentaria ficar negativa; Entropia mede forma da distribuição via
    histograma de 10 bins, não valor típico.
  - Tabela de síntese (método x pressuposição teórica x por que a
    diluição quebra) -- 4 linhas, uma por método incluindo o piso.
  - Callout com a frase-síntese: a furtividade não é falha de
    implementação, é exploração direta da pressuposição estatística de
    cada método.
  - Item novo na lista "O que isso significa para o TCC": "a limitação
    é teórica, não de engenharia".

Republicado o Artifact (mesma URL,
  https://claude.ai/code/artifact/0814988f-4e82-4140-b1c2-1edc25328e7b).
  Ao reconstruir o HTML, corrigido um bug latente: o mapa IMG_XX ainda
  apontava pros caminhos antigos (graficos_apresentacao/XX_...png, raiz)
  de antes da reorganização em subpastas (§77) -- se republicasse sem
  perceber, os embeds de imagem teriam falhado silenciosamente (arquivo
  não encontrado). Corrigido pros caminhos atuais em
  01_curva_capacidade_baseline/ e 03_deteccao_estatistica/.

Verificado:
  - Script de rebuild rodou sem "MISSING MAPPING" nem placeholder
    remanescente.
  - HTML final ~2.64MB, dentro do limite do Artifact.
  - Publicação confirmada na mesma URL (updated, não um artifact novo).

================================================================================
§83. Ajustes nos gráficos 20, 25 e 30 (pedido do usuário)
================================================================================
Contexto: usuário pediu três ajustes pontuais nos gráficos do relatório
  consolidado, cada um verificado com diagnóstico antes de editar.

Gráfico 20 (plot_graph4_pico_pre_escalonamento.py):
  - Fonte de dados trocada de normal_baseline (só RPS agregado) para
    clients_rps_grid (mesmo dataset do gráfico 17 -- RPS agregado x nº de
    clientes), a pedido do usuário. Mantido o estilo scatter+errorbar
    original, agora com 5 linhas (uma por nº de clientes 1/2/4/8/16) em vez
    de uma só.
  - Outliers removidos via IQR (quartis, 1.5x, dentro de cada célula
    agregado x clientes, n~5 reps) -- a pedido EXPLÍCITO do usuário, mesmo
    após eu rodar um teste ao vivo nesse dataset específico (terceiro teste
    de IQR neste projeto, ver feedback_no_iqr_outliers na memória) que
    confirmou o mesmo padrão dos dois testes anteriores: o IQR não distingue
    "segundo modo real de comportamento perto do limiar" de ruído. Nesse
    teste, 11 pontos foram flagados como outlier e 3 deles eram execuções
    que REALMENTE dispararam SCALE_UP (agg=512, clients=2/4/8, na zona-limite
    do limiar de 60%: 76.4%, 99.0%, 72.0%). Esses 3 pontos foram removidos do
    gráfico por decisão do usuário -- documentado em código (comentário +
    nota visual em caixa amarela no próprio gráfico) e aqui, para não passar
    despercebido numa leitura futura. A decisão geral registrada em
    feedback_no_iqr_outliers (não usar IQR por padrão no projeto) continua
    valendo para os outros gráficos -- esta foi uma exceção pontual pedida
    para o gráfico 20 especificamente.

Gráfico 25 (plot_comparacao_completa.py):
  - Adicionado rótulo "nº de instâncias" no eixo direito (twinx) de cada
    painel, que antes só tinha os ticks numéricos sem indicar o que
    representavam.
  - Adicionada linha de limiar SCALE_DOWN (config.CPU_THRESHOLD_SCALE_DOWN,
    25%) em todos os painéis, ao lado da linha de limiar SCALE_UP (60%) já
    existente. Ambas na legenda da figura.
  - Diagnóstico pedido (RPS=1, WU=400.000, por que o pico final não
    escalonou): o pico em t=135s (CPU=64.2%, acima do limiar de 60%) caiu
    dentro do cooldown de 20s (config.SCALE_COOLDOWN_SECONDS) contado a
    partir do SCALE_DOWN em t=125s (cooldown ativo até t=145s) -- o
    autoscaler ignora a decisão nesse intervalo mesmo com CPU acima do
    limiar. Não é bug nem dado ruim, é o mecanismo de cooldown funcionando
    como desenhado (evitar flapping), só que "custando" esse SCALE_UP
    específico. Não alterei o gráfico para marcar isso visualmente (não foi
    pedido), só documentando a explicação aqui.

Gráfico 30 (plot_grid_heatmap_combinado.py):
  - Diagnóstico pedido (RPS/atacante batendo com 1%/5%/10%): confirmado que
    NÃO batem, mas não é bug -- são duas convenções diferentes que
    reaproveitam por coincidência os números 1/5/10. Este gráfico vem de
    experiment_results/desempate_combinado_isolado/ (run_desempate_
    combinado_isolado.py + run_desempate_rps1.py), onde "1/5/10" é o valor
    literal de --rps por atacante (4 atacantes fixos, 40 agregado normal
    fixo) -- diferente da convenção 1%/5%/10% do wedos_grid/combined_sweep,
    onde é fração do agregado normal do cenário S1-S4.
  - Adicionada a % real do agregado normal (40, fixo neste dataset) em cada
    célula, ao lado do rótulo "rps agregado" já existente: rps=1 -> 10% do
    normal, rps=5 -> 50%, rps=10 -> 100%. Deixa a diferença de convenção
    explícita direto no gráfico, sem precisar decorar o contexto.

Verificado:
  - python3 -m py_compile -- sem erro nos 3 scripts.
  - Rodados de ponta a ponta (dados já existentes, sem Docker) -- 3 PNGs
    inspecionados visualmente.

================================================================================
§84. Novo experimento: grid atacantes×intensidade limitado a 25% (triagem)
================================================================================
Contexto: usuário pediu um gráfico novo relacionando intensidade do ataque
  com o tráfego legítimo, variando número de atacantes e RPS, com o teto
  explícito de a intensidade agregada do ataque nunca passar de 25% do
  agregado de tráfego normal. Desenho do grid definido em conjunto com o
  usuário (perguntas feitas, respostas aplicadas):
    - Cenários: S1-S4 (40/100/200/300 agregado normal) -- S4 incluído por
      pedido explícito, mesmo sendo conhecido por já estar acima do teto de
      capacidade do simulador sem ataque nenhum (~205-210 agregado, WU=10)
      -- resultado de S4 vai precisar de nota de cautela na análise.
    - Atacantes: {1, 2, 4}.
    - Intensidade: {1, 5, 10, 15, 20, 25}% do agregado normal do cenário.
    - WU fixo em 200.000 -- não fazia parte do que foi pedido pra variar;
      usei o mesmo valor intermediário de referência de outros gráficos do
      projeto (desempate_combinado_isolado, comparação completa) por não
      ter sido especificado. Avisei o usuário antes de rodar.
    - 1 repetição por célula (triagem, estilo stage1a do wedos_combined_grid)
      -- decisão do usuário, dado o custo de tempo do grid completo com 5
      reps (~20h+ de Docker). Total: 4 cenários x 3 atacantes x 6
      intensidades x 1 rep = 72 execuções, 180s cada (~3,6h de simulação +
      overhead de Docker).

Criado run_atacantes_intensidade_grid.py (não altera nenhum script
  existente -- mesmo padrão de run_point() do wedos_combined_grid.py, mas
  com nº de atacantes como dimensão variável em vez de fixo). Salva em
  experiment_results/atacantes_intensidade_grid/, convenção de nome
  metrics_{cenario}_atk{pct}pct_att{atacantes}_wu{WU}_rep{rep}.csv.

Disparado em background (nohup, PID inicial 75952) -- ainda não tem
  gráfico correspondente; será criado depois que os dados existirem.

Verificado:
  - python3 -m py_compile -- sem erro.
  - docker ps e getcap do tcpdump confirmados OK antes de disparar.
  - Primeira execução do grid (S1, 1 atacante, 1%) iniciou e logou
    corretamente.

================================================================================
§85. Artifact "Atlas W-EDoS" — gráficos selecionados para a monografia
================================================================================
Contexto: usuário pediu ajuda pra organizar os gráficos mais relevantes do
  projeto, com dados de origem, para apoiar a redação da monografia.

Publicado Artifact HTML (não um arquivo local -- vive em claude.ai):
  https://claude.ai/code/artifact/81e0cc22-e025-4eb7-9e57-b7bd5a5e5a8a

Conteúdo: 13 gráficos selecionados dos mais de 40 do projeto (organizados
  nas mesmas 3 seções de graficos_apresentacao/), cada um com "o que
  mostra" (extraído dos comentários reais de cada script, não resumo
  genérico), script gerador + pasta de dados, e uma sugestão de legenda
  pronta pra adaptar no texto. Seleção: 04, 17, 20, 19 (capacidade) / 06,
  07, 25, 30 (caracterização do ataque) / 31, 41, 45, 46, 47 (detecção).
  Inclui link de volta pro relatório consolidado
  (.../artifact/0814988f-4e82-4140-b1c2-1edc25328e7b) pra narrativa
  completa, e uma nota avisando que os gráficos 06 e 45 usam populações
  de execuções diferentes (usar o 47 pra comparação rigorosa entre
  efeito e detecção).

Não foi criado nenhum arquivo local novo além do HTML de origem em
  /tmp (scratchpad, não faz parte do repositório) -- o Artifact publicado
  é a entrega.

================================================================================
§86. Gráfico 48: atacantes × intensidade (teto 25%) — experimento concluído
================================================================================
Contexto: continuação do §84/85 -- as 72 execuções do grid novo terminaram
  (4 cenários S1-S4 x 3 contagens de atacantes x 6 intensidades x 1 rep,
  WU=200.000 fixo, ~3h43min de simulação real).

Criado plot_atacantes_intensidade_grid.py -- heatmap por cenário (S1-S4),
  eixo Y = nº de atacantes (1/2/4), eixo X = intensidade (1/5/10/15/20/25%
  do agregado normal), cor = pico de CPU, ▲ = SCALE_UP disparado, "% to"
  anotado quando taxa de timeout > 0.5%. Salvo em
  graficos_apresentacao/02_caracterizacao_ataque_wedos/
  48_atacantes_intensidade_grid.png.

Achados da triagem (1 rep, olhar com cautela antes de repetir):
  - S1 (40 agregado): único cenário onde a maioria das células fica limpa
    (sem timeout); SCALE_UP começa a aparecer a partir de ~10% de
    intensidade, mais cedo com menos atacantes concentrando mais RPS
    cada.
  - S2 (100 agregado): SCALE_UP em quase todas as células a partir de 1%;
    timeout ainda baixo (<3%) na maioria, só sobe visivelmente em
    combinações com mais atacantes.
  - S3 e S4: já mostram timeout de 30-100% em várias células a partir de
    ~10-15% de intensidade -- essas duas confirmam a instabilidade de
    baseline conhecida (S4 já documentada; S3 também aparenta estar perto
    do limite mesmo antes do teto de 25%). Resultados de S3/S4 nesse teste
    precisam ser lidos com essa ressalva.
  - Efeito do nº de atacantes isoladamente é ruidoso com 1 rep só (ex.:
    S1/1 atacante tem 15%=46% < 10%=63%, não-monotônico) -- não dá pra
    tirar conclusão forte sobre "mais atacantes = mais furtivo" sem
    repetir.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta sobre os dados reais das 72 execuções, sem
    nenhum arquivo faltando.
  - PNG inspecionado visualmente.

Próximo passo (não feito, decisão do usuário): se os achados acima
  justificarem, repetir com mais reps (a triagem de 1 rep já indica que
  S3/S4 são instáveis mesmo com pouco ataque, então repetições ali
  importam mais do que em S1/S2).

================================================================================
§87. Lote 2 do grid atacantes×intensidade: S5-S8 (20/80/160/320 agregado)
================================================================================
Contexto: usuário pediu mais 4 pontos de RPS agregado normal para o grid
  atacantes×intensidade (gráfico 48), com o objetivo de ter mais resolução
  ao longo da curva (S1-S4 = 40/100/200/300 já rodados). Confirmado com o
  usuário: soma aos 4 cenários existentes (8 no total), não substitui.

Alterado run_atacantes_intensidade_grid.py: NORMAL_SCENARIOS trocado para
  {"S5":20,"S6":80,"S7":160,"S8":320} (lote 2) -- mesmo arquivo, mesmo
  RESULTS_DIR (experiment_results/atacantes_intensidade_grid/), sem risco
  de colisão de nome já que o nome do arquivo inclui o cenário. Comentário
  do topo do arquivo atualizado explicando os 2 lotes.

Alterado plot_atacantes_intensidade_grid.py: NORMAL_SCENARIOS agora lista
  os 8 cenários (S1-S8); layout do heatmap mudou de 1x4 para 2x4 painéis
  (grid de eixos com os painéis extras desligados se sobrarem, embora
  8/4=2 encha exatamente as 2 linhas).

S8=320 já está acima do teto de capacidade medido do simulador (~205-210
  agregado, WU=10) -- mesma ressalva de instabilidade de baseline já
  documentada para S4=300 (§84/86): ler os resultados de S8 com cautela.

Disparado em background (nohup, PID 85184) -- 72 execuções novas
  (4 cenários x 3 atacantes x 6 intensidades x 1 rep, WU=200.000, ~3,6h +
  overhead). Log em scratchpad/atacantes_intensidade_grid_s5s8.log.

Verificado:
  - python3 -m py_compile nos 2 scripts -- sem erro.
  - docker ps OK antes de disparar.
  - Primeira execução do lote (S5, 1 atacante, 1%) iniciou e logou
    corretamente.

================================================================================
§88. Gráfico 48 regenerado com 8 cenários (S1-S8)
================================================================================
Contexto: continuação do §87 -- lote 2 (S5-S8: 20/80/160/320 agregado)
  terminou (72/72 execuções). Gráfico 48 regenerado com plot_
  atacantes_intensidade_grid.py já ajustado pro layout 2x4.

Achados novos (mesma ressalva de 1 rep só):
  - S5 (20 agregado, o mais leve de todos): ÚNICO cenário que nunca
    escala em nenhuma célula, nem em 25% de intensidade com 1 atacante --
    fica sempre abaixo do limiar de 60%. Complementa bem o extremo "leve"
    da curva, que antes começava em S1=40 (que já escala a partir de 10%).
  - S6 (80 agregado): comportamento intermediário entre S1 e S2, como
    esperado pela posição entre os dois na curva de RPS.
  - S7 (160 agregado): já bem instável a partir de ~15-20% de intensidade
    (timeout de até 95%), parecido com S3.
  - S8 (320 agregado, o mais pesado): o cenário mais instável de todos --
    até com 1 atacante e só 5% de intensidade já aparece timeout de
    13-100% em várias células. Mais extremo que S4 (300), coerente com
    estar ainda mais acima do teto de capacidade (~205-210 agregado).
  - Com os 8 pontos, fica mais visível uma progressão contínua: a partir
    de ~80-100 agregado (S2/S6) o ataque já força SCALE_UP na maioria das
    células mesmo em baixa intensidade; a partir de ~160-200 (S3/S7) o
    sistema já entra em timeout significativo mesmo sem muito ataque.

Verificado:
  - Rodado de ponta a ponta sobre as 144 execuções totais (72+72), sem
    nenhum arquivo faltando.
  - PNG (2x4, 8 painéis) inspecionado visualmente -- layout limpo, sem
    sobreposição de texto.

================================================================================
§89. plot_host_saturacao_grid48.py (novo) -- gráfico 49, prova de
  saturação do host aplicada às 144 células do grid 48 -- refuta a
  hipótese de "saturação de CPU" para a maior parte da grade
================================================================================
Contexto: usuário perguntou o que causa a instabilidade/timeout nos
  cenários de RPS agregado mais alto do gráfico 48 (S3/S4/S7/S8, acima ou
  perto do teto de capacidade ~205-210 agregado). Expliquei o mecanismo já
  documentado (gráfico 19, seção 56): saturação real dos 8 núcleos do host
  (MAX_INSTANCES=4 x INSTANCE_MAX_CONCURRENT_REQUESTS=2 = nproc=8) faz
  requisições não terminarem dentro do timeout de 2s do cliente. Usuário
  pediu prova equivalente aplicada ao gráfico 48 inteiro (opção B: grade
  completa, não só células pontuais).

Criado plot_host_saturacao_grid48.py: mesma estrutura de grid do gráfico
  48 (S1-S8 x atacantes x intensidade, mesmos CSVs de
  experiment_results/atacantes_intensidade_grid/, sem novo Docker). Cor da
  célula = % do tempo do run (excluindo t=0) com host_cpu_percent >= 95%
  (mesmo critério "gruda no teto" do gráfico 19). Anotação = pico de
  host_cpu_percent do run + % de timeout (mesmo cálculo do gráfico 48) +
  ▲ de SCALE_UP. Salvo graficos_apresentacao/02_caracterizacao_ataque_wedos/
  49_host_saturacao_grid48.png. Não sobrescreve nem altera o gráfico 48
  nem nenhum script existente.

ACHADO PRINCIPAL (contradiz a explicação dada ao usuário nesta mesma
  conversa, baseada no gráfico 19): a saturação real do host (>=95%
  sustentado) só aparece em UMA região da grade inteira -- S8, 4
  atacantes, intensidade 20-25% (63% e 20% do tempo saturado,
  respectivamente). Em TODO o resto da grade, incluindo células com
  timeout de 90-100%, o pico de host_cpu_percent fica na faixa de 40-70%,
  longe de saturar os 8 núcleos. Exemplo mais gritante: S4/1 atacante/15%
  de intensidade -- pico de host_cpu_percent de apenas 42,42%, com 100%
  de timeout (nenhuma requisição completou). Mesmo padrão em S3, S4, S7 e
  na maior parte de S8.

Interpretação: o gráfico 19 usava WU=400.000 (custo por requisição bem
  mais pesado); o grid 48 usa WU=200.000. Nessa faixa mais leve de custo
  por requisição, o colapso de timeout NÃO é explicado por saturação de
  CPU do host -- outro mecanismo domina (hipóteses ainda não testadas,
  discutidas com o usuário: esgotamento de concorrência por instância via
  fila do ProcessPoolExecutor com INSTANCE_MAX_CONCURRENT_REQUESTS=2 mesmo
  sem CPU média alta; esgotamento de portas/keep-alive já documentado em
  sessão anterior; ou janela de timeout concentrada logo no início do
  ataque, antes do SCALE_UP completar, diluída pela média de todo o run de
  180s). Nenhuma dessas hipóteses foi investigada ainda -- próximo passo
  pendente de decisão do usuário.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta sobre as 144 execuções já em disco (nenhum
    arquivo faltando, coluna host_cpu_percent presente em todos -- coleta
    já era posterior à seção 37).
  - PNG (2x4, 8 painéis) inspecionado visualmente.

================================================================================
§90. analyze_fila_processpool.py (novo) -- teste da hipótese de fila/backlog
  na célula S4/1 atacante/15% -- hipótese REFUTADA para as requisições
  bem-sucedidas
================================================================================
Contexto: usuário pediu para investigar primeiro a hipótese de fila do
  ProcessPoolExecutor (levantada na seção 89) como causa do timeout de
  100% em S4/1 atacante/15% sem saturação de host CPU (pico 42%).

Antes de criar o script, inspeção manual dos CSVs brutos já achou evidência
  indireta forte a favor de ALGUM tipo de backlog: attack_summary_log dessa
  célula mostra 0 sucessos em ambas as janelas do ataque (416 + 4784 erros,
  100% de falha); normal_traffic_summary_log mostra só 1 das 4 threads de
  cliente concluindo seu resumo final (as outras 3 não aparecem no
  arquivo) -- consistente com threads presas no loop de drenagem de
  pending_futures (`.result(timeout=3)` por future) quando esse loop não
  termina a tempo do `join(timeout=0.1)` do processo principal, sinal de
  fila grande nesse ponto.

Criado analyze_fila_processpool.py: reconstrói, a partir de rtt_log.csv
  (só requisições BEM-SUCEDIDAS -- início = timestamp-rtt, fim = timestamp),
  o nº de requisições simultaneamente "em voo" via varredura de eventos, e
  compara com a capacidade dinâmica do servidor (num_instances x
  INSTANCE_MAX_CONCURRENT_REQUESTS, de metrics.csv) e com o limite do
  _send_pool do cliente (HTTP_NORMAL_MAX_CONCURRENT_SENDS=64, config.py).
  Plota concorrência reconstruída + RTT ao longo do tempo (2 painéis).
  Salvo graficos_apresentacao/02_caracterizacao_ataque_wedos/
  50_fila_processpool_S4_att1_15pct.png. Só leitura de dados já em disco,
  nenhum Docker novo, nenhum arquivo do simulador alterado.

RESULTADO (refuta a hipótese, para a população que sucede): a concorrência
  reconstruída tem um pico de 28 logo no início (t~0-10s, warmup) e depois
  fica quase sempre abaixo de 15 pelo resto do run inteiro (180s) -- nunca
  chega perto do limite do _send_pool (64) nem sobe de forma sustentada
  acima da capacidade do servidor. O RTT das 22.444 requisições
  bem-sucedidas fica estável em ~40-60ms do início ao fim, SEM nenhuma
  tendência de subida (nenhum "creep" em direção ao timeout de 2000ms) --
  nem durante nem depois da janela de ataque (t=20-160s). Ou seja: entre as
  requisições que dão certo, não há sinal nenhum de fila crescendo.

Limitação importante do método: rtt_log só registra requisições
  bem-sucedidas -- as que falham/timeout (100% do ataque, ~52% do tráfego
  normal nessa célula) não têm timestamp/duração registrados em lugar
  nenhum, então esse método não pode ver diretamente o que acontece com
  elas. A leitura mais provável, dado o padrão observado (sucesso rápido
  e estável OU falha total, sem meio-termo/degradação gradual), é que o
  mecanismo de falha é mais parecido com uma rejeição binária/imediata no
  nível de conexão (ex.: conexão recusada/resetada, esgotamento de portas)
  do que com uma fila de computação crescendo aos poucos -- oposto do que
  a hipótese desta seção previa.

Próximo passo sugerido ao usuário (não decidido ainda): investigar
  traffic_capture.csv (captura tcpdump já existente dessa mesma célula)
  procurando sinais no nível TCP -- pacotes RST, contagem de portas de
  origem únicas ao longo do tempo (mecanismo de esgotamento de portas/
  keep-alive já mencionado em sessão anterior, ver [[project_ml_dataset_plan]]
  na memória) -- para testar a hipótese de rejeição de conexão em vez de
  fila de computação.

================================================================================
§91. analyze_port_churn.py (novo) -- teste de port churn na mesma célula --
  sinal sugestivo, mas com problema de confiabilidade da captura; hipótese
  refinada para saturação do _send_pool do CLIENTE (não do servidor)
================================================================================
Contexto: usuário pediu para seguir com a investigação de port
  churn/esgotamento de portas (próximo passo sugerido na seção 90). Antes
  de rodar, checado tcpdump_sniffer.py: o tcpdump roda com a flag -q (modo
  quieto) -- flags TCP (SYN/RST/FIN) NÃO são capturadas em
  traffic_capture.csv, só IP:porta e tamanho. Contagem de RST direta não é
  possível com os dados já coletados; port churn (portas de origem únicas
  por janela / total de pacotes cliente->servidor por janela) é o proxy
  disponível.

Criado analyze_port_churn.py: filtra traffic_capture.csv por pacotes
  cliente->servidor (dst_port numa das portas publicadas das instâncias,
  STARTING_HOST_PORT..+MAX_INSTANCES-1), agrupa em janelas de 5s (mesmo
  passo do MONITOR_INTERVAL_SECONDS) e calcula portas_unicas/pacotes por
  janela. Salvo graficos_apresentacao/02_caracterizacao_ataque_wedos/
  51_port_churn_S4_att1_15pct.png. Mesma célula da seção 90, só leitura de
  dados já em disco.

RESULTADO: a razão portas_únicas/pacotes sobe de forma bastante
  consistente ao longo do run -- de ~0,07-0,16 nos primeiros 20s para
  ~0,40-0,55 nos últimos 20-30s (quase 4x). Á primeira vista, sinal a
  favor da hipótese de esgotamento de keep-alive crescendo com o tempo.

RESSALVA IMPORTANTE que descarta conclusão forte: o volume total de
  pacotes cliente->servidor capturados nessa célula (10.810) é
  implausivelmente baixo -- menor até que o número de requisições
  BEM-SUCEDIDAS sozinho (22.444, da seção 90), que já exigiria pelo menos
  22.444 pacotes GET só do lado cliente->servidor, sem contar as
  requisições que falharam. Além disso, o total de pacotes por janela de
  5s CAI de forma constante ao longo do run (de ~500-568 no início para
  ~150-200 no final), o que não é coerente com uma carga de ataque+normal
  configurada para ficar constante o run inteiro -- é assinatura de o
  tcpdump (rodando via subprocess + leitura linha a linha em Python,
  tcpdump_sniffer.py) estar perdendo cada vez mais pacotes conforme a
  carga sobe, não de queda real de tráfego na rede. Ou seja: a tendência
  de subida do port churn pode ser um sinal real, mas também pode ser
  (parcial ou totalmente) artefato de captura cada vez mais incompleta --
  os dados atuais não permitem distinguir as duas coisas com confiança.

Hipótese refinada (não testada ainda, raciocínio a partir do padrão
  observado nas seções 90+91 combinadas): o gargalo pode estar no
  _send_pool do PRÓPRIO CLIENTE (ThreadPoolExecutor com
  HTTP_NORMAL_MAX_CONCURRENT_SENDS/HTTP_ATTACK_MAX_CONCURRENT_SENDS=64,
  fila interna sem limite de tamanho, config.py), não no
  ProcessPoolExecutor do servidor nem na CPU do host: se uma fração das
  tentativas de conexão passa a demorar até travar no timeout de 2s (por
  algum motivo ainda não identificado -- ex.: limite de conexões
  aceitas/backlog do socket do servidor, esgotamento de portas efêmeras no
  loopback, ou outro fator), essas tentativas ocupam uma thread do
  _send_pool por até 2s cada; com throughput alto o bastante, as 64
  threads do pool ficam dominadas por tentativas fadadas a falhar,
  reduzindo o número de tentativas NOVAS que conseguem ser despachadas por
  segundo -- o que explicaria ao mesmo tempo: (a) baixa concorrência
  reconstruída entre sucessos (seção 90, porque sucessos são raros e
  rápidos, não competem por muito tempo), (b) queda no volume de pacotes
  capturados ao longo do tempo (throughput real caindo, não só artefato de
  captura), e (c) threads de tráfego normal não conseguindo drenar
  pending_futures a tempo de logar o resumo final (seção 90).

Próximo passo sugerido ao usuário (não decidido, requer mudança de código
  + novo Docker para validar com precisão): instrumentar
  traffic_injectorV0.py/normal_traffic.py para logar periodicamente
  `_send_pool._work_queue.qsize()` e/ou nº de threads ocupadas -- mede o
  gargalo diretamente, sem depender da confiabilidade da captura tcpdump.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta sobre os dados já em disco dessa célula.
  - PNG (2 painéis) inspecionado visualmente.
  - Números citados (10.810 pacotes c2s, queda de ~500 para ~150-200
    pacotes/janela, razão subindo de ~0,1 para ~0,5) conferidos no
    console/tabela impressa, não é leitura visual aproximada.

================================================================================
§92. Causa raiz encontrada (sem código novo): autoscaler baseado em CPU fica
  preso/oscila em nº de instâncias insuficiente -- explica o colapso sem
  precisar de fila, port churn ou saturação de host CPU
================================================================================
Contexto: antes de implementar a instrumentação do _send_pool combinada na
  seção 91, reexaminei a série completa de num_instances/decision de
  metrics_S4_atk15pct_att1_wu200000_rep1.csv (mesma célula das seções
  90-91) e de mais 3 células (S4/S3/S7 atk20pct att1) para checar se a
  resposta já estava nos dados existentes antes de gastar Docker/código
  novo.

Achado (célula S4/1 atacante/15%, a mais gritante):
  t=20s: 2 instâncias, CPU média=15,86%, NO_ACTION
  t=25s: SCALE_DOWN (CPU=21,93% < CPU_THRESHOLD_SCALE_DOWN=25%) -- cai de
    2 para 1 instância
  t=25s a t=180s (155 dos 180s do run, 86% do tempo): PRESO em 1 instância
    só -- CPU média oscila entre 15% e 56% pelo resto do run, nunca mais
    cruza os 60% de CPU_THRESHOLD_SCALE_UP, então nenhum SCALE_UP novo é
    disparado.

Com 1 instância só tentando atender ~345 req/s de demanda combinada (300
  normal + 45 de ataque a 15%) contra o teto estrutural por instância de
  ~150 req/s JÁ DOCUMENTADO em config.py (comentário de
  INSTANCE_MAX_CONCURRENT_REQUESTS, sessão 12/08: aumentar o pool de
  processos de 2->4->8 workers não melhora a taxa de erro -- suspeita
  registrada na época era contenção de GIL ou o thread único de
  gerenciamento de resultados do ProcessPoolExecutor, nunca confirmada com
  certeza), o resultado é exatamente o observado: colapso quase total de
  throughput SEM precisar de CPU alta, porque esse gargalo específico não
  consome CPU proporcionalmente -- ele derruba requisições sem aquecer os
  núcleos.

Checagem de generalização (S3/1 atacante/20%, 97% timeout, pico CPU 50%):
  padrão mais complexo mas mesma raiz -- o autoscaler fica OSCILANDO o run
  inteiro (SCALE_UP -> CPU despenca porque a carga foi dividida
  corretamente entre mais instâncias -> SCALE_DOWN por "ociosidade" ->
  sobrecarrega nas instâncias restantes -> SCALE_UP de novo), repete esse
  ciclo pelo menos 4 vezes (t=25/55/75/100/120/145) e termina preso em 1
  instância nos últimos 30s também. S4/S7 atk20pct_att1 (checados só a
  trajetória, não os timeouts) mostram comportamento mais estável (chegam
  a 3-4 instâncias e ficam lá) -- ou seja, o padrão de "preso/oscila em
  poucas instâncias" NÃO acontece em toda célula do grid, mas explica bem
  as piores (S3/S4 em faixas específicas de intensidade).

Interpretação: CPU% é uma métrica CEGA para esse tipo de colapso -- quando
  a demanda ultrapassa o teto estrutural por instância, o sintoma não é
  CPU alta, é menos trabalho realmente concluído, o que a métrica lê como
  "tenho capacidade sobrando" e reage reduzindo capacidade ainda mais
  (SCALE_DOWN no pior momento possível) ou entra em oscilação (sobe,
  CPU cai por divisão de carga, desce de novo, sobrecarrega, repete). Essa
  é uma explicação mais completa e melhor sustentada pelos dados do que as
  hipóteses de fila (seção 90, refutada) ou port churn (seção 91,
  inconclusiva) -- e é relevante para o tema central da monografia (o
  autoscaler é vulnerável a um modo de falha que a métrica de CPU não
  consegue nem detectar nem corrigir).

Decisão: NÃO implementada a instrumentação do _send_pool combinada na
  seção 91 (sendpool_logger.py) -- ficou desnecessária, a causa já estava
  determinável nos dados existentes (coluna decision/num_instances de
  metrics.csv, já coletada desde o início do grid). Nenhum arquivo do
  simulador foi alterado nesta seção.

Verificado:
  - Números de CPU/decision/num_instances lidos diretamente dos CSVs
    (cut/column no console), não é leitura visual de gráfico.
  - Checado em 4 células (S4_atk15pct_att1, S3/S4/S7_atk20pct_att1) antes
    de generalizar -- explicitamente registrado que o padrão não é
    universal em toda a grade, só nas células mais afetadas.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta sobre os dados já em disco dessa célula (nenhum
    arquivo faltando).
  - PNG (2 painéis) inspecionado visualmente.
  - Números citados acima (pico=28, 0% de eventos acima de 64) conferidos
    no console, não é leitura visual aproximada do gráfico.
  - Tabela de valores brutos (sat_pct, peak_host_cpu, timeout_pct por
    célula) inspecionada no console para as linhas S3/S4 antes de reportar
    o achado ao usuário -- confirma os números citados acima célula a
    célula, não é leitura visual aproximada do heatmap.

================================================================================
§93. plot_cpu_vs_carga_paradoxo.py (novo) -- gráfico 52, pico de CPU x carga
  ofertada colorido por timeout -- sustenta pra banca o padrão
  "mais carga = pico de CPU menor" nas células que colapsam
================================================================================
Contexto: usuário perguntou como responder à banca/orientador sobre por
  que, em cenários com mais RPS agregado ou mais intensidade, o pico de
  CPU do gráfico 48 às vezes é MENOR do que em cenários mais leves.
  Expliquei que pico de CPU mede trabalho concluído, não carga ofertada --
  sob colapso (seção 92), a maior parte do tráfego em excesso falha antes
  de virar trabalho de CPU. Usuário pediu um gráfico pra sustentar esse
  argumento visualmente.

Criado plot_cpu_vs_carga_paradoxo.py: junta as 144 células do grid 48 num
  scatter único -- eixo X = carga total ofertada (rps agregado normal +
  rps agregado de ataque), eixo Y = pico de average_cpu_percent (container,
  mesma métrica de cor do gráfico 48 original), cor = % de timeout, contorno
  preto nos pontos com timeout > 50% (colapso). Linhas de referência nos
  limiares de SCALE_UP (60%) e SCALE_DOWN (25%). Salvo
  graficos_apresentacao/02_caracterizacao_ataque_wedos/
  52_cpu_vs_carga_paradoxo.png. Só leitura de dados já em disco.

ACHADO MAIS FORTE (melhor que o gráfico em si -- comparação isolando
  exatamente a variável certa): carga_ofertada = agg + agg*pct/100 não
  depende do nº de atacantes, então células com o MESMO agregado e MESMA
  intensidade mas atacantes diferentes têm carga ofertada IDÊNTICA.
  Exemplo (S4, 15% de intensidade, carga=345 rps em todos os 3 casos):
    1 atacante:  pico CPU=76,21%   timeout=100,0%   <- colapsa
    2 atacantes: pico CPU=216,74%  timeout=15,4%    <- funciona bem
    4 atacantes: pico CPU=212,31%  timeout=28,5%    <- funciona razoável
  Com carga total IDÊNTICA, o caso de 1 atacante colapsa (autoscaler cai
  na armadilha de SCALE_DOWN da seção 92) enquanto 2 e 4 atacantes -- MESMA
  carga, só distribuída diferente -- processam de verdade e mostram pico de
  CPU quase 3x maior. Prova direta e isolada de que o pico de CPU baixo não
  vem de "menos carga", vem de menos trabalho sendo concluído por causa da
  decisão errada do autoscaler -- a mesma carga, com sorte de timing
  diferente, produz resultados opostos.

Ressalva anotada: o "pico de CPU" usado aqui (e no gráfico 48 original) é
  o máximo de average_cpu_percent sobre o run inteiro (180s) -- no caso
  S4/1 atacante/15%, esse pico de 76,21% vem na verdade do transiente
  inicial (t=10s, ainda com 2 instâncias, ANTES do SCALE_DOWN fatal em
  t=25s), não do período de colapso em si (que fica na faixa de 15-56%,
  seção 92). Ou seja, mesmo o "pico" reportado já está subestimando o
  quanto a métrica fica cega DURANTE o colapso propriamente dito -- se
  olhássemos só a janela pós-colapso, o contraste seria ainda maior.

Verificado:
  - python3 -m py_compile -- sem erro.
  - Rodado de ponta a ponta sobre as 144 células já em disco.
  - PNG inspecionado visualmente.
  - Tabela completa impressa no console conferida para a faixa
    carga_ofertada=300-350 (região de S4/S8) antes de reportar o exemplo
    acima -- números exatos, não leitura aproximada do gráfico.
